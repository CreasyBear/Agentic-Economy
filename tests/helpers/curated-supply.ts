/**
 * Cluster A/B/C are evicted from the product catalog. Tests that need an
 * admitted operation publish a local fixture; they do not seed curated packs.
 */
export const CURATED_CAPABILITY_IDS = [] as const
