Rule: react-doctor/zod-v4-prefer-top-level-string-formats
Severity: warning
Category: Maintainability
Count: 1

This `z.string().<format>()` check is deprecated in Zod 4, so it can break during the upgrade.

Suggestion: Use the Zod 4 top-level format checks like `z.email()`, `z.uuid()`, or `z.ipv4()` instead of `z.string().<format>()`.

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/zod-v4-prefer-top-level-string-formats

Files:
  src/modules/billing/billing.functions.ts:112
