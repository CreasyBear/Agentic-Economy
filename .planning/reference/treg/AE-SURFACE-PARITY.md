# Treg → AE surface parity matrix

| Scope/source evidence | Value |
|---|---|
| AE target | Current working tree requested at `ac857aef0` plus tracked/untracked WIP; evidence limited to `src/`, `convex/`, `tools/ae/`, `tests/`, routes, package/config. |
| Treg source pin | `603540f653994080d4f507a9a3564e1017c28eef`, timestamp `2026-08-22T10:01:05+10:00`; reference inventory: `.planning/reference/treg/SURFACES.md`, `.planning/reference/treg/INTERACTIONS.md`. |
| Classification | `MATCHED`, `PARTIAL`, `MISSING`, `DIFFERENT-BY-DESIGN`, or `AE-EXTRA`; consequence is current behavior, not a proposed design. |

| Treg surface | AE current counterpart | Status | Exact AE evidence | Observable consequence |
|---|---|---|---|---|
| Web landing/catalog: `/`, `/catalog`, shelves/search | `/`, `/market`, `/api/v1/market-operations/search` | PARTIAL | `src/routes/index.tsx`, `src/routes/market.tsx`, `src/modules/registry/operation-paths.ts` | Anonymous landing and operation search work; AE catalog is operation/business oriented rather than Treg platform shelves. |
| Web detail: catalog endpoint/provider detail | `/$slug`, registry detail routes, `/api/v1/market-operations/detail` | PARTIAL | `src/routes/$slug.tsx`, `src/routes/api.v1.market-operations.detail.ts`, `src/modules/registry/operations.actions.ts` | Public business detail and operation detail exist; Treg endpoint access/examples semantics are not one-for-one. |
| Web dashboard: `/app` Tools/Secrets/Marketplace/Activity/Usage/Team | operator shell and agent workspace | PARTIAL | `src/routes/_operator.tsx`, `src/components/ae/layout/AeOperatorSidebar.tsx`, `src/routes/_operator/agent-access.tsx`, `src/components/ae/console/AeAgentOperatorConsole.tsx` | Authenticated users see role-scoped owner/agent/admin views, not one unified Treg dashboard. |
| Web onboarding: demo/team setup and invite entry | public agent/provider guides and Clerk sign-in | PARTIAL | `src/routes/for-agents.tsx`, `src/routes/for-providers.tsx`, `src/routes/sign-in.$.tsx`, `src/routes/sign-up.$.tsx` | Setup is guide → access request or provider supply; no anonymous demo-org/team creation path. |
| Web docs/tutorial: `/tutorial`, markdown quickstarts/help | `/for-agents`, `/for-providers`, `/SKILL.md`, `/llms.txt` | PARTIAL | `src/routes/for-agents.tsx`, `src/routes/for-providers.tsx`, `src/routes/SKILL[.]md.ts`, `src/routes/llms[.]txt.ts` | Agent-readable setup and recovery exist; Treg’s tutorial route family is not present. |
| CLI setup/catalog/spend: `config`, `login`, `catalog`, `call`, `balance`, `topup` | `manifest`, `search`, `inspect`, `compare`, `inspect-plan`, `connect`, `fund`, `invoke` | PARTIAL | `package.json`, `tools/ae/cli.ts`, `tools/ae/commands/manifest.ts`, `tools/ae/commands/fund.ts` | AE CLI covers machine discovery and typed operation execution; Treg’s org-scoped catalog/call/balance syntax is different. |
| CLI org/vault/import families: `org`, `secret`, `tool`, `skill`, `connections`, `scan`, `upload` | no corresponding root command family | MISSING | `tools/ae/cli.ts`, `tools/ae/commands/` (registered commands) | AE CLI cannot manage Treg-style org vault/resources, scan local credentials, or upload a Treg skill/tool bundle. |
| CLI execution/proxy: `run`, `shell`, `serve`, bare-program interception | typed operation commands | DIFFERENT-BY-DESIGN | `tools/ae/commands/invoke.ts`, `tools/ae/commands/status.ts`, `src/modules/capability-execution/operation-invoke-entry.ts` | AE sends an operation contract and idempotency key; it does not transparently intercept arbitrary upstream hosts. |
| HTTP public catalog reads and `POST /tool-requests` | registry and market operation POST reads | PARTIAL | `src/routes/api.v1.registry.ts`, `src/routes/api.v1.services.ts`, `src/routes/api.v1.market-operations.search.ts`, `src/routes/api.v1.market-operations.detail.ts` | Public machine discovery is available; Treg’s GET catalog and tool-request resource shapes are not exposed verbatim. |
| HTTP `/call/{rest:path}` proxy | `/api/v1/operations/call` | DIFFERENT-BY-DESIGN | `src/routes/api.v1.operations.call.ts`, `src/lib/server/operation-invoke-api.ts`, `src/modules/capability-execution/operation-invoke-entry.ts` | AE invokes a published operation through auth/admission; no arbitrary URL, named-tool path, or transparent upstream relay endpoint. |
| HTTP call status/runs: `/calls`, `/runs` | invocation status/cancel/reconcile | PARTIAL | `src/routes/api.v1.operations.$invocationRef.ts`, `src/routes/api.v1.operations.$invocationRef.cancel.ts`, `src/routes/api.v1.operations.$invocationRef.reconcile.ts` | AE exposes durable invocation state and recovery actions; Treg call/run list and receipt shapes are not equivalent. |
| MCP stable tools: `catalog_search`, `catalog_get`, `call`, `balance`, `my_tools`, `catalog_request` | dynamically registered `ae_*` action tools | PARTIAL | `src/routes/mcp.ts`, `src/lib/server/mcp-api.ts`, `src/modules/actions/index.ts` | `/mcp` is streamable HTTP with anonymous read-only gating and authenticated actions; tool names and schema set differ. |
| MCP OAuth/resource authorization and install flow | AE OAuth metadata, device/authorization-code grants | PARTIAL | `src/routes/[.]well-known/oauth-protected-resource.ts`, `src/routes/[.]well-known/oauth-authorization-server.ts`, `src/routes/oauth.*`, `src/lib/server/mcp-api.ts` | OAuth challenges and grant exchange exist; Treg’s team-pinned MCP token/install behavior is not the AE contract. |
| Discovery files: `/llms.txt`, `/skill.md`, UCP/skills index | `/llms.txt`, `/SKILL.md`, `/.well-known/ucp` | PARTIAL | `src/routes/llms[.]txt.ts`, `src/routes/SKILL[.]md.ts`, `src/routes/[.]well-known/ucp.ts`, `src/modules/discovery/internal/discovery-files.ts` | AE exposes machine-readable discovery with uppercase `SKILL.md`; Treg’s lowercase skill and skills index are not all present. |
| Install/plugin surfaces: `/install.sh`, skill installer, Claude/Codex/Cursor artifacts | no installer/plugin route; npm CLI package | MISSING | `package.json`, `tools/ae/README.md`, `src/routes/SKILL[.]md.ts` | Agents must obtain/run the repository CLI or follow docs; no Treg-style one-command install/plugin artifact is served. |
| Human identity: GitHub/Google/email OTP and session | Clerk sign-in/up | PARTIAL | `src/routes/sign-in.$.tsx`, `src/routes/sign-up.$.tsx`, `src/lib/server/` Clerk integration | Human sign-in exists with Clerk; provider set and session/org onboarding differ from Treg. |
| Teams/access: org creation, roles, invites, projects, deny rules | owner/operator role gates and agent access | MISSING | `src/lib/operator/route-options.ts`, `src/routes/_operator/owner.supply.tsx`, `src/routes/_operator/agent-access.tsx`, `src/modules/agent-access/agent-access.functions.ts` | AE has owner/operator and caller-key controls, but no Treg-equivalent org member/invite/project/deny-rule API family. |
| Grants/agent identity: team agent tokens and grant reassignment | device/authorization-code grant issuing AE caller key | PARTIAL | `src/modules/agent-access/oauth-state.ts`, `src/routes/oauth.device_authorization.ts`, `src/routes/oauth.token.ts`, `convex/agentAccessOAuth.ts` | Agent access is scoped by authority/mode and expires; it is not a Treg org token with team reassignment. |
| Catalog search | operation search | MATCHED | `src/routes/api.v1.market-operations.search.ts`, `tools/ae/commands/search.ts`, `src/modules/registry/operations.actions.ts` | Anonymous callers can search and recover from bounded/read failures via problem responses. |
| Catalog get/detail | operation detail | MATCHED | `src/routes/api.v1.market-operations.detail.ts`, `tools/ae/commands/inspect.ts`, `src/modules/registry/operations.actions.ts` | Anonymous callers can inspect exact operation schema/terms before any paid call. |
| Catalog compare | operation compare | MATCHED | `src/routes/api.v1.market-operations.compare.ts`, `tools/ae/commands/compare.ts` | Multiple operation refs can be compared without invoking providers. |
| Catalog access decision | inspect-plan | AE-EXTRA | `src/routes/api.v1.market-operations.inspect-plan.ts`, `tools/ae/commands/inspect-plan.ts`, `src/modules/registry/operations.actions.ts` | AE exposes a preflight plan/authority decision surface beyond Treg’s catalog endpoint comparison. |
| Call execution | operation invoke | PARTIAL | `src/routes/api.v1.operations.call.ts`, `tools/ae/commands/invoke.ts`, `src/modules/capability-execution/operation-invoke.actions.ts` | Authenticated calls enforce operation authority, stable body `idempotencyKey`, and durable invocation states; no Treg full-URL call syntax. |
| Credential ladder: org tool → org secret → platform key | supplier connection/credential admission and payment modes | PARTIAL | `src/modules/capability-supply/`, `src/modules/money/`, `src/lib/server/operation-invoke-api.ts`, `src/modules/agent-access/agent-access.functions.ts` | AE keeps supplier credentials separate from caller keys and admits declared operation credentials; Treg’s exact BYOK/org-secret/platform fallback is not exposed as the same ladder. |
| Balance and top-up: `/balance`, `/billing/topup`, `treg fund` analogue | authenticated credit panel and `fund` command | PARTIAL | `src/components/ae/console/AeCreditTopUpPanel.tsx`, `src/modules/money/internal/credit-topup-http.ts`, `tools/ae/commands/fund.ts` | Stripe-backed credit can be started/read back; missing account/config leaves balance unchanged and shows refusal copy. |
| Reserve/settle/release and 402 | paid operation usage/x402 and money ledger | PARTIAL | `src/modules/money/internal/ledger.ts`, `convex/moneyLedger.ts`, `src/modules/capability-supply/internal/route-transport-x402-payment.ts`, `src/lib/server/operation-invoke-api.ts` | AE can refuse paid admission, record charge state, or expose outcome uncertainty; its response and ledger contract are not Treg’s micro-USD hold headers. |
| Receipts/audit: calls/runs/activity/billing history | recent activity, gateway telemetry, admin audit events | PARTIAL | `src/components/ae/console/AeAgentOperatorConsole.tsx`, `src/lib/server/gateway-telemetry.ts`, `src/routes/_operator/admin.audit-events.tsx` | Users see invocation/credit activity and admins see audit readback; no Treg receipt header or `/calls/{call_ref}` shape. |
| Idempotent replay | stable operation idempotency and durable status | PARTIAL | `src/lib/server/operation-invoke-api.ts`, `src/modules/capability-execution/operation-invoke-entry.ts`, `tests/unit/action-invocation/dynamic-published-operation-recovery.test.ts` | Same invocation identity supports status/recovery; AE does not advertise Treg’s replay header/cost response semantics. |
| Reconcile/recover | `recover`, `/reconcile`, cancellation | PARTIAL | `tools/ae/commands/recover.ts`, `src/routes/api.v1.operations.$invocationRef.reconcile.ts`, `src/routes/api.v1.operations.$invocationRef.cancel.ts`, `tests/unit/capability-execution/operation-recovery-actions.test.ts` | Unknown outcome can be reconciled before retry; refusal/terminal states remain durable. |
| Health: `/health`, `/health/run` | `/api/health`, `/api/ready`, admin index health | PARTIAL | `src/routes/api.health.ts`, `src/routes/api.ready.ts`, `src/routes/_operator/admin.index-health.tsx`, `tests/e2e/developer-discovery.spec.ts` | AE exposes liveness/readiness and catalog health; Treg per-credential health-run/reconnect behavior is not the same surface. |
| Provider publication: tools/connections/skills and vendor listing | owner supply/offering publication and public business/operation registry | PARTIAL | `src/routes/for-providers.tsx`, `src/routes/_operator/owner.supply.tsx`, `src/routes/_operator/owner.offerings.tsx`, `src/modules/capability-supply/supply-funnel.functions.ts`, `src/modules/capability-supply/internal/publication-importer-openapi.ts` | Providers publish declared operations with readiness/terms/evidence; AE publication is not Treg’s tool/secret/skill bundle model. |
| Admin/super-admin: platform stats/orgs/users/tools/calls/reconcile | admin index health/runs/audit | PARTIAL | `src/routes/_operator/admin.index-health.tsx`, `src/routes/_operator/admin.runs.tsx`, `src/routes/_operator/admin.audit-events.tsx`, `src/lib/operator/route-options.ts` | AE admins can inspect catalog/runs/audit surfaces; broad Treg super-admin mutations and reconciliation families are absent. |

## Interaction sequences

| # | Treg evidence flow | AE current sequence, status/error/recovery evidence |
|---|---|---|
| 1 | `GET /` → optional `POST /demo/sandbox` → auth → `GET /auth/me`/`POST /orgs` | `GET /` → optional `q` redirect to `/market` → Clerk sign-in/up → `/for-agents` or `/agent-access`; no sandbox-org transition (`src/routes/index.tsx`, `src/routes/sign-in.$.tsx`). |
| 2 | catalog search → endpoint get/access → compare → call | `POST /api/v1/market-operations/search` → detail/compare/inspect-plan → operation invoke; read routes are anonymous, invalid/unavailable reads return problem states (`src/routes/api.v1.market-operations.*.ts`, `tools/ae/commands/*.ts`). |
| 3 | org tool/BYOK → org secret → platform key → reserve/relay/settle | AE agent key identifies caller; declared provider connection/operation admission selects execution; refusal or outcome-unknown is returned before/after provider boundary (`src/lib/server/operation-invoke-api.ts`, `src/modules/capability-supply/`, `src/modules/money/`). |
| 4 | insufficient balance → HTTP 402 → balance/topup or connect BYOK → retry | AE `fund`/credit panel starts Stripe session; `outcome_unknown` stores recovery locator, readback is retried, and browser return alone never updates canonical credit (`tools/ae/commands/fund.ts`, `src/components/ae/console/AeCreditTopUpPanel.tsx`). |
| 5 | same `Idempotency-Key` → 409 pending/422 mismatch or replayed success | AE same body `idempotencyKey` → invocation ref/status; uncertain calls use `/reconcile` or `recover`, while cancellation is explicit (`src/modules/capability-execution/operation-invoke-entry.ts`, `tools/ae/commands/recover.ts`). |
| 6 | CLI install/login → catalog search/get → call → runs/audit | AE `manifest` → `search` → `inspect`/`compare`/`inspect-plan` → `connect` → `invoke` → `status`/`recover`; CLI prints problem/refusal and preserves caller-supplied key (`tools/ae/README.md`, `tools/ae/cli.ts`). |
| 7 | MCP `/mcp/` → OAuth metadata/consent/token or team bearer → six stable tools | AE `/mcp` → dynamic `ae_*` tools; anonymous read-only list/read, protected tool challenge, OAuth device/authorization grant, then operation action; retry uses problem correlation and operation recovery (`src/lib/server/mcp-api.ts`, `src/modules/actions/index.ts`, `src/routes/oauth.*`). |
| 8 | scan/upload → skill/tool/secret publication → health → admin reconciliation | AE `/for-providers` → owner supply/offering publication → registry readiness/index health → owner/admin audit; no Treg scan/upload or super-admin reconcile route (`src/routes/for-providers.tsx`, `src/routes/_operator/owner.*.tsx`, `src/routes/_operator/admin.*.tsx`). |

## Status counts

| Status | Rows |
|---|---:|
| MATCHED | 3 |
| PARTIAL | 20 |
| MISSING | 3 |
| DIFFERENT-BY-DESIGN | 2 |
| AE-EXTRA | 1 |
| Total classified rows | 29 |
