---
phase: 01-ten-star-spine-foundation
plan: 10
type: execute
gap_closure: true
wave: 10
depends_on: ["01-09"]
files_modified:
  - convex/auth.config.ts
  - convex/authz.ts
  - convex/source-state.ts
  - convex/business.ts
  - convex/catalog.ts
  - convex/security.ts
  - convex/observability.ts
  - convex/discovery.ts
  - tests/unit/convex/source-state.test.ts
  - tests/unit/convex/authz.test.ts
autonomous: true
requirements: [R2, R3, R8, R10]
user_setup:
  - service: clerk
    why: "Real Clerk session identity and Convex JWT issuer are required before Convex auth/codegen can prove R2/R3/R8."
    env_vars:
      - name: VITE_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard -> API keys; must be a real test or live publishable key"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard -> API keys; must be a real secret key"
      - name: CLERK_JWT_ISSUER_DOMAIN
        source: "Clerk JWT issuer domain used by Convex auth.config.ts"
  - service: convex
    why: "Convex codegen and DB/auth readback must run against the configured deployment."
    env_vars:
      - name: CONVEX_DEPLOYMENT
        source: "Convex project deployment"
      - name: VITE_CONVEX_URL
        source: "Convex project URL"
must_haves:
  truths:
    - "R2: Convex auth/codegen completes with real Clerk issuer configuration and generated functions remain fail-closed when credentials are absent."
    - "R3: Convex claim/publish handlers can derive the owner actor from Clerk auth and persist source-owned state through DB adapters."
    - "R8: Admin helpers derive authority from source-owned membership rows, not from env/session identity alone."
    - "R10: Missing Clerk/Convex env is explicit setup evidence, not silent success."
  artifacts:
    - path: "convex/authz.ts"
      provides: "Clerk actor/admin resolution helpers for Convex handlers"
    - path: "convex/source-state.ts"
      provides: "DB-to-module source-state adapters and idempotent persistence helpers"
    - path: "tests/unit/convex/source-state.test.ts"
      provides: "Adapter coverage for row mapping and writeback semantics"
  key_links:
    - from: "convex/business.ts"
      to: "convex/authz.ts"
      via: "claim/suppress handlers resolve Clerk actor/admin before touching source rows"
      pattern: "resolve.*Actor|resolve.*Admin"
    - from: "convex/source-state.ts"
      to: "src/modules/*/public.ts"
      via: "Convex handlers build module source state, call module seams, then persist row deltas"
      pattern: "load.*SourceState|persist.*SourceState"
---

<objective>
Create the Convex auth/codegen and source-state foundation needed to close R2/R3/R8/R10 verifier failures.

Purpose: remove the fail-closed-only Convex boundary as the reason Phase 1 cannot prove durable authenticated behavior.
Output: Convex helpers for Clerk actor/admin resolution, DB source-state loading/persistence, and codegen-verifiable function files.
</objective>

<execution_context>
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/workflows/execute-plan.md
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ten-star-spine-foundation/01-SPEC.md
@.planning/phases/01-ten-star-spine-foundation/01-CONTEXT.md
@.planning/phases/01-ten-star-spine-foundation/01-VERIFICATION.md
@.planning/phases/01-ten-star-spine-foundation/01-02-contracts-schema-idempotency-admin-foundation-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-03-business-claim-publish-suppress-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-04-admin-dispute-operator-recovery-SUMMARY.md
@convex/auth.config.ts
@convex/schema.ts
@convex/business.ts
@convex/catalog.ts
@convex/security.ts
@convex/observability.ts
@convex/discovery.ts
@src/modules/business/public.ts
@src/modules/catalog/public.ts
@src/modules/security/public.ts
@src/modules/observability/public.ts
</context>

## Artifacts This Phase Produces

- `convex/authz.ts` with Convex-side Clerk actor resolution and source-owned admin membership resolution.
- `convex/source-state.ts` with explicit-table read/write helpers for Phase 1 source rows.
- Codegen evidence from `npm run check:convex-codegen` using real Clerk issuer configuration, or a fail-closed setup error naming missing values.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Convex auth and authority helpers</name>
  <read_first>
    @convex/auth.config.ts
    @convex/security.ts
    @src/modules/security/internal/admin-authority.ts
    @src/modules/security/public.ts
    @.planning/phases/01-ten-star-spine-foundation/01-CONTEXT.md
  </read_first>
  <files>convex/auth.config.ts, convex/authz.ts, tests/unit/convex/authz.test.ts</files>
  <behavior>
    - Test unauthenticated Convex identity resolves to an anonymous or denied actor and cannot perform claim, publish, admin, dispute, or operator actions.
    - Test a Clerk identity maps to `BusinessMutationActor.kind = authenticated_owner` without accepting actor, ownerId, adminId, or Clerk IDs from browser payloads.
    - Test admin authority reads active `adminMemberships` source rows and enforces the role/action matrix; env/session identity alone never grants power after bootstrap.
  </behavior>
  <action>Implement Convex-side helper functions for real Clerk identity resolution and source-owned admin membership lookup, preserving PR00/source-mining boundaries per D-01, plan renumbering and gap continuation per D-02, historical phase-authority limits per D-03, source-mining evidence authority per D-04, and the module seam/type rules per D-20, D-28, D-29, D-30, D-32, D-33, and D-34. Keep `convex/auth.config.ts` fail-closed when `CLERK_JWT_ISSUER_DOMAIN` is empty; do not add fake key literals or local bypasses.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/convex/authz.test.ts</automated>
  </verify>
  <acceptance_criteria>Convex handlers have a shared way to derive owner/admin identity from Clerk/session state and admin source rows, with tests proving anonymous/session-only denial.</acceptance_criteria>
  <done>Browser payloads cannot provide actor/admin authority, and absent Clerk issuer still fails closed with an explicit error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add source-state adapters for Convex DB rows</name>
  <read_first>
    @convex/schema.ts
    @src/modules/business/internal/schema.ts
    @src/modules/catalog/internal/schema.ts
    @src/modules/registry/internal/schema.ts
    @src/modules/discovery/internal/schema.ts
    @src/modules/observability/internal/schema.ts
    @src/modules/security/internal/schema.ts
    @src/modules/business/public.ts
    @src/modules/catalog/public.ts
    @src/modules/registry/public.ts
    @src/modules/discovery/public.ts
  </read_first>
  <files>convex/source-state.ts, tests/unit/convex/source-state.test.ts</files>
  <behavior>
    - Test Phase 1 DB rows map into module source-state shapes without leaking Convex document IDs into route/domain contracts.
    - Test writeback upserts new/changed business, catalog, projection, discovery, audit, operation-key, suppression, dispute, and operator-control rows idempotently.
    - Test source hash/version fields, operation keys, and audit event IDs survive round trips.
  </behavior>
  <action>Create explicit-table Convex source-state loaders and writeback helpers that use existing schema table names and indexes. Helpers must call owning module public seams rather than reimplementing business/catalog/security behavior in route files; use redacted audit snapshots and exact table writes per D-18, D-19, D-21, D-23, D-30, D-32, and D-33.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/convex/source-state.test.ts</automated>
  </verify>
  <acceptance_criteria>Convex function files can load durable Phase 1 source state, call existing module logic, and persist row deltas without route-local state factories.</acceptance_criteria>
  <done>Adapters are covered for mapping, idempotent writeback, redaction preservation, and source hash/version preservation.</done>
</task>

<task type="auto">
  <name>Task 3: Make current Convex files codegen-verifiable without weakening denial paths</name>
  <read_first>
    @package.json
    @convex/business.ts
    @convex/catalog.ts
    @convex/security.ts
    @convex/observability.ts
    @convex/discovery.ts
    @.planning/phases/01-ten-star-spine-foundation/01-VERIFICATION.md
  </read_first>
  <files>convex/business.ts, convex/catalog.ts, convex/security.ts, convex/observability.ts, convex/discovery.ts</files>
  <action>Import and use the new auth/source-state helpers only far enough that each Convex file typechecks and codegen can see real args/returns for the production handlers. Preserve existing denied/unavailable return codes when auth, admin membership, or deployment configuration is missing; do not convert them into success paths without source-row proof.</action>
  <verify>
    <automated>npm run typecheck && npm run check:convex-codegen && npm run test:unit -- tests/unit/convex/authz.test.ts tests/unit/convex/source-state.test.ts</automated>
  </verify>
  <acceptance_criteria>`npm run check:convex-codegen` completes when real Clerk/Convex env is present; when env is absent, execution stops with the missing key names and no fake proof.</acceptance_criteria>
  <done>R2/R10 codegen blocker is either green with real configuration or recorded as an explicit setup blocker in the plan summary.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | - | Phase goal requires durable source-owned claim/publish/admin state | 01-10 | COVERED | Foundation for failed R2/R3/R8/R10 truths. |
| REQ | R2 | Durable source model, type contracts, codegen proof | 01-10 | COVERED | Auth/codegen/source-state adapters. |
| REQ | R3 | Authenticated owner actor must be real Clerk-derived identity | 01-10 | COVERED | Shared actor helper feeds 01-11. |
| REQ | R8 | Admin authority is source-owned | 01-10 | COVERED | Shared admin helper feeds 01-12. |
| REQ | R10 | Missing Clerk/Convex evidence cannot be overclaimed | 01-10 | COVERED | User setup and fail-closed verification. |
| RESEARCH | - | Convex is source of truth and routes are adapters | 01-10 | COVERED | Source-state helpers keep route code out of DB assembly. |
| CONTEXT | D-01,D-02,D-03,D-04 | Source-mining remains completed context and new gap plans continue after executed plan IDs | 01-10 | COVERED | No backup import, no PR00 recreation, and no broad execution against historical phase authority. |
| CONTEXT | D-18-D-20,D-28-D-34 | Operation key, audit, admin, type, auth, and redaction rules | 01-10 | COVERED | Cited in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Clerk session -> Convex auth | Untrusted browser/session identity becomes server-side actor only after Clerk verification. |
| Convex DB -> route/domain DTOs | Durable rows cross into module source-state and public DTO builders. |
| Env/config -> auth config | Missing or fake auth configuration can create false proof if not fail-closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10-01 | Spoofing | `convex/authz.ts` | high | mitigate | Resolve actor from `ctx.auth.getUserIdentity()` only; reject browser-supplied actor/admin fields. |
| T-01-10-02 | Elevation of Privilege | admin membership helper | high | mitigate | Require active source-owned membership and role/action matrix before admin actions. |
| T-01-10-03 | Tampering | source-state writeback | medium | mitigate | Persist through operation-key/audit contracts and source hash/version checks. |
| T-01-10-04 | Repudiation | audit/source adapters | medium | mitigate | Preserve correlation IDs, operation keys, redacted before/after evidence, and audit event IDs. |
| T-01-10-05 | Information Disclosure | DB row mapping | medium | mitigate | Keep Convex document IDs, raw evidence, raw contact, and source hashes out of route-facing DTOs except admin diagnostics. |
| T-01-10-SC | Tampering | npm installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm run typecheck`, `npm run check:convex-codegen`, and the focused Convex unit tests. If real Clerk/Convex values are absent, execution must fail closed and name the missing keys.
</verification>

<success_criteria>
Convex auth/codegen no longer blocks R2 because of missing source-state helpers. Real credentials produce a green codegen run; missing credentials produce explicit setup evidence with no fake success.
</success_criteria>

<output>
Create `.planning/phases/01-ten-star-spine-foundation/01-10-SUMMARY.md` when done.
</output>
