---
phase: 01-ten-star-spine-foundation
plan: 11
type: execute
gap_closure: true
wave: 11
depends_on: ["01-10"]
files_modified:
  - convex/business.ts
  - convex/catalog.ts
  - src/modules/catalog/internal/owner-public-flow.ts
  - src/modules/catalog/public.ts
  - src/routes/claim.tsx
  - src/routes/claim.success.tsx
  - src/routes/owner.status.tsx
  - src/routes/$slug.tsx
  - tests/integration/claim-publish.test.ts
  - tests/integration/durable-claim-route.test.ts
  - tests/e2e/public-owner-ui.spec.ts
autonomous: true
requirements: [R3, R4, R10]
must_haves:
  truths:
    - "R3: A real authenticated owner claim/publish request persists submitted no-ABN catalog rows through Convex source tables."
    - "R4: Claim success, owner status, and public page read the submitted durable catalog, not a regenerated rehearsal record."
    - "R10: Browser route tests still pass without writing fake Clerk keys, and real auth remains the production path."
  artifacts:
    - path: "convex/business.ts"
      provides: "Durable authenticated claim mutation"
    - path: "convex/catalog.ts"
      provides: "Durable publish and public/readback queries"
    - path: "tests/integration/durable-claim-route.test.ts"
      provides: "Regression coverage that submitted owner data is the data displayed afterward"
  key_links:
    - from: "src/routes/claim.tsx"
      to: "convex/business.ts and convex/catalog.ts"
      via: "server function calls Convex claim then publish with Clerk-derived actor"
      pattern: "api\\.business|api\\.catalog|convex.*mutation"
    - from: "src/routes/$slug.tsx"
      to: "convex/catalog.ts"
      via: "loader reads public catalog by slug from durable source rows"
      pattern: "getPublic.*Catalog|read.*BySlug"
---

<objective>
Replace the hollow owner claim route data path with durable Clerk-authenticated Convex claim/publish/readback behavior.

Purpose: close the R3/R4/R10 failure where `/claim`, `/claim/success`, `/owner/status`, and `/{slug}` used a fixed actor literal and fresh in-memory source state.
Output: authenticated Convex claim/publish handlers, durable route loaders, and route tests proving submitted catalog data is reused after publish.
</objective>

<execution_context>
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/workflows/execute-plan.md
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ten-star-spine-foundation/01-SPEC.md
@.planning/phases/01-ten-star-spine-foundation/01-CONTEXT.md
@.planning/phases/01-ten-star-spine-foundation/01-UI-SPEC.md
@.planning/phases/01-ten-star-spine-foundation/01-VERIFICATION.md
@.planning/phases/01-ten-star-spine-foundation/01-05-public-owner-ui-routes-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-10-SUMMARY.md
@src/modules/catalog/internal/owner-public-flow.ts
@src/routes/claim.tsx
@src/routes/claim.success.tsx
@src/routes/owner.status.tsx
@src/routes/$slug.tsx
@convex/business.ts
@convex/catalog.ts
</context>

## Artifacts This Phase Produces

- Convex `claimBusiness` and `publishBusinessCatalog` handlers that use Clerk-derived owner identity and persist source rows.
- Route server functions/loaders that display the submitted durable catalog after claim/publish.
- Integration and browser coverage proving custom owner input appears in success, owner status, and public page output.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing durable-claim route coverage</name>
  <read_first>
    @tests/integration/claim-publish.test.ts
    @tests/e2e/public-owner-ui.spec.ts
    @src/modules/catalog/internal/owner-public-flow.ts
    @src/routes/claim.tsx
    @src/routes/claim.success.tsx
    @src/routes/$slug.tsx
  </read_first>
  <files>tests/integration/durable-claim-route.test.ts, tests/e2e/public-owner-ui.spec.ts</files>
  <behavior>
    - Submit a non-Sam no-ABN business through the same server route seam and assert the resulting success/status/public page uses the submitted slug, name, category, suburb, service, and unavailable capability state.
    - Assert the route data path does not rebuild the default Sam record after submission.
    - Assert wrong-owner, anonymous, missing CSRF, duplicate, and empty-service failures remain typed and audited.
  </behavior>
  <action>Add tests first for the verifier gap, preserving owner outcome/copy decisions per D-05, D-06, D-07, D-08, and D-09. The tests should fail against the current fresh in-memory flow and fixed actor literal, then pass only when route readbacks use durable submitted rows.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/durable-claim-route.test.ts && VITE_AE_DISABLE_CLERK_FOR_LOCAL_E2E=true npm run test:e2e -- tests/e2e/public-owner-ui.spec.ts</automated>
  </verify>
  <acceptance_criteria>The tests fail before implementation for the exact R3/R4 hollow-data reason and pass after durable route wiring.</acceptance_criteria>
  <done>Submitted owner data, not default rehearsal data, drives success/status/public page assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable claim and publish Convex handlers</name>
  <read_first>
    @convex/authz.ts
    @convex/source-state.ts
    @convex/business.ts
    @convex/catalog.ts
    @src/modules/business/public.ts
    @src/modules/catalog/public.ts
    @src/modules/observability/public.ts
  </read_first>
  <files>convex/business.ts, convex/catalog.ts, src/modules/catalog/public.ts</files>
  <behavior>
    - Authenticated Clerk owner can claim without ABN and publish at least one service into Convex source tables.
    - Publish replays by operation key and creates one audit, registry projection attempt, and discovery attempt per logical target.
    - Handler returns public/readback DTOs only from source-owned rows and does not expose private owner/contact/admin/evidence fields.
  </behavior>
  <action>Replace the claim/publish fail-closed-only handlers with source-state-backed Convex handlers. Use the auth/source adapters from 01-10, call module public seams for claim and publish, persist row deltas, keep operation-key and audit behavior per D-18 and D-19, keep `catalog` as the DTO owner per D-10 and `business` as transition owner per D-11, and preserve identity/redaction requirements per D-32, D-33, and D-34.</action>
  <verify>
    <automated>npm run typecheck && npm run check:convex-codegen && npm run test:integration -- tests/integration/claim-publish.test.ts tests/integration/durable-claim-route.test.ts</automated>
  </verify>
  <acceptance_criteria>Convex claim/publish writes durable source tables with Clerk-derived actor identity and no browser-supplied authority.</acceptance_criteria>
  <done>R3 claim/publish can complete through Convex when Clerk/Convex env is present, and missing auth remains a typed failure.</done>
</task>

<task type="auto">
  <name>Task 3: Wire owner route readbacks to durable catalog queries</name>
  <read_first>
    @src/modules/catalog/internal/owner-public-flow.ts
    @src/routes/claim.tsx
    @src/routes/claim.success.tsx
    @src/routes/owner.status.tsx
    @src/routes/$slug.tsx
    @convex/catalog.ts
  </read_first>
  <files>src/modules/catalog/internal/owner-public-flow.ts, src/routes/claim.tsx, src/routes/claim.success.tsx, src/routes/owner.status.tsx, src/routes/$slug.tsx</files>
  <action>Keep validation and status readback builders, but move mutation and readback authority to Convex-backed server functions/loaders. `/claim` should submit validated form facts only; `/claim/success`, `/owner/status`, and `/{slug}` should query durable public/readback DTOs by slug or authenticated owner session. Remove route-runtime dependence on regenerated default source state while retaining test fixtures where they are explicitly fixture-scoped.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/durable-claim-route.test.ts && VITE_AE_DISABLE_CLERK_FOR_LOCAL_E2E=true npm run test:e2e && npm run test:copy && npm run test:seo</automated>
  </verify>
  <acceptance_criteria>Success/status/public page route loaders consume durable submitted catalog rows and keep UI/copy guarantees from D-05 through D-09.</acceptance_criteria>
  <done>The verifier can no longer identify `/claim`, `/claim/success`, `/owner/status`, or `/{slug}` as hollow default-readback surfaces.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | - | Owner can claim/publish and see truthful status | 01-11 | COVERED | Durable owner route slice. |
| REQ | R3 | Authenticated no-ABN claim/publish | 01-11 | COVERED | Convex mutation path plus route tests. |
| REQ | R4 | Public page/status readback uses source state | 01-11 | COVERED | Success/status/page loaders query durable DTOs. |
| REQ | R10 | Local browser gates remain honest | 01-11 | COVERED | Local bypass remains command-scoped; real auth remains production path. |
| RESEARCH | - | Routes are adapters over public module seams | 01-11 | COVERED | Route loaders do not assemble source rows. |
| CONTEXT | D-05-D-11,D-18,D-19,D-23,D-32-D-34 | Owner status, DTO ownership, idempotency, audit, suppression posture, identity/redaction | 01-11 | COVERED | Cited in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser form -> server function | Claim facts are untrusted input and must not contain authority. |
| Server function -> Convex mutation | Authenticated owner identity is resolved server-side and persists source rows. |
| Convex source rows -> public page | Public route loaders expose only allowlisted DTO/readback data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-11-01 | Spoofing | `/claim` server function | high | mitigate | Resolve actor from Clerk/Convex auth, reject browser-supplied authority fields. |
| T-01-11-02 | Tampering | claim/publish source writes | high | mitigate | Use CSRF, same-site Origin, rate limits, operation keys, and duplicate detection before writes. |
| T-01-11-03 | Repudiation | publish audit | medium | mitigate | Persist claim/publish audit events with correlation IDs and operation keys. |
| T-01-11-04 | Information Disclosure | public route readbacks | medium | mitigate | Return `PublicCatalogContract`/public readback DTOs only; no raw contact/private evidence. |
| T-01-11-SC | Tampering | npm installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run focused durable-claim integration tests, Convex codegen, browser route tests with command-scoped local Clerk bypass, copy/SEO gates, typecheck, and build.
</verification>

<success_criteria>
R3 and the R4 data-source part of the verifier report are closed: a submitted authenticated owner catalog is the durable source for claim success, owner status, and public page output.
</success_criteria>

<output>
Create `.planning/phases/01-ten-star-spine-foundation/01-11-SUMMARY.md` when done.
</output>
