Rule: react-doctor/js-cache-property-access
Severity: warning
Category: Performance
Count: 2

This slows the loop because testCase.source.kind is read 4 times inside it, so read it once into a variable at the top

This slows the loop because testCase.source.caseId is read 4 times inside it, so read it once into a variable at the top

Suggestion: Read the value once into a variable at the top of the loop: `const { x, y } = obj.deeply.nested`

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-cache-property-access

Files:
  eval/answer/lib/coverage.ts:322
  eval/answer/lib/coverage.ts:323
