Rule: deslop/unused-export
Severity: warning
Category: Maintainability
Count: 155

Unused export: `api` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `components` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `query` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `internalQuery` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `mutation` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `action` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `internalAction` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `httpAction` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `parseAnswerSseBuffer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `streamAnswerSse` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `threadUrl` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `useOperatorDensity` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `Route` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ThinkingStepValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `createAnswerHarnessOperationState` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildAnswerHarnessOperationPhases` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `persistAnswerTurnWithHarnessLoop` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `appendAnswerToolCallsMutation` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readTurnToolCallsQuery` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `appendAnswerToolCalls` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `routeCallsTools` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `routeIsBoundary` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `parseFrozenProse` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AE_SESSION_COOKIE` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AE_SESSION_MAX_AGE_SECONDS` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readSessionIdFromRequest` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildSessionSetCookieHeader` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ANSWER_TURN_MAX_PER_THREAD` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `encodeAnswerId` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AnswerSourceSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AnswerCompareFieldSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AnswerArtifactSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AeAnswerArtifactsSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AnswerWorkStepPhaseValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AnswerWorkStepStatusValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AnswerLayoutProfileValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `z` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `validateCatalogGrounding` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `mergeProseIntoSnapshot` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `shouldUseCompactFollowUpLayout` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `DEFAULT_OPENROUTER_MODEL` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readAnswerEvalPassed` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readAnswerModelWhitelist` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `artifactsFromStructured` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AeSearchModeValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AeSearchLocationSourceValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AeSearchLocationSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `normalizeAeSearchContext` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildAeSearchContextFromLabel` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `aeSearchContextWithMode` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readPublicPaidActivationServer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `openCurrentOwnerBillingPortalServer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readCurrentOwnerBillingThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readPublicPaidActivationThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `startCurrentOwnerPaidActivationThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `openCurrentOwnerBillingPortalThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `recordCurrentOwnerBillingReturnThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readCurrentOwnerBillingReceiptThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readAdminBillingThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `publishAdminBillingOfferThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `reconcileAdminBillingOperationThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `recordAdminBillingEvidenceThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `markAdminBillingNoRepairThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `disableAdminPaidActivationThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `assertOwnerBusiness` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `reconcileBillingOperation` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `isPostCheckpointExternalProvider` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `BusinessActionCardPostureValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `isBusinessActionGuardrailProvider` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `verifyStripeWebhookSignature` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `VisibilityTargetTypeSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `PublicOwnerClaimFieldValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `PublicFirstRequestChannelSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `CapabilityKindSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `BusinessServiceStatusSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `submitOwnerClaimThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readPublicBusinessPageThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ResultKindValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `DiscoveryPathKindSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `DiscoveryAttemptStatusSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `HarnessEmissionSeverityValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `HarnessEmissionSurfaceValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `compareHarnessEmissionSeverity` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `appendHarnessSessionEntryToSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `appendHarnessSessionEntryToSourceFromServerContext` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `harnessSessionAppendOperationKey` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `listHarnessSessionEntriesFromSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `listHarnessRunEntriesFromSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readAdminHarnessSessionEntriesFromSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `HarnessToolTierValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `HarnessToolLoadModeValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `HarnessToolConcurrencyValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `HarnessApprovalPolicyValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildHarnessRunViewerDeniedResult` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildHarnessRunViewerListProjection` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildHarnessRunViewerListRow` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildHarnessRunViewerDetail` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AE_PROTECTED_TOOL_IDS` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `isAeProtectedToolId` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildHarnessReplayProjection` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `isHarnessTerminalSessionEntry` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `runHarnessRunLoop` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildHarnessToolSchemaBundle` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ownerThreadSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ownerReplySchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ownerVersionedSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `closeCurrentOwnerInquiryThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `LifecycleClassValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readAdminOwnerActivationSummaryThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `recordOwnerActivationEventServer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readAdminOwnerActivationSummaryServer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readAdminAnalyticsServer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `stableHash` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `OperationKeyStatusSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ActorKindSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ActivationStageSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `OwnerActivationStateSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `retryCurrentOwnerContactFollowUpServer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `markCurrentOwnerContactFollowUpNoRepairServer` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readCurrentOwnerContactFollowUpReceiptThroughSource` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `localPendingProposalId` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `localStaleProposalId` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `localRefusedProposalId` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `defaultContactFollowUpOperatorControls` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readContactFollowUpProposal` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readContactFollowUpReceipt` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `createContactFollowUpGatewayAdmission` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ContactFollowUpRetentionClass` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `CatalogSearchBackendValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `readCatalogSearchTimeoutMs` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `RegistrySearchDocumentSchemaVersion` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildRegistrySearchDocumentsFromCatalogs` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `buildRegistrySearchDocumentId` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `normalizeSearchToken` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `RegistryProjectionStatusSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `RegistryProjectionKindSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `IndexTargetTypeSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `RegistryRepairActionSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `RegistryRepairResultSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminActionMatrix` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminReadbackSurfaceValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminReadbackRowTypeValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminReadbackRowStateValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminReadbackRepairActionValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminReadbackRepairResultValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminMembershipStateSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminActionSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AdminMembershipAuditEventTypeSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `SuppressionRuleStatusSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `DisputeStatusSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `AbuseBucketStateSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `ClaimFingerprintStatusSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `RemovalDisputeReasonCodeValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `DisputeEvidenceMediaTypeValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Unused export: `SeoIndexDirectiveValues` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.

Suggestion: Drop the `export` keyword (or remove the declaration) if no other module uses this symbol.

Files:
  convex/_generated/api.js:21
  convex/_generated/api.js:23
  convex/_generated/server.js:29
  convex/_generated/server.js:39
  convex/_generated/server.js:49
  convex/_generated/server.js:72
  convex/_generated/server.js:80
  convex/_generated/server.js:93
  src/components/ae/chat/answer-stream.ts:6
  src/components/ae/chat/answer-stream.ts:32
  src/components/ae/chat/copy-thread-link.ts:3
  src/components/ae/layout/AeOperatorShell.tsx:27
  src/future-phases/05-paid-activation-money-rails/routes/api.billing.webhook.ts:6
  src/modules/answer-thread/answer-thread.schema.ts:34
  src/modules/answer-thread/internal/answer-harness-operation.ts:162
  src/modules/answer-thread/internal/answer-harness-operation.ts:185
  src/modules/answer-thread/internal/answer-turn-finalization.ts:108
  src/modules/answer-thread/internal/commands.ts:44
  src/modules/answer-thread/internal/commands.ts:48
  src/modules/answer-thread/internal/commands.ts:67
  src/modules/answer-thread/internal/intent-router.ts:43
  src/modules/answer-thread/internal/intent-router.ts:48
  src/modules/answer-thread/internal/public-projection.ts:77
  src/modules/answer-thread/internal/session-cookie.ts:1
  src/modules/answer-thread/internal/session-cookie.ts:2
  src/modules/answer-thread/internal/session-cookie.ts:4
  src/modules/answer-thread/internal/session-cookie.ts:33
  src/modules/answer-thread/internal/turn-guard.ts:6
  src/modules/answer/answer-id.ts:14
  src/modules/answer/answer-schema.ts:4
  src/modules/answer/answer-schema.ts:30
  src/modules/answer/answer-schema.ts:32
  src/modules/answer/answer-schema.ts:60
  src/modules/answer/answer-synthesizer.ts:48
  src/modules/answer/answer-synthesizer.ts:59
  src/modules/answer/internal/answer-layout-profile.ts:4
  src/modules/answer/internal/answer-tool-use-agent.ts:876
  src/modules/answer/internal/catalog-grounding.ts:14
  src/modules/answer/internal/emit-snapshot-events.ts:242
  src/modules/answer/internal/follow-up-compact-prose.ts:5
  src/modules/answer/internal/llm-config.ts:1
  src/modules/answer/internal/llm-config.ts:19
  src/modules/answer/internal/openrouter-models.ts:67
  src/modules/answer/internal/structured-artifacts.ts:4
  src/modules/answer/search-context.ts:3
  src/modules/answer/search-context.ts:6
  src/modules/answer/search-context.ts:14
  src/modules/answer/search-context.ts:42
  src/modules/answer/search-context.ts:65
  src/modules/answer/search-context.ts:89
  src/modules/billing/billing.functions.ts:276
  src/modules/billing/billing.functions.ts:284
  src/modules/billing/billing.functions.ts:320
  src/modules/billing/billing.functions.ts:341
  src/modules/billing/billing.functions.ts:352
  src/modules/billing/billing.functions.ts:406
  src/modules/billing/billing.functions.ts:460
  src/modules/billing/billing.functions.ts:492
  src/modules/billing/billing.functions.ts:520
  src/modules/billing/billing.functions.ts:550
  src/modules/billing/billing.functions.ts:575
  src/modules/billing/billing.functions.ts:619
  src/modules/billing/billing.functions.ts:670
  src/modules/billing/billing.functions.ts:717
  src/modules/billing/internal/authority.ts:52
  src/modules/billing/internal/operations.ts:696
  src/modules/business-action/internal/business-action.ts:1104
  src/modules/business-action/internal/schema.ts:29
  src/modules/business-action/internal/schema.ts:351
  src/modules/business-action/internal/stripe-checkout.ts:209
  src/modules/business/internal/validators.ts:8
  src/modules/catalog/internal/owner-public-flow.ts:15
  src/modules/catalog/internal/validators.ts:12
  src/modules/catalog/internal/validators.ts:14
  src/modules/catalog/internal/validators.ts:15
  src/modules/catalog/owner-claim.functions.ts:159
  src/modules/catalog/owner-claim.functions.ts:275
  src/modules/common/result.ts:1
  src/modules/discovery/internal/validators.ts:6
  src/modules/discovery/internal/validators.ts:7
  src/modules/harness/emission-guard.ts:3
  src/modules/harness/emission-guard.ts:12
  src/modules/harness/emission-guard.ts:185
  src/modules/harness/harness.functions.ts:180
  src/modules/harness/harness.functions.ts:186
  src/modules/harness/harness.functions.ts:230
  src/modules/harness/harness.functions.ts:237
  src/modules/harness/harness.functions.ts:243
  src/modules/harness/harness.functions.ts:249
  src/modules/harness/harness.schema.ts:35
  src/modules/harness/harness.schema.ts:38
  src/modules/harness/harness.schema.ts:41
  src/modules/harness/harness.schema.ts:44
  src/modules/harness/internal/run-viewer-projection.ts:110
  src/modules/harness/internal/run-viewer-projection.ts:124
  src/modules/harness/internal/run-viewer-projection.ts:180
  src/modules/harness/internal/run-viewer-projection.ts:215
  src/modules/harness/protected-evidence.ts:26
  src/modules/harness/protected-evidence.ts:66
  src/modules/harness/replay-projection.ts:69
  src/modules/harness/replay-projection.ts:123
  src/modules/harness/run-loop.ts:722
  src/modules/harness/tool-contract.ts:166
  src/modules/inquiries/inquiry.functions.ts:57
  src/modules/inquiries/inquiry.functions.ts:70
  src/modules/inquiries/inquiry.functions.ts:75
  src/modules/inquiries/inquiry.functions.ts:466
  src/modules/lifecycle/public.ts:4
  src/modules/observability/funnel.functions.ts:11
  src/modules/observability/funnel.functions.ts:17
  src/modules/observability/funnel.functions.ts:32
  src/modules/observability/funnel.functions.ts:36
  src/modules/observability/internal/redaction.ts:32
  src/modules/observability/internal/validators.ts:15
  src/modules/observability/internal/validators.ts:16
  src/modules/observability/internal/validators.ts:21
  src/modules/observability/internal/validators.ts:23
  src/modules/protected-action/contact-follow-up.functions.ts:169
  src/modules/protected-action/contact-follow-up.functions.ts:173
  src/modules/protected-action/contact-follow-up.functions.ts:222
  src/modules/protected-action/contact-follow-up.functions.ts:875
  src/modules/protected-action/contact-follow-up.functions.ts:877
  src/modules/protected-action/contact-follow-up.functions.ts:878
  src/modules/protected-action/internal/contact-follow-up.ts:93
  src/modules/protected-action/internal/contact-follow-up.ts:1027
  src/modules/protected-action/internal/contact-follow-up.ts:1039
  src/modules/protected-action/internal/gateway.ts:22
  src/modules/protected-action/internal/retention.ts:11
  src/modules/registry/internal/catalog-search-port.ts:10
  src/modules/registry/internal/catalog-search-port.ts:113
  src/modules/registry/internal/search-documents.ts:8
  src/modules/registry/internal/search-documents.ts:104
  src/modules/registry/internal/search-documents.ts:164
  src/modules/registry/internal/search-documents.ts:221
  src/modules/registry/internal/validators.ts:13
  src/modules/registry/internal/validators.ts:14
  src/modules/registry/internal/validators.ts:15
  src/modules/registry/internal/validators.ts:16
  src/modules/registry/internal/validators.ts:17
  src/modules/security/internal/admin-authority.ts:13
  src/modules/security/internal/admin-readbacks.ts:4
  src/modules/security/internal/admin-readbacks.ts:7
  src/modules/security/internal/admin-readbacks.ts:10
  src/modules/security/internal/admin-readbacks.ts:22
  src/modules/security/internal/admin-readbacks.ts:31
  src/modules/security/internal/validators.ts:15
  src/modules/security/internal/validators.ts:16
  src/modules/security/internal/validators.ts:17
  src/modules/security/internal/validators.ts:18
  src/modules/security/internal/validators.ts:19
  src/modules/security/internal/validators.ts:20
  src/modules/security/internal/validators.ts:21
  src/modules/security/public.ts:80
  src/modules/security/public.ts:88
  src/modules/seo/public.ts:7
