Rule: react-doctor/async-defer-await
Severity: warning
Category: Performance
Count: 1

This await blocks the function before an early-return that doesn't use the awaited value, so the skip path waits for nothing. Move the await below the guard so it only runs when you need the data

Suggestion: Move the `await` below the early-return guard so the skip path stays fast and avoids unnecessary async work.

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/async-defer-await

Files:
  src/modules/answer-thread/answer-thread.functions.ts:171
