---
phase: 05-consumer-operating-proof
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - src/modules/comparison/public.ts
  - src/modules/comparison/internal/contract.ts
  - src/modules/comparison/internal/url-state.ts
  - src/modules/comparison/internal/profiles/professional-service-v1.ts
  - src/modules/comparison/internal/profiles/machine-data-v1.ts
  - src/modules/comparison/internal/resolve.ts
  - src/modules/comparison/internal/compare.ts
  - src/modules/comparison/internal/projection.ts
  - tests/unit/comparison/contract.test.ts
  - tests/unit/comparison/profiles.test.ts
  - tests/unit/comparison/resolve.test.ts
  - tests/unit/comparison/compare.test.ts
autonomous: true
requirements: [P5-COMPARE]
must_haves:
  truths:
    - "A public URL contains at most four exact Offering tuples and three closed priority IDs, never facts, auth or free text."
    - "Every load re-resolves public eligibility/currentness/provenance; one refused item does not erase valid selections."
    - "No priority, ties, stale/unknown/not-supplied/not-comparable decisive facts return unranked; only comparable stated priorities produce an inspectable order."
  artifacts:
    - path: "src/modules/comparison/internal/contract.ts"
      provides: "offering-comparison:v1 semantic contract"
    - path: "src/modules/comparison/internal/resolve.ts"
      provides: "exact reference resolution ordinary outcomes"
    - path: "src/modules/comparison/internal/compare.ts"
      provides: "pure lexicographic comparison and tradeoff explanation"
  key_links:
    - from: "url-state.ts"
      to: "resolve.ts"
      via: "strict bounded public references"
    - from: "resolve.ts"
      to: "catalog/registry public seams"
      via: "exact revision and live suppression read ports"
    - from: "compare.ts"
      to: "projection.ts"
      via: "source-owned rows, order and reasons"
---

<objective>
Create the pure Offering comparison owner: bounded selection, exact resolution,
common semantic rows, explicit uncertainty and defensible stated-priority order
for D-04–D-09 and D-12.

Purpose: make routes and actions transport/render one result rather than own meaning.
Output: tested `offering-comparison:v1` application contract.
</objective>

<execution_context>@/Users/joelchan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-consumer-operating-proof/05-02-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-03-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-04-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-CONTEXT.md
@.planning/phases/05-consumer-operating-proof/05-RESEARCH.md
@.planning/phases/05-consumer-operating-proof/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict comparison and bounded URL contracts</name>
  <files>src/modules/comparison/internal/contract.ts, src/modules/comparison/internal/url-state.ts, src/modules/comparison/internal/profiles/professional-service-v1.ts, src/modules/comparison/internal/profiles/machine-data-v1.ts, src/modules/comparison/public.ts, tests/unit/comparison/contract.test.ts, tests/unit/comparison/profiles.test.ts</files>
  <behavior>
    - "RED: malformed, duplicate or fifth selection and fourth priority become bounded ordinary refused state without throwing."
    - "RED: URL state accepts only businessId/offeringRef/positive revision/projectionObservedAt and closed priority IDs."
    - "RED: profile-only facts cannot compare across profile/version/unit merely because labels match."
  </behavior>
  <action>Define `offering-comparison:v1` with selection identities, business/Offering context, fact cells, provenance/currentness, newer-current reference, priorities, tradeoffs and discriminated outcomes. Encode at most four selections and three priorities in canonical normalized URL state; reject duplicates and never include names, facts, URLs, free text, session/auth data or source hashes. Profile projectors map the two catalog-owned closed versions into registered dimension IDs; they cannot create keys or order. Export only the intentional public seam. This implements D-04–D-08/D-12.</action>
  <verify><automated>npm exec -- vitest run tests/unit/comparison/contract.test.ts tests/unit/comparison/profiles.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately; changed-path errors block, and only named unchanged 05-01 inherited failures may remain diagnostic until 05-08.</diagnostic>
  <done>Strict contracts accept valid two-category data, refuse hostile/over-cap URL and profile shapes, and expose no arbitrary property bag or client ranking seam.</done>
  <stop>Stop if the integrated catalog profile differs from accepted 05-02, URL state needs free text/private material, or cross-profile meaning requires label inference.</stop>
  <predecessor>Parent-integrated exact revision/tree containing 05-02 through 05-04 results.</predecessor>
  <evidence>Pure source/unit contract evidence only.</evidence>
  <handoff>Commit owned paths; return base/result revision/tree, changed paths, RED/GREEN commands, malformed-state observations, unresolved contract mismatch, earliest blocker and next safe action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve exact selections with live safety and history checks</name>
  <files>src/modules/comparison/internal/resolve.ts, src/modules/comparison/internal/projection.ts, tests/unit/comparison/resolve.test.ts</files>
  <behavior>
    - "RED: correct offeringRef with wrong business, never-public history, suppression or source mismatch returns unavailable without facts."
    - "RED: eligible old revision stays selected and separately reports current revision; projectionObservedAt never selects content."
    - "RED: one refused selection preserves other valid resolved selections in URL order."
  </behavior>
  <action>Implement a server-usable resolver over only catalog/registry public read ports. Resolve content by exact business/offering/revision; treat projectionObservedAt as displayed observation context. Recheck live business/Offering suppression before historical read, preserve selected exact history, return separate newer-current reference and explicit current/partial/stale/unknown states. Never copy Offering truth, import internal owners, substitute current or fetch an access-path URL. Use ordinary outcomes for every expected refusal.</action>
  <verify><automated>npm exec -- vitest run tests/unit/comparison/resolve.test.ts tests/unit/catalog/offering-public-history.test.ts tests/unit/registry/offering-runtime-guards.test.ts && npm run test:imports</automated></verify>
  <done>Resolver proves lineage/suppression/privacy refusal, exact history, changed-revision disclosure and partial-selection preservation through supported module seams.</done>
  <stop>Stop if required truth is absent from a supported 05-02/05-03 public seam, resolution would import `internal/*`, or suppression cannot precede fact access.</stop>
  <predecessor>Task 1 result.</predecessor>
  <evidence>Source and hostile fixture resolution evidence; not public/browser/hosted proof.</evidence>
  <handoff>Return result revision/tree, changed paths, commands/results, exact/refusal examples, unresolved read-port need, earliest blocker and next parent action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compare facts and explain tradeoffs without a score</name>
  <files>src/modules/comparison/internal/compare.ts, src/modules/comparison/internal/projection.ts, tests/unit/comparison/compare.test.ts</files>
  <behavior>
    - "RED: no priority and an exact tie return explicit unranked."
    - "RED: unknown, not supplied, stale or not comparable on a decisive priority blocks ordering for the whole set and never improves position."
    - "RED: a unique order follows stated priority sequence lexicographically and returns decisive facts/reasons without a score."
  </behavior>
  <action>Build common rows first, then profile rows, explicit cell states and plain semantic reasons. Apply priorities exactly in user order as a lexicographic rule; never weights, summed score, hidden tie-breaker, model inference, trust/reputation, provider/query branch or business-level comparison. Return `unranked` for absent priorities, ties or any decisive non-current/non-comparable cell; return ordered only for a unique evidence-backed sequence. Ensure mutations to a decisive fixture change order/refusal, making the test source-facing rather than snapshots.</action>
  <verify><automated>npm exec -- vitest run tests/unit/comparison/compare.test.ts tests/unit/comparison/resolve.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately under the same disposition; 05-08 requires full green.</diagnostic>
  <done>The same pure function yields honest tradeoffs, default/tie/blocked unranked and inspectable lexicographic order across both closed profiles without a score.</done>
  <stop>Stop if ordering requires missing defaults, weights, an unstated tie-breaker, UI/provider branching or Customer Request imports.</stop>
  <predecessor>Task 2 result.</predecessor>
  <evidence>Pure deterministic unit evidence; no claim that ordering is useful to real customers.</evidence>
  <handoff>Return base/result revision/tree, changed paths, commands/results, observable unranked/ordered cases, unresolved finding, earliest blocker and next safe parent integration action.</handoff>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser URL → server resolver | Public untrusted tuples and priorities must be bounded and revalidated. |
| resolved facts → ordering | Missing/stale/foreign semantics must not gain advantage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-05A | Spoofing | selection lineage | high | mitigate | exact business/offering/revision/history checks |
| T-05-05B | Information disclosure | suppressed history | high | mitigate | live suppression before historical projection |
| T-05-05C | Tampering | priorities/facts | high | mitigate | strict enums and fail-closed decisive cells |
| T-05-05D | Denial of service | URL fan-out | medium | mitigate | four selections, three priorities, bounded identifier lengths |
| T-05-05E | Repudiation | recommendation reason | medium | mitigate | return priority sequence and decisive facts; no hidden score |
</threat_model>

<verification>Parent integrates this result and reruns all four comparison unit files plus 05-03 registry and 05-04 consumer parity before Wave 6 / 05-06.</verification>
<success_criteria>One pure semantic owner resolves and compares exact Offerings honestly, while all expected uncertainty remains ordinary and explicit.</success_criteria>
<output>Create `.planning/phases/05-consumer-operating-proof/05-05-SUMMARY.md` when done.</output>
