---
phase: 05-consumer-operating-proof
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - src/routes/registry.tsx
  - src/routes/$slug.tsx
  - src/routes/$slug.offerings.$offeringRef.tsx
  - src/routes/compare.tsx
  - src/routeTree.gen.ts
  - src/components/ae/offerings/offering-presentation.ts
  - src/components/ae/offerings/AeOfferingSupplyList.tsx
  - src/components/ae/comparison/AeShortlistBar.tsx
  - src/components/ae/comparison/AeOfferingDetail.tsx
  - src/components/ae/comparison/AeOfferingComparison.tsx
  - src/modules/comparison/internal/presentation.ts
  - src/modules/comparison/internal/presentation-adapter.ts
  - tests/unit/comparison/presentation.test.ts
  - tests/unit/ui/offering-comparison.test.tsx
  - tests/e2e/comparison-surface.spec.ts
  - tests/e2e/a11y/comparison.spec.ts
autonomous: true
requirements: [P5-HUMAN]
must_haves:
  truths:
    - "Per D-03, a no-login visitor browses businesses, opens a concrete Offering and selects two to four exact revisions; authentication never blocks the loop."
    - "Refresh/share preserves exact selections; malformed/refused/changed items recover honestly without client-owned truth."
    - "The first viewport gives the source-owned answer and decisive evidence; the complete desktop table/mobile fact list remains one disclosure away and exposes the same states/order with keyboard, focus, zoom and reduced-motion behavior."
    - "GenUI is the primary customer presentation and adapts registered composition to question, category and device using density/emphasis IDs only; invalid, absent, slow, unsafe, disabled or switched-model output falls back to the complete deterministic answer-first surface."
    - "No Phase-5 control reaches call, inquiry, endpoint invocation, Customer Request, booking or payment."
  artifacts:
    - path: "src/routes/compare.tsx"
      provides: "server-authoritative validated public comparison route"
    - path: "src/components/ae/comparison/AeOfferingComparison.tsx"
      provides: "Astryx answer-first surface plus disclosed native table and equivalent mobile fact projection"
    - path: "src/modules/comparison/internal/presentation.ts"
      provides: "strict digest-bound registered presentation resolver and deterministic fallback"
  key_links:
    - from: "public route URL state"
      to: "comparison public application seam"
      via: "server loader on every load"
    - from: "comparison semantic cells"
      to: "desktop table/mobile dl"
      via: "render-only identical source order"
---

<objective>
Deliver the public Astryx Browse → Offering detail → shortlist → answer-first
compare loop specified by D-01–D-07 and D-11, including complete accessible
state behavior and bounded registered composition.

Purpose: make the inspect-only choosing outcome observable to a visitor.
Output: public routes, domain compositions and local browser evidence.
</objective>

<execution_context>@/Users/joelchan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-consumer-operating-proof/05-05-SUMMARY.md
@DESIGN.md
@.planning/phases/05-consumer-operating-proof/05-UI-SPEC.md
@.planning/phases/05-consumer-operating-proof/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build render-only Offering detail, shortlist and bounded presentation contracts</name>
  <files>src/components/ae/offerings/offering-presentation.ts, src/components/ae/offerings/AeOfferingSupplyList.tsx, src/components/ae/comparison/AeOfferingDetail.tsx, src/components/ae/comparison/AeShortlistBar.tsx, src/modules/comparison/internal/presentation.ts, src/modules/comparison/internal/presentation-adapter.ts, tests/unit/comparison/presentation.test.ts, tests/unit/ui/offering-comparison.test.tsx</files>
  <behavior>
    - "RED: add/remove uses aria-pressed, exact names, four-item cap, focus recovery and one bounded live region."
    - "RED: every profile field renders value/not known/not supplied/out of date; no omission/empty/default substitute."
    - "RED: effect verbs/controls and protocol literals are absent from the Phase-5 action region."
    - "RED: presentation accepts only a closed mode, density, responsive composition and bounded existing semantic IDs bound to the comparison digest; extra text/components/actions/URLs/ARIA and unknown or duplicate IDs fall back."
    - "RED: no accepted presentation proposal can open, close, remove or relabel a disclosure."
    - "RED: disabled, timeout, unavailable, unsafe and switched-model adapters produce the same complete deterministic answer-first brief without delaying it."
  </behavior>
  <action>Use Astryx neutral components and existing semantic tokens only. Refactor Offering summary/detail as render-only compositions over `offering-comparison:v1`; do not parse facts, infer freshness/support or sort. Add one comparison-owned presentation resolver accepting `unknown` and a model-neutral GenUI adapter port. GenUI is the primary customer presentation. A proposal may select only `answer_first|guided_compare`, one registered density, one registered responsive composition for the current question/category/device class, and at most three existing semantic IDs; it supplies no facts, prose, component names, URLs, actions, ARIA, disclosure state or code. Bind accepted plans to the semantic digest, preserve canonical order, keep provider prose outside operative instructions and return typed fallback for disabled/timeout/invalid/unsafe/unavailable output. The deterministic brief renders immediately and never waits on composition. Implement the UI-SPEC exact copy, 44px controls, persistent labels, add/remove/four-cap behavior, revision/observed/provenance/currentness and descriptive `View published details` link with no-referrer protections. Isolate the labelled Phase-5 task region from all business-level call/inquiry callbacks/imports. Do not introduce a generic UI protocol, new primitive/design system, A2UI/AG-UI runtime or iframe.</action>
  <verify><automated>npm exec -- vitest run tests/unit/comparison/presentation.test.ts tests/unit/ui/offering-comparison.test.tsx tests/unit/ui/offering-surfaces.test.tsx && npm run test:copy</automated></verify>
  <done>Offering cards/detail/shortlist and the bounded presentation resolver preserve exact truth, expose only inspect-only controls and remain complete without a model.</done>
  <stop>Stop if a component must compute meaning, share a callback/import with inquiry/effect code, or Astryx cannot emit the required accessible semantic without source-contract change.</stop>
  <predecessor>Exact parent-integrated 05-05 revision/tree; clean UI child.</predecessor>
  <evidence>Component fixture evidence only; not browser/hosted/accessibility-in-use proof.</evidence>
  <handoff>Commit owned files; return base/result revision/tree, paths, commands/results, rendered states, unresolved UI/source mismatch, earliest blocker and next safe action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire public browse, Offering detail and server-authoritative compare routes</name>
  <files>src/routes/registry.tsx, src/routes/$slug.tsx, src/routes/$slug.offerings.$offeringRef.tsx, src/routes/compare.tsx, src/routeTree.gen.ts</files>
  <behavior>
    - "RED: all routes work without authentication and every compare load re-resolves the validated URL server-side."
    - "RED: malformed/duplicate/fifth selection preserves a bounded ordinary state; old exact revision never auto-replaces."
    - "RED: compare is noindex/follow, absent from global navigation/sitemap, and transient responses are no-store."
  </behavior>
  <action>Make `/registry` business-led but Offering-aware, add exact Offering detail under the business, and add `/compare` consuming only the comparison public seam. Use canonical bounded URL state and server loader; browser edits references/priorities only. Preserve valid items through refused entries and require deliberate current-revision replacement. Remove/isolate mutating demand-capture empty-state and contact/inquiry controls from the Phase-5 loop. Set canonical/noindex/no-store policy from UI-SPEC. Regenerate `src/routeTree.gen.ts` once from the clean route sources; never hand-edit it.</action>
  <verify><automated>npm exec -- vitest run tests/unit/ui/offering-comparison.test.tsx tests/integration/registry-api.test.ts && npm run test:seo</automated></verify>
  <diagnostic>Run `npm run typecheck` separately; changed route/component errors block, while only named unchanged inherited failures may remain diagnostic until 05-08.</diagnostic>
  <done>No-login routes traverse the accepted loop, refresh/share re-resolves exact safe truth, SEO/cache behavior is bounded and generated routing matches source.</done>
  <stop>Stop if route generation includes unrelated dirty changes, loader requires authentication/browser truth, or removing a nested parent effect group needs ownership outside this plan.</stop>
  <predecessor>Task 1 result.</predecessor>
  <evidence>Source/unit/integration evidence; no browser accessibility or hosted claim yet.</evidence>
  <handoff>Return result revision/tree, route paths, generated diff, commands/results, exact URL behavior, unresolved route collision, earliest blocker and next action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the answer-first surface, responsive full evidence and zero-effect controls</name>
  <files>src/components/ae/comparison/AeOfferingComparison.tsx, tests/e2e/comparison-surface.spec.ts, tests/e2e/a11y/comparison.spec.ts</files>
  <behavior>
    - "RED: direct posture, decisive differences and every required caveat precede full evidence; default/invalid composition remains complete."
    - "RED: the native See full comparison disclosure contains a desktop table and CSS-selected mobile dl with identical cells/order and no JS viewport branch."
    - "RED: keyboard/focus recovery, 320px, declared 400% zoom and reduced motion satisfy UI-SPEC."
    - "RED: browse/detail/shortlist/priority/share network and ledger observations show zero mutation/effect."
  </behavior>
  <action>Resolve the GenUI presentation over the already-complete deterministic brief: direct posture, up to three decisive differences, every mandatory caveat and one source-owned continuation. The deterministic answer renders immediately as fallback while composition is absent or invalid. Put complete evidence in a native, initially closed, user-operated `details` disclosure labelled **See full comparison**: caption/scoped table on desktop and equivalent fact-by-fact `dl` below `md`; only CSS changes visible projection. Implement complete loading, empty, refused, partial, stale, unknown, not supplied, not comparable, changed, no-priority, blocked, tie, ordered, composition-fallback and share failure/success states using UI-SPEC copy. Browser tests drive the actual public loop, back/forward, refresh/share, keyboard, focus, responsiveness, model disabled/failed/switched fixtures and zero mutation/endpoint fetch. Automated results are not a real screen-reader/comprehension claim.</action>
  <verify><automated>npm exec -- vitest run tests/unit/comparison/presentation.test.ts tests/unit/ui/offering-comparison.test.tsx && npm exec -- playwright test tests/e2e/comparison-surface.spec.ts tests/e2e/a11y/comparison.spec.ts</automated></verify>
  <done>All canonical states and interaction/accessibility contracts pass locally; every Phase-5 control remains effect-free.</done>
  <stop>Stop at the first semantic mismatch between table/list, hidden state at zoom, focus loss, endpoint/inquiry network call or mutation observation; do not waive it with snapshots.</stop>
  <predecessor>Task 2 route result.</predecessor>
  <evidence>Labelled local browser automation; not hosted, real screen-reader, comprehension or customer proof.</evidence>
  <handoff>Return base/result revision/tree, paths, commands/results, browser observations/screenshots, first failure, earliest blocker, claim ceiling and next parent integration action.</handoff>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/browser → server loader | Browser state is input, never eligibility/currentness authority. |
| semantic result → human controls | Rendering must not widen into inquiry/effect behavior. |
| GenUI proposal → presentation resolver | Untrusted output may adapt registered density/emphasis only and must never delay deterministic truth or control disclosure state. |
| external published-details navigation | Query/referrer and endpoint execution must remain contained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-06A | Tampering | URL priorities/selections | high | mitigate | shared strict codec and server re-resolution |
| T-05-06B | Information disclosure | external links/referrers | medium | mitigate | public refs only; no-referrer external navigation |
| T-05-06C | Elevation of privilege | shared inquiry callbacks | high | mitigate | hard action-region isolation and browser zero-effect test |
| T-05-06D | Denial of service | malformed/over-cap state | medium | mitigate | bounded ordinary recovery and stable loaders |
| T-05-06E | Spoofing | visual recommendation | medium | mitigate | no score/best styling; source-owned posture and reasons |
| T-05-06F | Tampering | model presentation proposal | high | mitigate | strict IDs/enums, digest binding, mandatory regions and deterministic fallback |
</threat_model>

<verification>Parent integrates 05-06, reruns UI/component/copy/SEO/browser checks, then gives that exact loader-bearing revision to 05-07.</verification>
<success_criteria>A public visitor can complete the full inspect-only loop accessibly from labelled local data, with no effect path reachable.</success_criteria>
<output>Create `.planning/phases/05-consumer-operating-proof/05-06-SUMMARY.md` when done.</output>
