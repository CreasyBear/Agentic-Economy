---
phase: 05-consumer-operating-proof
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - src/modules/registry/public-inquiry-projection.ts
  - src/modules/registry/internal/search-documents.ts
  - src/modules/answer/internal/dto-to-answer-source.ts
  - src/modules/answer/answer-synthesizer.ts
  - src/modules/answer-thread/internal/tool-runner.ts
  - src/modules/discovery/internal/ucp-manifest.ts
  - src/modules/discovery/developer-discovery.ts
  - src/modules/discovery/internal/discovery-files.ts
  - tests/integration/discovery-llms-offering-parity.test.ts
  - tests/integration/answer-tool-calls.test.ts
autonomous: true
requirements: [P5-REGISTRY]
must_haves:
  truths:
    - "Every live consumer of registry action output uses profile-bearing Offering-v2 semantics or an explicitly isolated legacy-only source that never receives v2 action output."
    - "Answer and Answer Thread preserve Offering revision/profile facts without rebuilding services[], contact ranking or trust meaning."
    - "A literal source inventory fails when any new live v1 services[] action-output consumer appears outside the accepted list."
  artifacts:
    - path: "src/modules/answer/internal/dto-to-answer-source.ts"
      provides: "Offering-aware answer projection"
    - path: "src/modules/answer-thread/internal/tool-runner.ts"
      provides: "Offering-v2 registered-action result handling"
    - path: "tests/integration/discovery-llms-offering-parity.test.ts"
      provides: "discovery consumer and literal inventory enforcement"
  key_links:
    - from: "registry actions"
      to: "Answer, Answer Thread and discovery consumers"
      via: "one profile-bearing Offering-v2 output contract"
    - from: "consumer inventory test"
      to: "repository source graph"
      via: "literal allowlist plus fail-on-new-consumer scan"
---

<objective>
Migrate the bounded live v1 `services[]` consumer blast radius after the
profile-bearing registry cutover, preserving D-07, D-10 and D-12 without
expanding Plan 03 beyond registry/HTTP ownership.

Purpose: prevent downstream Answer/discovery adapters from silently erasing or
reconstructing Offering identity and profile facts.
Output: migrated consumers plus a literal fail-closed inventory gate.
</objective>

<execution_context>@/Users/joelchan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-consumer-operating-proof/05-03-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-CONTEXT.md
@.planning/phases/05-consumer-operating-proof/05-RESEARCH.md
@.planning/phases/05-consumer-operating-proof/05-PATTERNS.md
@src/modules/registry/registry.actions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate registry-adjacent, Answer and Answer Thread consumers</name>
  <files>src/modules/registry/public-inquiry-projection.ts, src/modules/registry/internal/search-documents.ts, src/modules/answer/internal/dto-to-answer-source.ts, src/modules/answer/answer-synthesizer.ts, src/modules/answer-thread/internal/tool-runner.ts, tests/integration/answer-tool-calls.test.ts</files>
  <behavior>
    - "RED: professional_service:v1 and machine_data:v1 action results retain exact Offering revision, common facts and profile facts through Answer DTO/synthesis."
    - "RED: Answer Thread tool execution consumes Offering-v2 directly and never reconstructs services[], trustTier or contact-availability order."
    - "RED: registry inquiry/search projections cannot feed v2 actions back through legacy service identity."
  </behavior>
  <action>Start from the exact 05-03 result. Migrate `public-inquiry-projection.ts`, `search-documents.ts`, `dto-to-answer-source.ts`, `answer-synthesizer.ts` and `tool-runner.ts` to the strict Offering-v2 contract. Preserve business context, exact Offering revision, provenance/currentness, support posture and both closed profiles; do not add comparison ordering or effect controls. Where a legacy-only inquiry/search projection must remain, give it an explicit legacy input type and prove no v2 registered-action result can enter it. Never adapt v2 back into `services[]`, infer a primary service, restore `trustTier`, or rank by contact availability.</action>
  <verify><automated>npm exec -- vitest run tests/integration/answer-tool-calls.test.ts tests/integration/registry-offering-parity.test.ts tests/unit/answer/answer-tool-use-agent.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately; changed-path errors block, while only the unchanged named 05-01 inherited-owner list may remain diagnostic until 05-08.</diagnostic>
  <done>Registry-adjacent, Answer and Answer Thread consumers preserve both Offering profiles and exact revision semantics without legacy reconstruction or new ranking.</done>
  <stop>Stop if a consumer requires lossy v2→services adaptation, Answer-owned ranking, an effect continuation, or a path outside this plan. Return the exact consumer and smallest owner decision.</stop>
  <predecessor>Exact clean 05-03 result revision/tree.</predecessor>
  <evidence>Source and focused consumer fixture evidence only; no hosted agent or recommendation-quality claim.</evidence>
  <handoff>Return base/result revision/tree, changed paths, commands/results, profile round-trip observations, unresolved consumer, earliest blocker and next safe action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate discovery consumers and enforce the literal inventory</name>
  <files>src/modules/discovery/internal/ucp-manifest.ts, src/modules/discovery/developer-discovery.ts, src/modules/discovery/internal/discovery-files.ts, tests/integration/discovery-llms-offering-parity.test.ts</files>
  <behavior>
    - "RED: UCP/developer discovery preserves its intentionally versioned Offering projection and never consumes v2 action results as services[]."
    - "RED: the literal live consumer inventory equals the reviewed list; a new matching source path fails with its filename."
    - "RED: discovery descriptors do not claim agentJson metadata alone is callable reachability."
  </behavior>
  <action>The accepted live v1 `services[]` action-output consumer inventory is exactly: `src/modules/registry/public-inquiry-projection.ts`, `src/modules/registry/internal/search-documents.ts`, `src/modules/answer/internal/dto-to-answer-source.ts`, `src/modules/answer/answer-synthesizer.ts`, `src/modules/answer-thread/internal/tool-runner.ts`, `src/modules/discovery/internal/ucp-manifest.ts`, and `src/modules/discovery/developer-discovery.ts`; `src/modules/discovery/internal/discovery-files.ts` owns their emitted descriptors. Migrate the discovery paths to Offering-aware or explicit isolated-legacy inputs, then add a source-facing inventory assertion that scans live production consumers and fails with any path outside this literal list. Do not merely count tokens: classify imports/types flowing from registry action output. Keep descriptors honest that callable registry reachability comes from the three public HTTP adapters and comparison reachability will come from 05-07 fixed POST.</action>
  <verify><automated>npm exec -- vitest run tests/integration/discovery-llms-offering-parity.test.ts tests/integration/registry-offering-parity.test.ts && npm run test:imports && npm run test:copy</automated></verify>
  <done>Discovery consumers preserve intended Offering semantics, emitted claims name real adapters, and the literal inventory fails closed on any newly introduced v1 action-output consumer.</done>
  <stop>Stop on a new consumer outside the literal inventory, profile loss, descriptor-only reachability claim or need to widen into a generic tools host.</stop>
  <predecessor>Task 1 result.</predecessor>
  <evidence>Source/integration/copy/import evidence for the bounded consumer graph; not hosted reachability.</evidence>
  <handoff>Return base/result revision/tree, changed paths, final literal inventory, scan command/result, unresolved consumer, earliest blocker and next safe parent integration action.</handoff>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Offering-v2 action output → Answer/discovery adapters | Consumers must preserve exact public meaning without legacy reconstruction. |
| repository source graph → inventory gate | New live consumers must not bypass reviewed migration ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04A | Tampering | v2 consumer adaptation | high | mitigate | both-profile round-trip fixtures and no services reconstruction |
| T-05-04B | Spoofing | trust/contact projection | high | mitigate | remove trustTier/contact ranking from v2 action consumers |
| T-05-04C | Information disclosure | Answer/discovery output | high | mitigate | strict public fields only; no private snapshot fields |
| T-05-04D | Repudiation | consumer inventory | medium | mitigate | literal path inventory and fail-on-new-consumer source scan |
| T-05-04E | Elevation of privilege | descriptor reachability | high | mitigate | descriptors name only executable HTTP adapters; no metadata-only claim |
</threat_model>

<verification>Parent integrates 05-04, reruns registry parity plus consumer/inventory/copy/import checks, and gives the exact clean result to 05-05 comparison semantics.</verification>
<success_criteria>All reviewed v1 action-output consumers have explicit truthful dispositions and no unreviewed consumer can enter silently.</success_criteria>
<output>Create `.planning/phases/05-consumer-operating-proof/05-04-SUMMARY.md` when done.</output>
