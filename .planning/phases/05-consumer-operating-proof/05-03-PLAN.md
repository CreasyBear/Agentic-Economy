---
phase: 05-consumer-operating-proof
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/modules/registry/public.ts
  - src/modules/registry/registry.functions.ts
  - src/modules/registry/registry.actions.ts
  - src/modules/registry/internal/offering-api-projection.ts
  - src/modules/registry/internal/offering-snapshot-codec.ts
  - src/routes/api.businesses.ts
  - src/routes/api.businesses.search.ts
  - src/routes/api.businesses.$slug.ts
  - convex/registry.ts
  - tests/unit/actions/registry.test.ts
  - tests/unit/registry/offering-runtime-guards.test.ts
  - tests/integration/registry-api.test.ts
  - tests/integration/registry-offering-parity.test.ts
autonomous: true
requirements: [P5-REGISTRY]
must_haves:
  truths:
    - "Public HTTP and registered registry list/search/detail expose the same Offering-v2 facts and exact revisions."
    - "Stored snapshots and Convex returns are strictly decoded; private, extra and legacy service fields have no public destination."
    - "Action descriptor surfaces correspond to a real tested adapter, not registration alone."
  artifacts:
    - path: "src/modules/registry/internal/offering-snapshot-codec.ts"
      provides: "strict stored/public v2 decode"
    - path: "tests/integration/registry-offering-parity.test.ts"
      provides: "HTTP/application/action semantic parity falsifier"
  key_links:
    - from: "HTTP routes and registry actions"
      to: "registry Offering-v2 application reads"
      via: "one profile-preserving source port and strict schemas"
---

<objective>
Cut the legacy service-shaped registered registry actions to the existing safe
Offering-v2 application meaning and harden hostile projections, implementing
D-02, D-07, D-10 and D-11.

Purpose: make browse/detail semantics trustworthy before comparison actions exist.
Output: v2 list/search/detail parity with explicit reachable structured adapter.
</objective>

<execution_context>@/Users/joelchan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-consumer-operating-proof/05-02-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-RESEARCH.md
@.planning/phases/05-consumer-operating-proof/05-PATTERNS.md
@convex/_generated/ai/guidelines.md
@src/modules/registry/registry.actions.ts
@src/modules/registry/registry.functions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve profile-bearing Offering-v2 facts through strict registry codecs</name>
  <files>src/modules/registry/public.ts, src/modules/registry/internal/offering-api-projection.ts, src/modules/registry/internal/offering-snapshot-codec.ts, convex/registry.ts, tests/unit/registry/offering-runtime-guards.test.ts</files>
  <behavior>
    - "RED: extra keys, source hashes, credentials, adapter config, private reasons and arbitrary JSON refuse before projection."
    - "RED: both professional_service:v1 and machine_data:v1 facts round-trip catalog → registry HTTP → registry action without loss, reinterpretation or extra fields."
    - "RED: all v2 Convex list/search/detail returns use exact validators rather than permissive values."
    - "RED: bounded Offering search/hydration never widens to a catalog scan or presentation-text identity lookup."
  </behavior>
  <action>Before any Convex edit, read `convex/_generated/ai/guidelines.md` completely and follow its installed-version validator/index/bounded-read rules. Create one strict stored-snapshot codec and one safe public Offering-v2 DTO schema from the integrated 05-02 profile-bearing contract. Replace cast-only JSON parsing and each permissive v2 Convex return validator. Keep public allowlisting so unsafe fields have no destination while every safe common/profile fact, provenance and currentness field survives unchanged. Add a catalog → registry HTTP → registered action round-trip fixture for both closed profiles and compare semantic payloads after transport-only fields. Preserve D-07 support distinctions without a trust grade. Keep reads indexed/cap-plus-one bounded and do not probe declared endpoints. If the integrated 05-02 interface lacks a required field, stop and request parent reconciliation rather than dropping it.</action>
  <verify><automated>npm exec -- vitest run tests/unit/registry/offering-api-projection.test.ts tests/unit/registry/offering-runtime-guards.test.ts tests/integration/registry-api.test.ts tests/integration/registry-offering-parity.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately; changed-path errors block this task, while only named unchanged inherited 05-01 failures may remain diagnostic until 05-08.</diagnostic>
  <done>Both profile versions round-trip catalog → HTTP → action exactly, hostile snapshots/returns refuse, private/legacy fields are absent and reads stay bounded.</done>
  <stop>Stop on profile field loss/reinterpretation, any need to trust a cast, expose a private source field, edit a 05-02 path, or replace indexed bounded work with a scan.</stop>
  <predecessor>Exact 05-02 result revision/tree in a clean isolated child. The source-audit decision is fixed: existing public business API routes must execute their corresponding registered actions.</predecessor>
  <evidence>Source/unit/integration fixture evidence; no public hosted reachability claim.</evidence>
  <handoff>Commit owned paths; return base/result revision/tree, changes, RED/GREEN commands, hostile refusal payloads, first unresolved mismatch, earliest blocker and next parent action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut registry actions and public API routes to profile-bearing v2</name>
  <files>src/modules/registry/registry.functions.ts, src/modules/registry/registry.actions.ts, src/routes/api.businesses.ts, src/routes/api.businesses.search.ts, src/routes/api.businesses.$slug.ts, tests/unit/actions/registry.test.ts, tests/integration/registry-api.test.ts, tests/integration/registry-offering-parity.test.ts</files>
  <behavior>
    - "RED: list/search/detail HTTP application reads and `registry.*.run` deep-agree on Offering revisions/support after transport-only fields."
    - "RED: action output contains no `services`, `trustTier`, synthetic legacy ref or contact-availability ordering, while both closed profiles remain intact."
    - "RED: GET `/api/businesses`, `/api/businesses/search` and `/api/businesses/$slug` execute their corresponding registered actions with public/anonymous auth and strict route schemas; descriptor-only exposure fails parity."
  </behavior>
  <action>Preserve action IDs but bump their output/invocation contracts to profile-bearing Offering-v2 and delegate list/search/detail to the same source-owned application port. Per the resolved source audit, make the existing public anonymous GET routes `src/routes/api.businesses.ts`, `src/routes/api.businesses.search.ts` and `src/routes/api.businesses.$slug.ts` validate their strict request/query schemas and execute `registry.list`, `registry.search` and `registry.detail` respectively; they must not call application reads beside the action or accept a caller-selected action ID. Keep actions authority-free, replayable and consequence class read-only per D-10/D-11. Do not migrate Answer, Answer Thread, discovery, public-inquiry projection or search-document consumers here; 05-04 owns that separate blast radius.</action>
  <verify><automated>npm exec -- vitest run tests/unit/actions/registry.test.ts tests/unit/registry/offering-runtime-guards.test.ts tests/integration/registry-offering-parity.test.ts</automated></verify>
  <done>All three public API routes execute their registered actions and preserve both Offering profiles with public/anonymous, strict read-only semantics.</done>
  <stop>Stop if any profile fact is lost, an API route does not execute its corresponding action, a route needs caller-selected action identity, or migration would add inquiry/execution behavior.</stop>
  <predecessor>Task 1 child result.</predecessor>
  <evidence>Source and focused fixture parity only; registration does not itself prove public hosted reachability.</evidence>
  <handoff>Return base/result revision/tree, changed paths, commands/results, compared human/action payload digests, unresolved consumer, earliest blocker and next safe parent integration action.</handoff>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| stored projection → public DTO | Hostile/stale JSON must be decoded and allowlisted. |
| HTTP/action adapter → source application | Transport must not own or widen Offering meaning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-03A | Tampering | stored projectionJson | high | mitigate | strict codec and exact return validators |
| T-05-03B | Information disclosure | public agent/HTTP payload | high | mitigate | safe DTO allowlist and hostile substitution tests |
| T-05-03C | Spoofing | legacy Offering adapters | high | mitigate | native refs only; parity rejects synthetic residue |
| T-05-03D | Repudiation | declared agent surface | medium | mitigate | execute named adapter against the same fixture |
| T-05-03E | Denial of service | public search | medium | mitigate | indexed/cap-plus-one bounded hydration |
</threat_model>

<verification>Parent integrates 05-03, regenerates any parent-owned edge once, and reruns its registry/HTTP matrices before releasing the exact result to 05-04 consumer migration.</verification>
<success_criteria>Registry browse/detail has one safe Offering-v2 meaning across HTTP and registered structured action paths.</success_criteria>
<output>Create `.planning/phases/05-consumer-operating-proof/05-03-SUMMARY.md` when done.</output>
