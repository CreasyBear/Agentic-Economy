---
phase: 05-consumer-operating-proof
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .planning/adr/ADR-026-one-business-supply-graph.md
  - src/modules/catalog/internal/offering-supply.ts
  - src/modules/catalog/internal/offering-source.ts
  - src/modules/catalog/internal/offering-public-history.ts
  - src/modules/catalog/internal/schema.ts
  - src/modules/catalog/public.ts
  - convex/catalog.ts
  - convex/catalogSupplyProjection.ts
  - tests/unit/catalog/offering-public-history.test.ts
  - tests/unit/comparison/contract.test.ts
  - tests/unit/comparison/profiles.test.ts
autonomous: false
requirements: [P5-CATALOG]
must_haves:
  truths:
    - "Only an exact revision proven historically public and still safe can be resolved; no current revision is substituted."
    - "Offering revisions contain one strict common envelope and either professional_service:v1 or machine_data:v1 closed facts."
    - "Unknown, not supplied and stale remain typed facts with provenance/currentness, never empty defaults."
  artifacts:
    - path: "src/modules/catalog/internal/offering-public-history.ts"
      provides: "exact historical public eligibility ordinary outcomes"
    - path: "src/modules/catalog/internal/offering-supply.ts"
      provides: "closed profile facts inside immutable revision identity"
  key_links:
    - from: "offering source hash"
      to: "closed profile facts"
      via: "revision-owned validated input"
    - from: "historical resolver"
      to: "live suppression/current revision"
      via: "exact indexed public-history check before projection"
---

<objective>
Make exact historical Offering revisions and two bounded fact profiles truthful
at the catalog source owner, implementing D-04–D-09 and D-14.

Purpose: give comparison a source-owned immutable fact/refusal contract.
Output: accepted ADR policy, strict revision/profile contracts and indexed history read.
</objective>

<execution_context>@/Users/joelchan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-consumer-operating-proof/05-01-SUMMARY.md
@.planning/adr/ADR-026-one-business-supply-graph.md
@.planning/phases/05-consumer-operating-proof/05-CONTEXT.md
@.planning/phases/05-consumer-operating-proof/05-RESEARCH.md
@convex/_generated/ai/guidelines.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Accept the historical withdrawal and safe-display policy</name>
  <files>.planning/adr/ADR-026-one-business-supply-graph.md; no production source changes before this decision</files>
  <action>Before historical-public storage exists, decide the irreversible public-history rule required by D-04/D-06: whether a previously public revision remains displayable after ordinary Offering withdrawal when the business is still public, versus withdrawal making historical facts unavailable. In both options, live business suppression, privacy/safety withdrawal, never-public state, business mismatch and source-hash mismatch always refuse before facts. Record retention, `withdrawnAt`, safe-display disposition, current-revision notice and explicit non-substitution behavior in the accepted ADR amendment.</action>
  <verify><automated>rg -n "historical public|withdrawn|safe display|suppression|never public|never substitute" .planning/adr/ADR-026-one-business-supply-graph.md</automated></verify>
  <done>ADR-026 contains one accepted, testable withdrawal/safe-display policy with no ambiguity about suppression, privacy or current-version substitution.</done>
  <stop>Stop all 05-02 production edits until the decision owner accepts one policy. Do not encode the research recommendation as implicit authority.</stop>
  <predecessor>Exact 05-01 result revision/tree and custody handoff.</predecessor>
  <evidence>Accepted architecture decision only; no runtime history evidence.</evidence>
  <handoff>Return base revision/tree, accepted policy text, ADR path, verification result, unresolved concern, earliest blocker and next safe implementation action.</handoff>
  <decision>Historical public-revision withdrawal and safe-display policy.</decision>
  <context>The choice changes canonical public data meaning and cannot be left to a resolver implementation.</context>
  <options>
    <option id="retain-safe-history"><name>Retain safe public history</name><pros>Exact shared selections remain inspectable when ordinary withdrawal is not a privacy/safety suppression.</pros><cons>Requires explicit retained-public-history governance.</cons></option>
    <option id="withdraw-hides-history"><name>Withdrawal hides history</name><pros>More conservative disclosure rule.</pros><cons>Older shared selections become unavailable even when once public.</cons></option>
  </options>
  <resume-signal>Select `retain-safe-history` or `withdraw-hides-history` and accept the ADR amendment.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and test exact historical-public eligibility</name>
  <files>.planning/adr/ADR-026-one-business-supply-graph.md, src/modules/catalog/internal/offering-public-history.ts, src/modules/catalog/internal/schema.ts, convex/catalog.ts, convex/catalogSupplyProjection.ts, tests/unit/catalog/offering-public-history.test.ts</files>
  <behavior>
    - "RED: a never-public, cross-business, source-hash-mismatched or currently suppressed revision reveals no facts."
    - "RED: a previously public eligible revision stays exact and separately reports a newer current public revision."
    - "RED: a legacy synthetic ref is ineligible and reads are exact-indexed and bounded."
  </behavior>
  <action>Implement the exact policy accepted in Task 1: immutable publication evidence keyed by exact businessId/offeringRef/revision/sourceHash, publishedAt, optional withdrawal and safe-display disposition; live business/Offering suppression always precedes historical projection. Keep the accepted ADR wording and code in lockstep. Never read an immutable revision row as proof it was public, never silently substitute current, and do not use scans or `collect()` then slice. This implements D-04–D-06 and the research stop condition.</action>
  <verify><automated>npm exec -- vitest run tests/unit/catalog/offering-public-history.test.ts tests/unit/schema/convex-schema.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately; changed-path errors block, while only the literal unchanged inherited-owner list from 05-01 may remain diagnostic until 05-08.</diagnostic>
  <done>Accepted ADR text and source agree; hostile/never-public/suppressed refs refuse, eligible history remains exact, newer current is separate, and queries are bounded by exact indexes.</done>
  <stop>Stop before implementation if the ADR remains unaccepted, withdrawal/privacy policy is ambiguous, or the required schema edge is absent from the exact 05-01 predecessor. Return the smallest policy decision needed.</stop>
  <predecessor>Exact 05-01 result revision/tree plus Task 1 accepted ADR policy; child worktree must be clean and match it.</predecessor>
  <evidence>ADR plus source/unit/Convex fixture evidence only; no public/hosted history claim.</evidence>
  <handoff>Commit owned files; return base/result revision/tree, changed paths, RED/GREEN commands, observed refusal/exactness, unresolved policy, earliest blocker and next parent action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add strict common envelope and two closed profile codecs</name>
  <files>src/modules/catalog/internal/offering-supply.ts, src/modules/catalog/internal/offering-source.ts, src/modules/catalog/public.ts, tests/unit/comparison/contract.test.ts, tests/unit/comparison/profiles.test.ts</files>
  <behavior>
    - "RED: extra keys, arbitrary profile IDs, unbounded strings, invalid units/dates and cast-only payloads refuse."
    - "RED: professional_service:v1 accepts only scopeBasis, priceBasis, timingBasis and serviceArea fact contracts."
    - "RED: machine_data:v1 accepts only interfaceFormat, requestMethod, authentication, priceBasis and freshnessOrUpdateCadence fact contracts."
  </behavior>
  <action>Extend immutable Offering revision facts with one versioned common comparison envelope and a discriminated closed profile union per D-07/D-08/D-14. Each field uses typed `known`, `unknown`, `not_supplied` or `stale` states with bounded provenance and observed/valid dates; `not_comparable` remains a derived comparison-cell state. Validate before hashing and persist the profile inside the exact revision source hash. No `Record<string, unknown>`, property array, generic ontology, score, reputation/trust grade or renderer-defined meaning. Preserve compatible current Offering authoring through explicit migration/refusal rather than unsafe casts.</action>
  <verify><automated>npm exec -- vitest run tests/unit/comparison/contract.test.ts tests/unit/comparison/profiles.test.ts tests/unit/catalog/offering-source.test.ts tests/unit/catalog/offering-supply.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately with the same changed-path/inherited-owner disposition; full green is mandatory at 05-08.</diagnostic>
  <done>Both materially unlike profiles round-trip strictly, hostile/extra data refuses, fact provenance/currentness is explicit, and profile facts participate in exact revision hashing.</done>
  <stop>Stop if compatibility would require an arbitrary property bag, current records cannot be migrated/refused deterministically, or profile facts would be joined after hashing.</stop>
  <predecessor>Task 2 result inside the same child revision.</predecessor>
  <evidence>Source and focused fixture contract evidence; no recommendation quality or real-supply claim.</evidence>
  <handoff>Return base/result revision/tree, changed paths, commands/results, two profile examples and hostile refusals, unresolved finding, earliest blocker and next safe parent integration action.</handoff>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public tuple → historical catalog read | Untrusted refs must not expose unpublished/cross-business facts. |
| authored/stored profile → immutable revision | Hostile or drifting shapes must be strictly decoded before hashing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-02A | Information disclosure | historical resolver | high | mitigate | publication evidence plus live suppression before fact read |
| T-05-02B | Spoofing | business/offering lineage | high | mitigate | exact composite identity and source-hash checks |
| T-05-02C | Tampering | profile payload | high | mitigate | strict closed codecs before revision hash |
| T-05-02D | Denial of service | public history query | medium | mitigate | exact indexes, bounded terminal reads, no scans |
</threat_model>

<verification>Run both task matrices from the clean child; parent integrates the result and reruns them before 05-03 starts from that exact profile-bearing revision.</verification>
<success_criteria>Catalog can supply an exact safe historical revision with one strict profile and explicit fact states without comparison or transport owning truth.</success_criteria>
<output>Create `.planning/phases/05-consumer-operating-proof/05-02-SUMMARY.md` when done.</output>
