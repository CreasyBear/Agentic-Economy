---
phase: 05-consumer-operating-proof
plan: 07
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - src/modules/comparison/comparison.functions.ts
  - src/modules/comparison/comparison.actions.ts
  - src/modules/actions/index.ts
  - src/routes/api.compare.ts
  - src/routeTree.gen.ts
  - tests/integration/comparison-public-agent-route.test.ts
  - tests/integration/comparison-surface-parity.test.ts
  - tests/imports/comparison-boundaries.test.ts
  - tests/eval/offering-comparison-transfer.test.ts
autonomous: true
requirements: [P5-AGENT]
must_haves:
  truths:
    - "A structured no-authority read-only comparison action returns the same offering-comparison:v1 meaning as the human loader."
    - "The full comparison import graph cannot reach inquiry, Customer Request, RoutePlan, Action Invocation, mandate, booking, payment, provider transport or effect owners."
    - "One professional-service vertical and one machine/data horizontal transfer use the same resolver/comparator/host contract."
  artifacts:
    - path: "src/modules/comparison/comparison.actions.ts"
      provides: "registered inspect-only comparison contract"
    - path: "tests/imports/comparison-boundaries.test.ts"
      provides: "recursive effect-owner fence"
    - path: "src/routes/api.compare.ts"
      provides: "fixed public anonymous POST adapter for comparison.compare only"
    - path: "tests/eval/offering-comparison-transfer.test.ts"
      provides: "vertical/horizontal falsification"
  key_links:
    - from: "comparison action and human loader"
      to: "comparison public application seam"
      via: "one input/output semantic contract"
    - from: "POST /api/compare"
      to: "comparisonCompareAction"
      via: "fixed import and validated read-only harness; no caller-selected action ID"
---

<objective>
Expose exact Offering comparison to structured agents through one fixed public
POST adapter over the registered action, then falsify semantic drift and every effect import for
D-10–D-15.

Purpose: prove comparison is one reusable inspect-only capability, not a UI-only
workflow or disguised execution surface.
Output: registered action, human/agent parity, recursive fence and two-axis evals.
</objective>

<execution_context>@/Users/joelchan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-consumer-operating-proof/05-03-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-05-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-06-SUMMARY.md
@.planning/phases/05-consumer-operating-proof/05-CONTEXT.md
@.agents/skills/ae-actions-and-modules/SKILL.md
@.agents/skills/ae-agent-surfaces/SKILL.md
@.planning/phases/05-consumer-operating-proof/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register comparison.compare and expose one fixed public POST adapter</name>
  <files>src/modules/comparison/comparison.functions.ts, src/modules/comparison/comparison.actions.ts, src/modules/actions/index.ts, src/routes/api.compare.ts, src/routeTree.gen.ts, tests/integration/comparison-public-agent-route.test.ts, tests/integration/comparison-surface-parity.test.ts</files>
  <behavior>
    - "RED: external POST response and the actual `/compare` loader/application result over the same fixture/priorities deep-agree after transport-only fields."
    - "RED: POST body is strict and bounded to four exact selections and three closed priorities; malformed is 400 and oversized is 413."
    - "RED: action is readOnly, authority none, replayable and POST runs the validated harness with surface agentJson and allowWrites false."
    - "RED: add/remove shortlist are client reference edits, not registered mutations; compare initiates zero external effect."
  </behavior>
  <action>Register `comparison.compare` through `defineAction` with the strict shared body/output schemas, `consequenceClass: read_only`, authority none and replayable retry. Add exactly `POST /api/compare` at `src/routes/api.compare.ts`; it imports only `comparisonCompareAction`, accepts no action ID or generic tool name, is public/anonymous, runs the existing validated harness with `surface: 'agentJson'` and `allowWrites: false`, and returns only `offering-comparison:v1` with `Cache-Control: no-store`. The strict body allows at most four exact selection tuples and three closed priorities. Return ordinary unavailable/unranked results as HTTP 200, malformed input as 400 and oversized input as 413. The server must re-resolve exact publication/suppression through `comparison.functions.ts`; neither route nor action recomputes meaning. Regenerate `src/routeTree.gen.ts` from the integrated 05-05 route tree. Deep-compare the external POST response to the actual `/compare` loader/application result, not a hand-written surrogate. This fixed adapter is the resolved source-audit decision; `agentJson` metadata alone remains non-evidence.</action>
  <verify><automated>npm exec -- vitest run tests/integration/comparison-public-agent-route.test.ts tests/integration/comparison-surface-parity.test.ts tests/unit/actions/registry.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately; changed-path errors block, while only named unchanged inherited 05-01 failures may remain diagnostic until mandatory 05-08 green.</diagnostic>
  <done>The fixed anonymous POST executes only comparisonCompareAction, enforces exact status/schema/cache behavior, re-resolves source truth and deep-equals the actual human loader result with zero effect.</done>
  <stop>Stop if the adapter needs a caller-selected action ID, generic tools runtime, authentication, writable harness, client-owned resolution, permissive body or parity adapter. Return the exact mismatch.</stop>
  <predecessor>Exact parent-integrated 05-06 revision/tree containing the actual `/compare` loader; clean agent child.</predecessor>
  <evidence>Source/integration evidence for the fixed public request adapter; not hosted external-agent proof.</evidence>
  <handoff>Commit owned files; return base/result revision/tree, paths, commands/results, action descriptor and compared digests, unresolved adapter gap, earliest blocker and next parent action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence the comparison graph from effects and private projections</name>
  <files>tests/imports/comparison-boundaries.test.ts, tests/integration/comparison-public-agent-route.test.ts, tests/integration/comparison-surface-parity.test.ts</files>
  <behavior>
    - "RED: recursive local-import traversal detects direct, aliased and transitive effect-owner imports from comparison actions/functions and Phase-5 public seams."
    - "RED: outputs contain no source hash, credential, adapter config, private reason, arbitrary stored field or effect continuation."
    - "RED: comparison execution produces zero mutation, Request, invocation, attempt, endpoint fetch or provider call observation."
  </behavior>
  <action>Adapt the established recursive import-graph traversal, starting at comparison functions/actions, `src/routes/api.compare.ts`, the actual human compare loader and supported public module seams. Assert the API route imports only the fixed comparison action/harness dependencies and no registry of caller-selected actions. Forbid inquiry/demand capture, Customer Request, RoutePlan, Action Invocation, mandate, booking, payment, provider transport/effect and private `internal/*` imports outside the comparison owner. Keep catalog/registry supported read ports allowed. Pair architecture fencing with executed POST/loader parity and spy assertions so string grep is not the sole oracle. The allowed continuation vocabulary is view/remove/compare/change-priorities only per D-11.</action>
  <verify><automated>npm exec -- vitest run tests/imports/comparison-boundaries.test.ts tests/integration/comparison-public-agent-route.test.ts tests/integration/comparison-surface-parity.test.ts && npm run test:imports</automated></verify>
  <done>Recursive ownership and runtime spies prove that all comparison entries are read-only, safe-projection consumers with no effect continuation.</done>
  <stop>Stop at the first transitive effect/private import or observed mutation/fetch. Do not allowlist it merely because current tests do not trigger it; move the dependency behind a safe read port or return the blocker.</stop>
  <predecessor>Task 1 result.</predecessor>
  <evidence>Static import-graph plus integration fixture negative evidence; it proves only named paths/fixtures.</evidence>
  <handoff>Return result revision/tree, changed tests, graph entries/forbidden families, commands/results, first violation, earliest blocker and next safe action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run vertical and horizontal comparison falsification evals</name>
  <files>tests/eval/offering-comparison-transfer.test.ts</files>
  <behavior>
    - "RED: professional-service pair covers unranked, comparable priority order, stale/unknown blocker and changed revision without substitution."
    - "RED: machine/data pair uses the same host/resolver/comparator and orders on one registered machine dimension."
    - "RED: cross-profile pair preserves common rows and marks profile-only rows not comparable without a branch or second workflow."
  </behavior>
  <action>Build at least four clearly labelled demo Offerings, two per profile. Drive them through the real comparison application and registered action, not hand-written semantic objects. Mutate one decisive fact/state and require the order or refusal to change. Assert the same result vocabulary, action ID and host across both domains; only profile projection differs. Include no-priority/tie, current/partial/stale/unknown/not-supplied/not-comparable and zero-effect observations. This is the required vertical/horizontal loop for D-12/D-14/D-15.</action>
  <verify><automated>npm exec -- vitest run tests/eval/offering-comparison-transfer.test.ts tests/integration/comparison-surface-parity.test.ts</automated></verify>
  <done>Both materially unlike profiles traverse one comparison capability; the falsifier changes output and cross-profile facts remain honestly non-comparable.</done>
  <stop>Stop if the second profile requires a host/action/route branch, a generic property bag, label matching, score or effect dependency. Return the exact abstraction failure.</stop>
  <predecessor>Task 2 result.</predecessor>
  <evidence>Labelled fixture/eval evidence; not real supply, recommendation quality, customer value or hosted agent proof.</evidence>
  <handoff>Return base/result revision/tree, changed paths, commands/results, vertical/horizontal observations, falsifier outcome, earliest blocker, claim ceiling and next parent integration action.</handoff>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| structured caller → registered action | Untrusted inputs must remain strict, public and read-only. |
| action/application → source read ports | Agent host must not inherit authority or effect machinery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-07A | Elevation of privilege | comparison action | critical | mitigate | read-only contract, no authority, recursive effect fence and zero-mutation spies |
| T-05-07B | Information disclosure | structured output | high | mitigate | strict safe DTO parity and hostile private-field assertions |
| T-05-07C | Spoofing | descriptor-only reachability | high | mitigate | fixed POST route executes only comparisonCompareAction through read-only harness |
| T-05-07D | Tampering | host-owned ranking | high | mitigate | deep parity to one application result and transfer mutation eval |
| T-05-07E | Repudiation | recommendation explanation | medium | mitigate | retain priorities, blocking/decisive facts and result version |
</threat_model>

<verification>Parent integrates 05-07 after 05-06, then 05-08 independently regenerates/verifies routing and reruns UI/e2e plus POST/loader parity/import/eval matrices before deployment.</verification>
<success_criteria>Human and structured callers consume one effect-free Offering comparison contract that transfers across both accepted profiles.</success_criteria>
<output>Create `.planning/phases/05-consumer-operating-proof/05-07-SUMMARY.md` when done.</output>
