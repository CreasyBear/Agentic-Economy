---
phase: 05-consumer-operating-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parent-issued literal Offering predecessor allowlist covering src/modules/catalog, src/modules/registry/internal/offering-api-projection.ts, convex catalog/registry/schema/generated edges, discovery Offering projection, public/operator Offering routes/components, and focused tests
autonomous: false
requirements: [P5-CUSTODY]
must_haves:
  truths:
    - "One exact clean commit/tree owns the complete inherited Offering lane before Phase 5 children start."
    - "Native Offering identity and cutover fail closed; synthetic legacy references never become durable comparison identity."
    - "Generated/schema/readback edges agree and flags remain disabled until their evidence passes."
  artifacts:
    - path: "parent-issued custody packet"
      provides: "exact base revision/tree, literal owned paths and named integrator"
    - path: "05-01-SUMMARY.md"
      provides: "exact integrated result revision/tree and child dispatch handoff"
  key_links:
    - from: "catalog Offering source/schema"
      to: "Convex projection and registry Offering-v2 readback"
      via: "native exact Offering/crosswalk identity"
    - from: "cutover flags"
      to: "focused schema/migration/projection evidence"
      via: "fail-closed enablement"
---

<objective>
Freeze and integrate the complete dirty Offering predecessor lane as one exact,
coherent revision. This is the mandatory Gate 0 for D-01, D-05, D-06 and D-10.

Purpose: prevent downstream executors from inventing interfaces or absorbing an
uncommitted mixed-custody source lane.
Output: one clean exact Offering-v2 predecessor revision/tree and custody packet.
</objective>

<execution_context>
@/Users/joelchan/.codex/gsd-core/workflows/execute-plan.md
@/Users/joelchan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PRODUCT.md
@.planning/adr/ADR-026-one-business-supply-graph.md
@.planning/phases/05-consumer-operating-proof/05-CONTEXT.md
@.planning/phases/05-consumer-operating-proof/05-RESEARCH.md
@.planning/phases/05-consumer-operating-proof/05-PATTERNS.md
@convex/_generated/ai/guidelines.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Accept exact Offering custody and integration base</name>
  <files>parent-issued custody packet; no repository path may change in this task</files>
  <action>The parent integrator must provide: exact base commit and tree; named custody owner; literal allowlist containing every untracked/modified Offering owner plus schema, Convex, generated API, registry/discovery, public/operator route/component and focused-test edge; excluded unrelated dirty paths; intended result branch/worktree; and a literal list of every pre-existing typecheck failure with owning lane/person and first diagnostic. The parent must choose one of two executable dispositions: integrate a typecheck-green predecessor now, or declare those inherited owners so Gate 0 uses focused Offering commands while `npm run typecheck` remains diagnostic until the mandatory clean 05-08 integration gate. The shared checkout observed during planning at HEAD `1bbc52a4dd1133526285af87fc9143547a3a2581` / tree `a652293c48064635e6c0ca95b9628c4da320d2ce` is dirty and is not an executable base. Do not dispatch a child from it or infer that its untracked files share custody.</action>
  <verify><automated>git diff --quiet && git diff --cached --quiet && test -z "$(git ls-files --others --exclude-standard)" && test "$(git rev-parse HEAD)" = "$OFFERING_BASE_REVISION" && test "$(git rev-parse HEAD^{tree})" = "$OFFERING_BASE_TREE"</automated></verify>
  <done>The isolated integration worktree is clean, its exact revision/tree match the custody packet, every required predecessor edge has one owner, exclusions are explicit, and typecheck is either green or every inherited failure has a named owner and final-green deadline at 05-08.</done>
  <stop>Stop immediately if any base/tree mismatch, unowned untracked path, overlapping child ownership, broad staging requirement or missing generated/schema edge remains. Return the earliest mismatch; do not clean, reset, restore or partially commit the shared tree.</stop>
  <evidence>Read-only git/source custody evidence only; no Offering integration claim yet.</evidence>
  <handoff>Return base revision/tree, custody owner, literal allowlist/exclusions, command results, unresolved ownership, earliest blocker and next safe parent action.</handoff>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate native Offering source, cutover and fail-closed edges</name>
  <files>Only the parent-issued literal allowlist: bounded owners under `src/modules/catalog/**`; `src/modules/registry/internal/offering-api-projection.ts`; Offering-related `src/modules/registry/public.ts`; bounded Offering owners under `convex/catalog*.ts`, `convex/registry.ts`, `convex/schema.ts`, `convex/_generated/api.d.ts`; bounded Offering discovery files; bounded Offering public/operator routes/components; and their named focused tests</files>
  <behavior>
    - "RED: migration drift or source-hash mismatch refuses v2 cutover and leaves v1 authoritative."
    - "RED: synthetic `legacy-offering:*` / `legacy-access:*` identities cannot pass native durable identity guards."
    - "RED: authoring/public-projection flags cannot enable before schema, migration, projection and readback checks agree."
  </behavior>
  <action>Before any Convex edit, read `convex/_generated/ai/guidelines.md` completely and apply its installed-version validator, schema, function-registration and bounded-read rules. From the accepted exact base, integrate the complete inherited Offering parcel without redesigning it. Reconcile native Offering source/schema, stable crosswalk/source hashes, expand–migrate–contract behavior, safe public Offering-v2 projection, generated API and every declared consumer. Keep `offering_authoring_enabled` and `offering_public_projection_enabled` fail closed until their owning evidence passes. Generate `convex/_generated/api.d.ts` only after the integrated Convex source agrees; never hand-edit it. Preserve D-05/D-06 exact Offering identity and D-10 v2 source meaning. Do not absorb comparison semantics, historical-public policy, UI comparison or unrelated typecheck repairs into Gate 0.</action>
  <verify><automated>npm exec -- vitest run tests/unit/catalog/offering-*.test.ts tests/unit/registry/offering-*.test.ts tests/unit/schema/convex-schema.test.ts tests/integration/discovery-llms-offering-parity.test.ts</automated></verify>
  <diagnostic>Run `npm run typecheck` separately. If it fails only at custody-packet paths with named inherited owners, record the exact command and first failure without making it a false Gate-0 failure. Any new failure in this plan's changed paths is blocking.</diagnostic>
  <done>The allowlisted lane is coherently integrated; native/cutover identities agree, migration drift refuses, flags are evidence-gated, generated API matches source and all focused predecessor tests pass. Any unrelated typecheck failure is recorded with first path and assigned owner.</done>
  <stop>Stop at the first source-hash/crosswalk ambiguity, generated-edge disagreement, flag that must open before evidence, or need to touch an excluded path. Do not synthesize identity from names/slugs and do not weaken a failing guard.</stop>
  <predecessor>Exact base revision/tree accepted in Task 1.</predecessor>
  <evidence>Committed-source plus focused fixture/typecheck evidence only; not hosted, provider, supply-quality or customer proof.</evidence>
  <handoff>Commit only the allowlist. Return base/result revision and tree, changed paths, focused commands/results, observable safe cutover, first unrelated failure, earliest blocker and next parent integration action.</handoff>
</task>

<task type="auto">
  <name>Task 3: Freeze clean predecessor and publish child dispatch contract</name>
  <files>05-01-SUMMARY.md (execution output); no production source beyond Task 2 allowlist</files>
  <action>Verify the committed candidate from a fresh clean checkout, inspect exact HTTP Offering-v2 readback and cutover refusal fixtures, and record the single serialized handoff to 05-02. State the exact result revision/tree as the only permitted predecessor. Include the current typecheck result: either PASS or the unchanged literal inherited failure/owner list accepted in Task 1. Confirm current shared dirty-tree content was neither staged nor claimed. Do not dispatch later plans yourself.</action>
  <verify><automated>git diff --quiet && git diff --cached --quiet && test -z "$(git ls-files --others --exclude-standard)" && npm exec -- vitest run tests/unit/catalog/offering-supply.test.ts tests/unit/catalog/offering-migration.test.ts tests/unit/registry/offering-api-projection.test.ts tests/unit/registry/offering-runtime-guards.test.ts tests/integration/discovery-llms-offering-parity.test.ts</automated></verify>
  <done>One clean result commit/tree reproduces focused Offering-v2 behavior and the summary gives 05-02 the exact dispatch base plus an auditable typecheck disposition.</done>
  <stop>If the clean checkout differs, any fixture depends on uncommitted state, a new changed-path type error appears, or an inherited type error lacks a named owner, stop and return the first divergence.</stop>
  <predecessor>Task 2 committed candidate.</predecessor>
  <evidence>Clean committed-source and focused fixture evidence; claim ceiling remains an integrated predecessor mechanism.</evidence>
  <handoff>Return exact base/result revisions/trees, all changed paths, commands/results, observable Offering readback/refusal, unresolved finding, earliest blocker and next safe action: parent integrates or authorizes Wave 2 from this exact revision.</handoff>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dirty shared tree → integrated predecessor | Mixed/untracked custody must not enter an executable base implicitly. |
| legacy catalog → native Offering | Synthetic adapters must not become durable identity. |
| source/schema → generated/readback | Generated edges and flags must match integrated source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01A | Tampering | shared dirty tree | high | mitigate | exact clean base/tree, literal allowlist and fresh-checkout reproduction |
| T-05-01B | Spoofing | legacy synthetic refs | high | mitigate | source-hash crosswalk and native identity refusal tests |
| T-05-01C | Information disclosure | v2 public projection | high | mitigate | safe DTO allowlist and fail-closed flags |
| T-05-01D | Denial of service | unrelated broad-suite red | low | accept | record first unrelated failure; gate changed lane with focused checks |
</threat_model>

<verification>Gate 0 passes only from the clean exact result commit/tree; current dirty shared-tree tests cannot satisfy it.</verification>
<success_criteria>Parent receives one coherent committed Offering-v2 predecessor and can safely dispatch only 05-02 from that exact revision.</success_criteria>
<output>Create `.planning/phases/05-consumer-operating-proof/05-01-SUMMARY.md` when done.</output>
