---
phase: 01-ten-star-spine-foundation
plan: 12
type: execute
gap_closure: true
wave: 12
depends_on: ["01-11"]
files_modified:
  - convex/business.ts
  - convex/security.ts
  - convex/observability.ts
  - src/routes/admin.claims.tsx
  - src/routes/admin.audit-events.tsx
  - src/routes/admin.index-health.tsx
  - src/routes/privacy.remove-business.tsx
  - tests/integration/admin-runtime.test.ts
  - tests/integration/suppression-runtime.test.ts
  - tests/unit/security/admin-readbacks.test.ts
  - tests/unit/observability/operator-controls.test.ts
autonomous: true
requirements: [R6, R8, R10]
must_haves:
  truths:
    - "R8: Admin, suppression, dispute, removal, and operator controls enforce source-owned authority through Convex."
    - "R6: Operator/admin readbacks consume durable projection and discovery state with repair actions."
    - "R10: Admin route denial and readback can be proven with real Clerk/admin storage state once supplied."
  artifacts:
    - path: "convex/security.ts"
      provides: "Admin membership, dispute, and admin readback handlers backed by source rows"
    - path: "convex/observability.ts"
      provides: "Operator control mutation/query backed by source rows"
    - path: "tests/integration/admin-runtime.test.ts"
      provides: "Runtime authority and admin route coverage"
  key_links:
    - from: "src/routes/admin.*.tsx"
      to: "convex/security.ts"
      via: "route loaders read admin rows through source-owned membership guard"
      pattern: "readAdmin.*"
    - from: "src/routes/privacy.remove-business.tsx"
      to: "convex/security.ts"
      via: "public removal request opens durable dispute with abuse controls"
      pattern: "openRemovalDispute"
---

<objective>
Wire source-owned admin, suppression, dispute/removal, and operator controls through durable Convex functions and protected route loaders.

Purpose: close the R8/R6/R10 verifier failure where admin/operator/dispute Convex functions returned unavailable or denied without source-owned membership/readback wiring.
Output: Convex-backed admin runtime controls and tests for non-admin denial, owner_admin success, audit, suppression, and operator readback.
</objective>

<execution_context>
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/workflows/execute-plan.md
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ten-star-spine-foundation/01-SPEC.md
@.planning/phases/01-ten-star-spine-foundation/01-CONTEXT.md
@.planning/phases/01-ten-star-spine-foundation/01-VERIFICATION.md
@.planning/phases/01-ten-star-spine-foundation/01-04-admin-dispute-operator-recovery-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-10-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-11-SUMMARY.md
@convex/business.ts
@convex/security.ts
@convex/observability.ts
@src/routes/admin.claims.tsx
@src/routes/admin.audit-events.tsx
@src/routes/admin.index-health.tsx
@src/routes/privacy.remove-business.tsx
@src/modules/security/public.ts
@src/modules/observability/public.ts
</context>

## Artifacts This Phase Produces

- Convex admin/dispute/operator handlers that read/write source-owned membership, dispute, suppression, operator, audit, and readback rows.
- Admin route loaders that call protected Convex queries instead of passing `membership: undefined`.
- Integration tests proving owner_admin success, support/reviewer denial, non-admin route denial, dispute intake, suppression removal, and operator controls.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime admin and suppression tests</name>
  <read_first>
    @tests/unit/security/admin-authority.test.ts
    @tests/unit/security/admin-readbacks.test.ts
    @tests/unit/business/suppression.test.ts
    @tests/unit/security/disputes.test.ts
    @tests/unit/observability/operator-controls.test.ts
    @src/routes/admin.claims.tsx
    @src/routes/admin.index-health.tsx
  </read_first>
  <files>tests/integration/admin-runtime.test.ts, tests/integration/suppression-runtime.test.ts, tests/unit/security/admin-readbacks.test.ts, tests/unit/observability/operator-controls.test.ts</files>
  <behavior>
    - Non-admin reads return 401/403 and no private rows.
    - Active owner_admin can read claims, audit, index health, set operator controls, suppress, unsuppress, and close disputes with audit evidence.
    - Support/reviewer roles are denied for suppression, membership changes, operator controls, and dispute-close transitions with `admin.action_denied`.
    - Suppression hides page, registry, API, search, sitemap, llms, and UCP through one eligibility predicate after 01-13 runs.
  </behavior>
  <action>Add tests before implementation for the R8 verifier gap, preserving public discovery block conditions per D-16, admin surface minimums per D-17, bootstrap/admin authority rules per D-20, repair dispatch rules per D-21, cache-safe suppression per D-23, and identity/redaction rules per D-32 and D-33.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/admin-runtime.test.ts tests/integration/suppression-runtime.test.ts && npm run test:unit -- tests/unit/security/admin-readbacks.test.ts tests/unit/observability/operator-controls.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests fail on the current unavailable/denied-only Convex functions and pass only after source-owned admin wiring exists.</acceptance_criteria>
  <done>Admin, suppression, dispute, and operator runtime behavior is specified before handler edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement source-owned admin, dispute, suppression, and operator Convex handlers</name>
  <read_first>
    @convex/authz.ts
    @convex/source-state.ts
    @convex/business.ts
    @convex/security.ts
    @convex/observability.ts
    @src/modules/business/public.ts
    @src/modules/security/public.ts
    @src/modules/observability/public.ts
  </read_first>
  <files>convex/business.ts, convex/security.ts, convex/observability.ts</files>
  <behavior>
    - First owner-admin bootstrap is allowed only through explicit preauthorization when no active owner_admin exists, then all later authority comes from stored membership.
    - Grant/revoke, suppression/unsuppression, dispute open/close, and operator controls persist audited source rows with reason/evidence/correlation IDs.
    - Public removal/dispute intake rate-limits, dedupes, hashes contact data, caps evidence, and excludes private evidence from public outputs.
  </behavior>
  <action>Replace unavailable/denied-only Convex paths for admin, dispute, suppression, unsuppression, and operator controls with source-state-backed handlers. Use `convex/authz.ts` for identity and role/action checks, `convex/source-state.ts` for row reads/writes, and owning module public seams for behavior. Keep denial responses for missing membership, inactive membership, or action mismatch; do not let env/session identity bypass stored membership.</action>
  <verify>
    <automated>npm run typecheck && npm run check:convex-codegen && npm run test:integration -- tests/integration/admin-runtime.test.ts tests/integration/suppression-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>Convex admin/operator/dispute/suppression handlers mutate source rows only after source-owned authority succeeds and audit/reason/evidence is present.</acceptance_criteria>
  <done>R8 Convex boundary functions no longer report unavailable solely because generated source-state wiring is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Wire admin and removal routes to protected runtime readbacks</name>
  <read_first>
    @src/routes/admin.claims.tsx
    @src/routes/admin.audit-events.tsx
    @src/routes/admin.index-health.tsx
    @src/routes/privacy.remove-business.tsx
    @convex/security.ts
    @convex/observability.ts
  </read_first>
  <files>src/routes/admin.claims.tsx, src/routes/admin.audit-events.tsx, src/routes/admin.index-health.tsx, src/routes/privacy.remove-business.tsx</files>
  <action>Update admin route loaders/actions to call protected Convex queries/mutations for claims, audit, index health, disputes, and operator controls. Keep route guards as UX only; server/Convex denial remains enforcement. `/privacy/remove-business` must submit durable dispute intake with CSRF/same-site controls and preserve form/error UX from 01-08.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/admin-runtime.test.ts && VITE_AE_DISABLE_CLERK_FOR_LOCAL_E2E=true npm run test:e2e -- tests/e2e/public-owner-ui.spec.ts && npm run test:copy && npm run test:ui-contract</automated>
  </verify>
  <acceptance_criteria>Admin routes deny non-admins through server/Convex enforcement and expose source-owned readback rows only to active authorized admins.</acceptance_criteria>
  <done>The verifier can no longer identify admin/operator/dispute routes as `membership: undefined` shells.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | - | Operator can suppress or repair public exposure safely | 01-12 | COVERED | Admin/operator runtime controls. |
| REQ | R6 | Repair/readback is durable and operator-visible | 01-12 | COVERED | Admin index health reads durable attempts. |
| REQ | R8 | Source-owned admin, suppression, dispute, removal, operator controls | 01-12 | COVERED | Runtime Convex handlers and routes. |
| REQ | R10 | Admin/deploy proof can be read back once storage states exist | 01-12 | COVERED | Protected route behavior prepares deploy smoke. |
| RESEARCH | - | Public discovery blocked until admin/repair exists | 01-12 | COVERED | D-16/D-17 implemented before 01-13 surface rewrite. |
| CONTEXT | D-16-D-23,D-32,D-33 | Admin/repair/suppression/audit rules | 01-12 | COVERED | Cited in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Clerk session -> admin action | User identity becomes admin power only through source-owned membership rows. |
| Public removal form -> dispute source state | Public-write data crosses into private dispute records. |
| Admin route -> private readback rows | Protected route can expose private operational readbacks to authorized admin only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12-01 | Elevation of Privilege | admin handlers | high | mitigate | Enforce source-owned membership and role/action matrix in Convex handlers. |
| T-01-12-02 | Tampering | suppression/operator controls | high | mitigate | Require reason/evidence/CSRF/operation key and audit before state changes. |
| T-01-12-03 | Information Disclosure | admin routes | high | mitigate | Denied admin reads return no private rows; authorized reads redact payloads. |
| T-01-12-04 | Denial of Service | removal intake | medium | mitigate | Rate-limit, dedupe, evidence caps, and contact hashing. |
| T-01-12-SC | Tampering | npm installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run typecheck, Convex codegen, admin/suppression integration tests, local e2e, copy, and UI contract gates.
</verification>

<success_criteria>
R8 is closed at runtime: admin/operator/dispute/suppression controls use source-owned membership and durable state, while non-admins remain denied without private rows.
</success_criteria>

<output>
Create `.planning/phases/01-ten-star-spine-foundation/01-12-SUMMARY.md` when done.
</output>
