---
phase: 01-ten-star-spine-foundation
plan: 14
type: execute
gap_closure: true
wave: 14
depends_on: ["01-13"]
files_modified:
  - convex/discovery.ts
  - src/modules/discovery/internal/source-state.ts
  - src/modules/discovery/public.ts
  - src/routes/$slug.ucp.ts
  - src/routes/llms[.]txt.ts
  - src/routes/sitemap[.]xml.ts
  - tests/integration/discovery-routes.test.ts
  - tests/integration/discovery-route-parity.test.ts
  - tests/seo/discovery-files.test.ts
autonomous: true
requirements: [R6, R7, R8, R10]
must_haves:
  truths:
    - "R6: Discovery projection/readback consumes durable attempts and repair state."
    - "R7: UCP, llms, sitemap, and robots are generated from eligible durable source rows and are suppression-aware."
    - "R8: Suppression removes the same persisted catalog from public page, registry/API dependencies, UCP, llms, and sitemap through one eligibility predicate."
    - "R10: Deploy smoke discovery surfaces have a real business slug backed by durable state, not default route data."
  artifacts:
    - path: "convex/discovery.ts"
      provides: "Discovery health/regeneration queries backed by durable source state"
    - path: "tests/integration/discovery-route-parity.test.ts"
      provides: "Cross-surface URL and suppression parity coverage after registry/API wiring"
    - path: "tests/seo/discovery-files.test.ts"
      provides: "SEO/discovery file coverage for durable eligible source rows"
  key_links:
    - from: "src/routes/$slug.ucp.ts"
      to: "convex/discovery.ts"
      via: "UCP route builds from durable eligible catalog state"
      pattern: "buildCatalogDiscoveryManifest|readDiscovery"
    - from: "src/routes/llms[.]txt.ts and src/routes/sitemap[.]xml.ts"
      to: "convex/discovery.ts"
      via: "discovery file routes build from durable eligible catalog state"
      pattern: "buildLlmsTxt|buildSitemapXml"
---

<objective>
Replace default in-process discovery factories in UCP, llms, sitemap, and discovery readback routes with durable published/suppression-aware catalog state.

Purpose: close the R6/R7/R8/R10 verifier failures where discovery outputs rebuilt default Sam state or could not prove suppression parity after registry/API wiring.
Output: durable Convex discovery queries, discovery route rewiring, and cross-surface suppression parity tests.
</objective>

<execution_context>
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/workflows/execute-plan.md
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ten-star-spine-foundation/01-SPEC.md
@.planning/phases/01-ten-star-spine-foundation/01-CONTEXT.md
@.planning/phases/01-ten-star-spine-foundation/01-VERIFICATION.md
@.planning/phases/01-ten-star-spine-foundation/01-07-discovery-llms-sitemap-robots-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-11-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-12-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-13-SUMMARY.md
@src/modules/discovery/internal/source-state.ts
@src/modules/discovery/public.ts
@src/routes/$slug.ucp.ts
@src/routes/llms[.]txt.ts
@src/routes/sitemap[.]xml.ts
@src/routes/robots[.]txt.ts
@src/lib/http/discovery-response.ts
@convex/discovery.ts
</context>

## Artifacts This Phase Produces

- Durable Convex discovery source/readback queries.
- UCP, llms, sitemap, and robots route handlers that no longer create default in-process catalog state.
- Cross-surface tests proving published, suppressed, missing, stale, and degraded states match across page, registry, API, sitemap, llms, and UCP.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable discovery and suppression parity tests</name>
  <read_first>
    @tests/integration/discovery-routes.test.ts
    @tests/integration/discovery-route-parity.test.ts
    @tests/seo/discovery-files.test.ts
    @src/routes/$slug.ucp.ts
    @src/routes/llms[.]txt.ts
    @src/routes/sitemap[.]xml.ts
  </read_first>
  <files>tests/integration/discovery-routes.test.ts, tests/integration/discovery-route-parity.test.ts, tests/seo/discovery-files.test.ts</files>
  <behavior>
    - Publish a non-Sam catalog through the durable source path and assert public page, registry/API dependencies, UCP, llms, and sitemap expose that same catalog.
    - Suppress the same catalog and assert page, registry, API, search, sitemap, llms, and UCP omit it or return the documented not-public shape.
    - Assert discovery files keep unsupported capability flags negative and exclude private owner/contact/admin/evidence fields.
  </behavior>
  <action>Add failing tests for R6/R7/R8 public discovery parity after 01-13 registry/API wiring exists. Preserve global discovery readback per D-15, public discovery block conditions per D-16, discovery availability semantics per D-22, cache-safe suppression per D-23, scanner scoping per D-31, and owner-text safety per D-34.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/discovery-routes.test.ts tests/integration/discovery-route-parity.test.ts && npm run test:seo -- tests/seo/discovery-files.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests fail on route-local default discovery factories and pass only when discovery outputs read durable eligible catalog state from 01-13.</acceptance_criteria>
  <done>The verifier can trace one persisted catalog and one suppression through public page, registry/API dependencies, UCP, llms, and sitemap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable discovery source and readback queries</name>
  <read_first>
    @convex/source-state.ts
    @convex/discovery.ts
    @src/modules/discovery/public.ts
    @src/modules/discovery/internal/source-state.ts
    @src/modules/registry/public.ts
  </read_first>
  <files>convex/discovery.ts, src/modules/discovery/internal/source-state.ts, src/modules/discovery/public.ts</files>
  <behavior>
    - Discovery source queries build UCP, llms, sitemap, and robots inputs from the same durable eligible catalog rows produced by 01-13.
    - Discovery health readbacks include latest attempt, source hash/version, repair action, repair result, stale/degraded state, and affected surfaces.
    - Suppressed or private catalogs are excluded by the same eligibility predicate used by page and registry/API outputs.
  </behavior>
  <action>Move discovery data access behind Convex-backed query helpers. Keep generator modules deterministic, but feed them durable public catalog state and durable projection/discovery attempts. Keep test fixture builders fixture-scoped only. Discovery routes must not assemble source rows or call default catalog state factories at runtime.</action>
  <verify>
    <automated>npm run typecheck && npm run check:convex-codegen && npm run test:integration -- tests/integration/discovery-routes.test.ts tests/integration/discovery-route-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>Convex discovery queries return the public discovery inputs and health readbacks expected by UCP, llms, sitemap, and discovery status builders.</acceptance_criteria>
  <done>Public discovery module functions can be called with durable source rows and no route-level default state construction.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire UCP, llms, sitemap, and robots outputs</name>
  <read_first>
    @src/routes/$slug.ucp.ts
    @src/routes/llms[.]txt.ts
    @src/routes/sitemap[.]xml.ts
    @src/routes/robots[.]txt.ts
    @src/lib/http/discovery-response.ts
  </read_first>
  <files>src/routes/$slug.ucp.ts, src/routes/llms[.]txt.ts, src/routes/sitemap[.]xml.ts</files>
  <action>Update discovery route handlers to call durable discovery queries. Preserve response headers, no-store behavior, SEO/noindex rules, explicit empty and 404 shapes, discovery degradation states, and strict public DTO subsets. Leave `/robots.txt` generated from canonical base and private-route exclusions unless durable source data is required by tests.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/discovery-routes.test.ts tests/integration/discovery-route-parity.test.ts && npm run test:seo && npm run test:copy && npm run build && ! rg -n "createDefaultDiscoverySourceState" 'src/routes/$slug.ucp.ts' 'src/routes/llms[.]txt.ts' 'src/routes/sitemap[.]xml.ts' 'src/modules/discovery/internal/source-state.ts'</automated>
  </verify>
  <acceptance_criteria>The negative `rg` assertion finds no default discovery source-state factory calls in runtime discovery routes or runtime discovery source-state code; fixture/test files may still use explicit fixture builders.</acceptance_criteria>
  <done>R7 discovery surfaces no longer rebuild default Sam state per request.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | - | Published catalog appears in AE-hosted discovery | 01-14 | COVERED | Durable catalog source feeds UCP/llms/sitemap. |
| REQ | R6 | Discovery projection/readback/repair durable | 01-14 | COVERED | Discovery health readbacks consume durable attempts. |
| REQ | R7 | AE-hosted discovery from eligible source catalog | 01-14 | COVERED | UCP/llms/sitemap route rewiring. |
| REQ | R8 | Suppression removes every public output | 01-14 | COVERED | Cross-surface suppression tests run after 01-13. |
| REQ | R10 | Deploy smoke has real discovery slug source | 01-14 | COVERED | Durable discovery route path prepares 01-15 evidence. |
| RESEARCH | - | No hand-authored manifests | 01-14 | COVERED | Generated discovery stays. |
| CONTEXT | D-15,D-16,D-22,D-23,D-30,D-31,D-34 | Discovery/suppression/route boundary rules | 01-14 | COVERED | Cited in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable catalog rows -> public text/XML/UCP | Discovery outputs must be allowlisted, suppression-aware projections. |
| Owner-authored text -> agent-facing discovery files | Text must remain inert and not alter trust/capability state. |
| Public route handlers -> cache/crawler surfaces | Suppressed/private data can leak through sitemap, llms, or UCP if eligibility is inconsistent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14-01 | Information Disclosure | discovery routes | high | mitigate | Feed outputs through public DTO/subsets and one eligibility predicate. |
| T-01-14-02 | Tampering | discovery generation | medium | mitigate | Generate from durable source hash/version/readback, not hand-authored route state. |
| T-01-14-03 | Spoofing | discovery capability flags | medium | mitigate | Keep unsupported capability flags explicit and negative; copy/output scans reject positive claims. |
| T-01-14-04 | Repudiation | discovery readback | medium | mitigate | Preserve source hash/version, latest attempt, repair action, and audit correlation. |
| T-01-14-SC | Tampering | npm installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run typecheck, Convex codegen, discovery integration tests, SEO/copy gates, build, and the runtime negative grep check showing default discovery factories are absent from runtime discovery routes.
</verification>

<success_criteria>
R7 and discovery-side R6/R8 are closed: UCP, llms, sitemap, and discovery readback consume the same durable published/suppression-aware catalog source, and cross-surface suppression parity is proven after 01-13 registry/API wiring.
</success_criteria>

<output>
Create `.planning/phases/01-ten-star-spine-foundation/01-14-SUMMARY.md` when done.
</output>
