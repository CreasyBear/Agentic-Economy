---
phase: 01-ten-star-spine-foundation
plan: 13
type: execute
gap_closure: true
wave: 13
depends_on: ["01-11", "01-12"]
files_modified:
  - convex/registry.ts
  - src/modules/registry/internal/search.ts
  - src/routes/registry.tsx
  - src/routes/api.businesses.ts
  - src/routes/api.businesses.search.ts
  - src/routes/api.businesses.$slug.ts
  - tests/integration/registry-api.test.ts
autonomous: true
requirements: [R5, R6, R8, R10]
must_haves:
  truths:
    - "R5: Registry, search, and public API read eligible published catalogs from one durable catalog source."
    - "R6: Registry projection readbacks consume durable attempts and repair state."
    - "R8: Suppression removes the same persisted catalog from registry, search, and public API outputs through one eligibility predicate."
    - "R10: Deploy smoke registry/API surfaces have a real business slug backed by durable state, not default route data."
  artifacts:
    - path: "convex/registry.ts"
      provides: "Public registry/search/detail queries backed by durable source state"
    - path: "tests/integration/registry-api.test.ts"
      provides: "Registry/search/API durable source and suppression coverage"
  key_links:
    - from: "src/routes/api.businesses*.ts"
      to: "convex/registry.ts"
      via: "route handlers call durable registry queries"
      pattern: "listPublicBusinessCatalog|searchPublicBusinessCatalog|getPublicBusinessCatalogBySlug"
---

<objective>
Replace default in-process public catalog factories in registry and public API routes with one durable published/suppression-aware catalog source.

Purpose: close the R5/R6/R8/R10 verifier failures where registry/search/API outputs rebuilt a default Sam state per request.
Output: durable Convex registry queries, registry/API route rewiring, and suppression parity tests for registry/search/API.
</objective>

<execution_context>
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/workflows/execute-plan.md
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ten-star-spine-foundation/01-SPEC.md
@.planning/phases/01-ten-star-spine-foundation/01-CONTEXT.md
@.planning/phases/01-ten-star-spine-foundation/01-VERIFICATION.md
@.planning/phases/01-ten-star-spine-foundation/01-06-registry-search-api-repair-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-07-discovery-llms-sitemap-robots-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-11-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-12-SUMMARY.md
@src/modules/registry/internal/search.ts
@src/routes/registry.tsx
@src/routes/api.businesses.ts
@src/routes/api.businesses.search.ts
@src/routes/api.businesses.$slug.ts
@convex/registry.ts
</context>

## Artifacts This Phase Produces

- Durable Convex registry/search/detail queries.
- Registry and public API handlers/loaders that no longer create default in-process catalog state.
- Registry/search/API tests proving published, suppressed, missing, stale, and degraded states come from durable source rows.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one-source registry and API regression tests</name>
  <read_first>
    @tests/integration/registry-api.test.ts
    @src/routes/api.businesses.ts
    @src/routes/api.businesses.search.ts
    @src/routes/api.businesses.$slug.ts
    @src/routes/registry.tsx
  </read_first>
  <files>tests/integration/registry-api.test.ts</files>
  <behavior>
    - Publish a non-Sam catalog through the durable source path and assert registry, search, API list, and API detail expose that same catalog.
    - Suppress the same catalog and assert registry, search, API list, and API detail omit it or return the documented not-public shape.
    - Assert public DTO shape remains strict and excludes private owner/contact/admin/evidence fields, raw source hashes, MCP/OpenAPI/API-key, callable-positive, and payment-positive claims.
  </behavior>
  <action>Add failing tests for R5/R6/R8 registry/API parity before route rewiring. Preserve catalog DTO ownership per D-10, deterministic search per D-12, privacy-safe measurement posture per D-13, public freshness boundaries per D-14, cache-safe suppression per D-23, scanner scoping per D-31, and owner-text safety per D-34.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/registry-api.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests fail on route-local default catalog factories and pass only when registry/search/API read the same durable source.</acceptance_criteria>
  <done>The verifier can trace one persisted catalog through registry/search/API and one suppression through each registry/API omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable registry queries</name>
  <read_first>
    @convex/source-state.ts
    @convex/registry.ts
    @src/modules/registry/public.ts
    @src/modules/registry/internal/search.ts
  </read_first>
  <files>convex/registry.ts, src/modules/registry/internal/search.ts</files>
  <behavior>
    - Registry list/search/detail queries build from durable source rows and one public catalog DTO/subset.
    - Registry projection health readbacks include latest attempt, source hash/version, repair action, repair result, stale/degraded state, and affected registry/API surfaces.
  </behavior>
  <action>Move registry data access behind Convex-backed query helpers. Keep deterministic in-process search as the algorithm, but feed it durable public catalog state. Keep test fixture builders fixture-scoped only. Registry and API routes must not assemble source rows or call default catalog state factories at runtime.</action>
  <verify>
    <automated>npm run typecheck && npm run check:convex-codegen && npm run test:integration -- tests/integration/registry-api.test.ts</automated>
  </verify>
  <acceptance_criteria>Convex registry queries return the same public catalog DTO/subsets and health readbacks expected by existing registry/API builders.</acceptance_criteria>
  <done>Public registry module functions can be called with durable source rows and no route-level default state construction.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire registry and public API routes</name>
  <read_first>
    @src/routes/registry.tsx
    @src/routes/api.businesses.ts
    @src/routes/api.businesses.search.ts
    @src/routes/api.businesses.$slug.ts
  </read_first>
  <files>src/routes/registry.tsx, src/routes/api.businesses.ts, src/routes/api.businesses.search.ts, src/routes/api.businesses.$slug.ts</files>
  <action>Update registry and public API route loaders/handlers to call durable registry queries. Preserve response headers, no-store behavior, CORS, SEO/noindex rules, explicit empty and 404 shapes, and strict public DTO subsets.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/registry-api.test.ts && npm run test:copy && npm run build && ! rg -n "createDefaultRegistrySourceState" 'src/routes/registry.tsx' 'src/routes/api.businesses.ts' 'src/routes/api.businesses.search.ts' 'src/routes/api.businesses.$slug.ts'</automated>
  </verify>
  <acceptance_criteria>The negative `rg` assertion finds no default registry source-state factory calls in runtime registry/API routes; fixture/test files may still use explicit fixture builders.</acceptance_criteria>
  <done>R5 registry/search/API surfaces no longer rebuild default Sam state per request.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | - | Published catalog appears in registry/search/API | 01-13 | COVERED | One durable catalog source feeds registry/API outputs. |
| REQ | R5 | Registry/search/API from one DTO | 01-13 | COVERED | Durable registry queries and route rewiring. |
| REQ | R6 | Registry projection/readback/repair durable | 01-13 | COVERED | Registry health readbacks consume durable attempts. |
| REQ | R8 | Suppression removes registry/API outputs | 01-13 | COVERED | Registry/API suppression tests. |
| REQ | R10 | Deploy smoke has real slug source for API/registry | 01-13 | COVERED | Durable registry/API route path prepares 01-15 evidence. |
| RESEARCH | - | No external search; no hand-authored manifests | 01-13 | COVERED | Deterministic search and generated discovery stay. |
| CONTEXT | D-10,D-12-D-14,D-23,D-30,D-31,D-34 | DTO/search/suppression/route boundary rules | 01-13 | COVERED | Cited in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable catalog rows -> public JSON/HTML | Registry/API outputs must be allowlisted, suppression-aware projections. |
| Public route handlers -> cache/API surfaces | Suppressed/private data can leak through registry/API caches if eligibility is inconsistent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13-01 | Information Disclosure | registry/API routes | high | mitigate | Feed outputs through public DTO/subsets and one eligibility predicate. |
| T-01-13-02 | Tampering | registry projection | medium | mitigate | Build from durable source hash/version/readback, not hand-authored route state. |
| T-01-13-03 | Spoofing | public capability flags | medium | mitigate | Keep unsupported capability flags explicit and negative; copy/output scans reject positive claims. |
| T-01-13-04 | Repudiation | projection/readback | medium | mitigate | Preserve source hash/version, latest attempt, repair action, and audit correlation. |
| T-01-13-SC | Tampering | npm installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run typecheck, Convex codegen, registry integration tests, copy gates, build, and the runtime negative grep check showing default registry factories are absent from registry/API routes.
</verification>

<success_criteria>
R5 and registry-side R6/R8 are closed: registry, search, and public API consume the same durable published/suppression-aware catalog source.
</success_criteria>

<output>
Create `.planning/phases/01-ten-star-spine-foundation/01-13-SUMMARY.md` when done.
</output>
