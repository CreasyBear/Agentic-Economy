---
phase: 01-ten-star-spine-foundation
plan: 13
type: execute
gap_closure: true
wave: 13
depends_on: ["01-11", "01-12"]
files_modified:
  - convex/registry.ts
  - convex/discovery.ts
  - src/modules/registry/internal/search.ts
  - src/modules/discovery/internal/source-state.ts
  - src/modules/discovery/public.ts
  - src/routes/registry.tsx
  - src/routes/api.businesses.ts
  - src/routes/api.businesses.search.ts
  - src/routes/api.businesses.$slug.ts
  - src/routes/$slug.ucp.ts
  - src/routes/llms[.]txt.ts
  - src/routes/sitemap[.]xml.ts
  - tests/integration/registry-api.test.ts
  - tests/integration/discovery-routes.test.ts
  - tests/integration/discovery-route-parity.test.ts
  - tests/seo/discovery-files.test.ts
autonomous: true
requirements: [R5, R6, R7, R8, R10]
must_haves:
  truths:
    - "R5: Registry, search, and public API read eligible published catalogs from one durable catalog source."
    - "R6: Registry/discovery projection readbacks consume durable attempts and repair state."
    - "R7: UCP, llms, sitemap, and robots are generated from eligible durable source rows and are suppression-aware."
    - "R8: Suppression removes the same persisted catalog from every public output through one eligibility predicate."
    - "R10: Deploy smoke surfaces have a real business slug backed by durable state, not default route data."
  artifacts:
    - path: "convex/registry.ts"
      provides: "Public registry/search/detail queries backed by durable source state"
    - path: "convex/discovery.ts"
      provides: "Discovery health/regeneration queries backed by durable source state"
    - path: "tests/integration/discovery-route-parity.test.ts"
      provides: "Cross-surface URL and suppression parity coverage"
  key_links:
    - from: "src/routes/api.businesses*.ts"
      to: "convex/registry.ts"
      via: "route handlers call durable registry queries"
      pattern: "listPublicBusinessCatalog|searchPublicBusinessCatalog|getPublicBusinessCatalogBySlug"
    - from: "src/routes/llms[.]txt.ts and src/routes/sitemap[.]xml.ts"
      to: "convex/discovery.ts"
      via: "discovery file routes build from durable eligible catalog state"
      pattern: "buildLlmsTxt|buildSitemapXml"
---

<objective>
Replace default in-process public catalog factories in registry, API, discovery, llms, sitemap, and UCP routes with one durable published/suppression-aware catalog source.

Purpose: close R5/R6/R7/R8/R10 verifier failures where public outputs rebuilt a default Sam state per request.
Output: durable Convex registry/discovery queries, route rewiring, and suppression parity tests across every public output.
</objective>

<execution_context>
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/workflows/execute-plan.md
@/Users/skchan/Jcsyc_Projects/agentic-economy/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ten-star-spine-foundation/01-SPEC.md
@.planning/phases/01-ten-star-spine-foundation/01-CONTEXT.md
@.planning/phases/01-ten-star-spine-foundation/01-VERIFICATION.md
@.planning/phases/01-ten-star-spine-foundation/01-06-registry-search-api-repair-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-07-discovery-llms-sitemap-robots-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-11-SUMMARY.md
@.planning/phases/01-ten-star-spine-foundation/01-12-SUMMARY.md
@src/modules/registry/internal/search.ts
@src/modules/discovery/internal/source-state.ts
@src/routes/registry.tsx
@src/routes/api.businesses.ts
@src/routes/api.businesses.search.ts
@src/routes/api.businesses.$slug.ts
@src/routes/$slug.ucp.ts
@src/routes/llms[.]txt.ts
@src/routes/sitemap[.]xml.ts
@convex/registry.ts
@convex/discovery.ts
</context>

## Artifacts This Phase Produces

- Durable Convex registry/search/detail queries and discovery source queries.
- Route handlers/loaders that no longer create default in-process catalog state.
- Cross-surface tests proving published, suppressed, missing, stale, and degraded states match across page, registry, API, sitemap, llms, and UCP.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one-source public catalog regression tests</name>
  <read_first>
    @tests/integration/registry-api.test.ts
    @tests/integration/discovery-routes.test.ts
    @tests/integration/discovery-route-parity.test.ts
    @tests/seo/discovery-files.test.ts
    @src/routes/api.businesses.ts
    @src/routes/$slug.ucp.ts
  </read_first>
  <files>tests/integration/registry-api.test.ts, tests/integration/discovery-routes.test.ts, tests/integration/discovery-route-parity.test.ts, tests/seo/discovery-files.test.ts</files>
  <behavior>
    - Publish a non-Sam catalog through the durable source path and assert registry, search, API detail, public page, UCP, llms, and sitemap expose that same catalog.
    - Suppress the same catalog and assert page, registry, API, search, sitemap, llms, and UCP omit it or return the documented not-public shape.
    - Assert public DTO shape remains strict and excludes private owner/contact/admin/evidence fields, raw source hashes, MCP/OpenAPI/API-key, callable-positive, and payment-positive claims.
  </behavior>
  <action>Add failing tests for R5/R6/R7/R8 public-surface parity before route rewiring. Preserve catalog DTO ownership per D-10, deterministic search per D-12, privacy-safe measurement posture per D-13, public freshness boundaries per D-14, global discovery readback per D-15, discovery availability semantics per D-22, cache-safe suppression per D-23, scanner scoping per D-31, and owner-text safety per D-34.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/registry-api.test.ts tests/integration/discovery-routes.test.ts tests/integration/discovery-route-parity.test.ts && npm run test:seo -- tests/seo/discovery-files.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests fail on route-local default catalog factories and pass only when every public output reads the same durable source.</acceptance_criteria>
  <done>The verifier can trace one persisted catalog through every public output and one suppression through every public omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable registry and discovery queries</name>
  <read_first>
    @convex/source-state.ts
    @convex/registry.ts
    @convex/discovery.ts
    @src/modules/registry/public.ts
    @src/modules/registry/internal/search.ts
    @src/modules/discovery/public.ts
    @src/modules/discovery/internal/source-state.ts
  </read_first>
  <files>convex/registry.ts, convex/discovery.ts, src/modules/registry/internal/search.ts, src/modules/discovery/internal/source-state.ts, src/modules/discovery/public.ts</files>
  <behavior>
    - Registry list/search/detail queries build from durable source rows and one public catalog DTO/subset.
    - Discovery source queries build UCP, llms, sitemap, and robots inputs from the same durable eligible catalog rows.
    - Projection and discovery health readbacks include latest attempt, source hash/version, repair action, repair result, stale/degraded state, and affected surfaces.
  </behavior>
  <action>Move registry and discovery data access behind Convex-backed query helpers. Keep deterministic in-process search as the algorithm, but feed it durable public catalog state. Keep test fixture builders fixture-scoped only. Registry and discovery routes must not assemble source rows or call default catalog state factories at runtime.</action>
  <verify>
    <automated>npm run typecheck && npm run check:convex-codegen && npm run test:integration -- tests/integration/registry-api.test.ts tests/integration/discovery-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>Convex registry/discovery queries return the same public catalog DTO/subsets and health readbacks expected by existing route/API/discovery builders.</acceptance_criteria>
  <done>Public registry/discovery module functions can be called with durable source rows and no route-level default state construction.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire public API, registry, UCP, llms, and sitemap routes</name>
  <read_first>
    @src/routes/registry.tsx
    @src/routes/api.businesses.ts
    @src/routes/api.businesses.search.ts
    @src/routes/api.businesses.$slug.ts
    @src/routes/$slug.ucp.ts
    @src/routes/llms[.]txt.ts
    @src/routes/sitemap[.]xml.ts
    @src/routes/robots[.]txt.ts
    @src/lib/http/discovery-response.ts
  </read_first>
  <files>src/routes/registry.tsx, src/routes/api.businesses.ts, src/routes/api.businesses.search.ts, src/routes/api.businesses.$slug.ts, src/routes/$slug.ucp.ts, src/routes/llms[.]txt.ts, src/routes/sitemap[.]xml.ts</files>
  <action>Update public route loaders/handlers to call durable registry/discovery queries. Preserve response headers, no-store behavior, CORS, SEO/noindex rules, explicit empty and 404 shapes, and strict public DTO subsets. Leave `/robots.txt` generated from canonical base and private-route exclusions unless durable source data is required by tests.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/registry-api.test.ts tests/integration/discovery-routes.test.ts tests/integration/discovery-route-parity.test.ts && npm run test:seo && npm run test:copy && npm run build && rg -n "createDefaultRegistrySourceState|createDefaultDiscoverySourceState" src/routes src/modules/discovery/internal/source-state.ts</automated>
  </verify>
  <acceptance_criteria>The `rg` command prints no matches in runtime routes or discovery runtime source-state code; fixture/test files may still use explicit fixture builders.</acceptance_criteria>
  <done>R5/R6/R7 public surfaces no longer rebuild default Sam state per request.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | - | Published catalog appears in registry/search/API/discovery | 01-13 | COVERED | One durable catalog source feeds every public output. |
| REQ | R5 | Registry/search/API from one DTO | 01-13 | COVERED | Durable registry queries and route rewiring. |
| REQ | R6 | Projection/readback/repair durable | 01-13 | COVERED | Health readbacks consume durable attempts. |
| REQ | R7 | AE-hosted discovery from eligible source catalog | 01-13 | COVERED | UCP/llms/sitemap route rewiring. |
| REQ | R8 | Suppression removes every public output | 01-13 | COVERED | Cross-surface suppression tests. |
| REQ | R10 | Deploy smoke has real slug source | 01-13 | COVERED | Durable public route path prepares 01-14 evidence. |
| RESEARCH | - | No external search; no hand-authored manifests | 01-13 | COVERED | Deterministic search and generated discovery stay. |
| CONTEXT | D-10,D-12-D-15,D-22,D-23,D-30,D-31,D-34 | DTO/search/discovery/suppression/route boundary rules | 01-13 | COVERED | Cited in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable catalog rows -> public JSON/HTML/text/XML | Public outputs must be allowlisted, suppression-aware projections. |
| Owner-authored text -> agent-facing discovery files | Text must remain inert and not alter trust/capability state. |
| Public route handlers -> cache/crawler surfaces | Suppressed/private data can leak through sitemap, llms, or UCP if eligibility is inconsistent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13-01 | Information Disclosure | public API/discovery routes | high | mitigate | Feed outputs through public DTO/subsets and one eligibility predicate. |
| T-01-13-02 | Tampering | discovery generation | medium | mitigate | Generate from durable source hash/version/readback, not hand-authored route state. |
| T-01-13-03 | Spoofing | discovery capability flags | medium | mitigate | Keep unsupported capability flags explicit and negative; copy/output scans reject positive claims. |
| T-01-13-04 | Repudiation | projection/readback | medium | mitigate | Preserve source hash/version, latest attempt, repair action, and audit correlation. |
| T-01-13-SC | Tampering | npm installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run typecheck, Convex codegen, registry/discovery integration tests, SEO/copy gates, build, and the runtime grep check showing default factories are absent from routes.
</verification>

<success_criteria>
R5/R6/R7 and suppression-related R8 are closed: page, registry, API, UCP, llms, sitemap, and discovery readback all consume the same durable published/suppression-aware catalog source.
</success_criteria>

<output>
Create `.planning/phases/01-ten-star-spine-foundation/01-13-SUMMARY.md` when done.
</output>
