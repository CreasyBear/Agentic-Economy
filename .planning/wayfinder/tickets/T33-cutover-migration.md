# T33 — Cutover: enginePlans→tree, expiry split, `/` swap

Labels: `wayfinder:grilling` (HITL). Map: [Framework](../MAP-framework.md). Blocked by: [T26](T26-node-contract-and-rollup-algebra.md), [T28](T28-gardener-verbs-contract.md).

## Question

The migration decision set: widen-migrate-narrow from 15-minute enginePlans to project-lifetime trees
(PLAN_EXPIRY fences, 128-event journal, thread-scoped reads — per PremortemEngLibs); what survives of
the current plan card/dialog during transition; when `/` swaps from the one-shot engine to the
framework behind which flag; rollback posture; and what retires (which files/contracts are deleted,
clean cutover, no shims).

## Resolution

(pending)
