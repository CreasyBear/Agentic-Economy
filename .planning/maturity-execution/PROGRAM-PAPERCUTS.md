# AE maturity execution papercut registry

Papercuts are small product, code, evidence, tool or process defects that create
repeated friction, hide risk or make correct execution unnecessarily difficult.
They are not allowed to disappear into chat history.

Statuses: `OPEN`, `MITIGATED`, `REPAIRED`, `ACCEPTED_LIMITATION`.

| ID | Category | Status | Papercut and evidence | Consequence | Owner / owning gate | Required follow-up |
|---|---|---|---|---|---|---|
| AE-PAP-001 | process | MITIGATED | Some GSD workflows assume `.planning/ROADMAP.md` and phase summaries, while this program intentionally uses the frozen maturity tree. | Agents can waste time replanning or fail on missing unrelated artifacts. | Program manager / every launch | Dispatch typed executor/verifier roles directly; prohibit roadmap/discovery workflows in launch prompts. |
| AE-PAP-002 | tool | OPEN | Gate checkers can prove command/regex agreement without proving the semantic trust boundary. Phase 1 mechanical gates initially missed forged succession and reset receipts. | False confidence in security-sensitive completion. | Every phase reviewer / root G1/G7 | Pair every mechanical gate with hostile semantic counterexamples and an independent diff/trust-boundary review. |
| AE-PAP-003 | code | REPAIRED | Phase 1 release imports could pass because ignored `packages/cli/dist` artifacts existed locally. | Non-hermetic release evidence; clean checkout failure. | Phase 1 / release gate | Hermetic repair landed and passed from a fresh clone with artifacts absent; preserve the clean-checkout probe in later phases. |
| AE-PAP-004 | evidence | MITIGATED | Hosted credentials, vendor access and elapsed-time proof were being treated as source blockers. | Safe source work stalled on non-source work. | Program manager / later owning gates | Use `SOURCE_ACCEPTED_EVIDENCE_OPEN`; track exact open proof without weakening root completion. |
| AE-PAP-005 | process | MITIGATED | Phase implementation and acceptance previously risked sharing context or self-certifying. | Reviewer inherits assumptions and misses semantic defects. | Program manager / every phase | New task for every acceptance attempt; reviewer never silently repairs reviewed source. |
| AE-PAP-006 | process | MITIGATED | Phase handoffs can be ambiguous about implementation source versus evidence-only commits. | Successor may start from the wrong revision or omit acceptance artifacts. | Program manager / every handoff | Report accepted source and final evidence handoff separately; launch from the explicit final handoff and retain ancestry proof. |
| AE-PAP-007 | tool | OPEN | Desktop task creation can return a queued client ID before a usable task ID and rejects subtly malformed target shapes with only `invalid arguments`. | Dispatch becomes brittle and easy to duplicate. | Program manager / task dispatch | Stage creation, resolve the created task through the task list, then send the full contract once; record exact task ID/worktree. |
| AE-PAP-008 | process | OPEN | A new task may begin before a long execution contract is delivered. In Phase 2, the staged goal/hold prompt caused an immediate false `blocked pending contract` close before the follow-up contract arrived. | Early edits can precede constraints, or the task can consume a false blocker/restart cycle. | Program manager / task dispatch | Prefer one complete creation prompt when accepted by the task service; if staged delivery is necessary, immediately send the contract and verify the task goal is active before leaf dispatch. |
| AE-PAP-009 | tool | OPEN | Ox/provider review can be disrupted by rate limits or safety-filter behavior unrelated to the source verdict. | Acceptance may stall or be mistaken for a product failure. | Acceptance reviewer / every acceptance gate | Preserve prompts and partial output, retry boundedly, independently verify all claims, and classify provider failure as evidence/tool friction rather than source failure. |
| AE-PAP-010 | architecture | OPEN | Authorization and identity assumptions span far more files and surfaces than a local helper suggests. Phase 2’s parser-confirmed baseline measured 298 Convex declarations across 52 production files—119 public, 172 internal and seven HTTP—in addition to legacy identity, environment, connection, cron, callback and reconciliation touchpoints. | Partial migration can leave bypasses on background or adapter surfaces; treating this as a checker repair hides a material authority-entry architecture change. | Phase 2 / P2-02 and phase gate | Freeze documented middleware/custom-function seams per runtime family, create a new Unlazy repair branch with disjoint migration leaves, and require an explicit protected/exempt disposition for all 298 declarations plus reconciliation with the runtime-surface manifest. |
| AE-PAP-011 | evidence | OPEN | Existing checked gate lines can be stale baseline evidence before the phase implementation begins (for example Phase 2 integration G3 was already checked). | A child phase could inherit a green mark without proving its changed source. | Phase 2 reviewer, then all phases / phase gates | Treat pre-existing checkmarks as historical baseline only; replace/remeasure evidence against the exact candidate ref and flag stale gate state during acceptance. |
| AE-PAP-012 | process | MITIGATED | Completed tasks, review worktrees, scratch output and side-chat artifacts can outlive their useful evidence. | Worktree/task sprawl obscures the active source, consumes space and encourages stale evidence reuse. | Program manager / every phase close | Mandatory housekeeping ledger; archive tasks and remove only clean reachable worktrees after evidence preservation; retain branches until downstream integration. |
| AE-PAP-013 | tool | OPEN | Installing a missing project-local Convex skill can refresh tracked AGENTS/guideline/state files as a side effect. Phase 2 preflight produced three guidance-file edits before source implementation. | Tool setup can contaminate a phase diff and obscure product changes. | Phase 2 driver and later launchers / housekeeping gate | Inventory installer side effects immediately; retain only required reviewed guidance changes, otherwise remove installer-only churn safely before the candidate handoff. Prefer already installed skills when available. |
| AE-PAP-014 | process | MITIGATED | Phase 1 and Phase 2 allowed reviewers to discover domain boundaries, trust-chain assumptions and proof semantics after substantial implementation. | Repeated fault-finding and repair loops replace deliberate architecture work, increase churn and make each fix expose the next hidden assumption. | Program manager / Phase 3 design gate and later consequential phases | Require a planning-only architecture task, Design It Twice alternatives, full engineering review and a fresh Ox Alpha design-acceptance task before implementation. Material design changes invalidate acceptance. |
| AE-PAP-015 | evidence | OPEN | Phase 2’s isolation matrix could show that a labelled surface reached an authorization sink without proving that every actual effectful path was dominated by that sink; projecting 27 sink evaluations across 207 surfaces overstated runtime proof. | A mechanically green matrix can coexist with a bypass before, after or around the canonical boundary. | Phase 2 driver / P2-02, Phase 2 gate and every later runtime-surface gate | Freeze a handler-to-effect control-flow proof model before further repair. Bind every registered handler to actual effect paths, prove dominance or identical composition, and keep unprovable rows red. Prohibit synthetic parallel evaluators as runtime evidence. |
| AE-PAP-016 | process | MITIGATED | Architecture work can become plausible model-generated invention when it is not anchored to existing source, official framework documentation, primary standards and mature first-party examples. | Reviewers debate an invented shape after code exists, creating avoidable redesign and repair loops. | Program manager / every pre-code design gate | Require named architecture/research/framework skills, primary-source evidence packs and a source-to-decision matrix. Treat every unsupported decision as a first-principles exception requiring an ADR and fresh adversarial acceptance. |
| AE-PAP-017 | architecture | MITIGATED | The first Phase 2 runtime-dominance redesign proposed a custom context-sensitive interprocedural TypeScript control-flow engine before evaluating maintained analyzers and framework-native runtime proof. | AE would inherit a complex, security-critical analysis platform whose own soundness and maintenance become new maturity risks. | Phase 2 driver / P2-02 and every proof-tool design gate | Reject the custom engine. Use the research skill to compare official CodeQL, Semgrep, ESLint, TypeScript and Convex runtime mechanisms; select the smallest established combination and limit AE code to bounded queries/adapters and hostile tests. |
| AE-PAP-018 | tool | OPEN | Phase 2 research selected CodeQL conditionally, but the public repository has no OSI license and the official CodeQL CLI terms restrict analysis of non-OSI codebases unless a paid GitHub security license applies. | A seemingly mature verification choice can become unauthorized tooling or an unresolved source gate; public visibility alone does not establish use rights. | Phase 2 driver / proof-mechanism design gate | Do not run or require CodeQL until a paid entitlement is positively verified. Resolve the gate using documented framework middleware/registration seams plus eligible maintained analyzers and actual runtime tests; changing AE’s license or buying a tool is outside Phase 2 scope. |
| AE-PAP-019 | evidence | MITIGATED | Simple builder-pattern scans reported 203 and then 208 Convex registrations, but omitted 90 generic forms and other declarations. The parser-confirmed baseline is 298 across 52 files. | Regex counts can materially understate a migration blast radius and falsely support completeness claims even after an apparent corrective recount. | Phase 2 driver and later inventory owners / numeric and migration gates | Generate inventories from syntax/symbol-aware tooling, retain public/internal/HTTP totals, reconcile them against runtime-surface manifests, and hostile-test generic, typed, aliased and factory registration forms. |
| AE-PAP-020 | tool | OPEN | The installed Clerk router points TanStack Start and E2E work to missing `clerk-tanstack-patterns` and `clerk-testing` skills. The existing local E2E mode disables production Clerk handling, and a clean built-runtime probe found the generated Vercel handler fails before dispatch on an unresolved Clerk symbol. Official positive multi-identity Clerk testing requires a development instance and secrets. | A real source/build defect can be confused with unavailable hosted evidence, while the fixed-session harness cannot prove multi-account production-middleware composition. | Phase 2 driver / Start authority-entry migration gate; positive Clerk token chain remains later hosted evidence | Fix the unresolved-symbol source defect and prove credential-free production composition/negative paths using current official docs and installed types. Track the true Clerk-issued multi-identity chain separately against an exact candidate/deployed revision and later hosted gate. |
| AE-PAP-021 | architecture | MITIGATED | The first Phase 3 kernel draft exposed `store.transact(...)`/`transactAccept(...)` ports even though Convex mutations are already the serializable transaction boundary and provide no nested application transaction API. | A hypothetical seam can imply atomicity that the production adapter cannot implement and push tests onto a fake transaction model. | Phase 3 architecture driver / kernel and test design gates | Keep the registered mutation and actual `MutationCtx` as the sole transaction boundary; use plain bounded helpers/read-write operations, remove one-adapter ports, and prove OCC/atomicity through real Convex behavior. |
| AE-PAP-022 | architecture | OPEN | The first Phase 3 kernel draft promised idempotent Operation/Offer authoring but defined neither an Account-scoped idempotency key nor an exact natural compare-and-set replay identity. | A response lost after commit could make a retry append another Operation version or Offer revision despite the documentation claiming replay safety. | Phase 3 architecture driver / catalog and test design gates | Freeze a durable authoring retry identity, exact lookup/index and same-material replay versus changed-material conflict semantics; test response loss and concurrent retries through the registered authoring sink without adding a generic idempotency cache. |
| AE-PAP-023 | product architecture | OPEN | The original maturity plan imported a conventional marketplace ladder (`Operation -> Offer -> Resolution -> Quote -> Order -> Invocation`) before proving that AE's immediate agent-call workflow gives Offer, Quote and Order independent identities or lifecycles. Whop's research demonstrates many mature commerce resources, but its Ox reviews repeatedly warn that useful platform primitives do not by themselves evidence AE demand or market-making value. | Speculative aggregates duplicate the existing Offering and Invocation models, expand every API, money, support, scale and compatibility surface, and create implementation and repair work without strengthening an L3 invariant. | Phase 3 model-reset driver / Phase 3 architecture acceptance and all downstream contract owners | Make slim commercial admission the default. Apply a documented entity-inclusion test to each proposed aggregate, scavenge Whop's invariants rather than its nouns, retain optional L4 seams for genuinely delayed acceptance or grouped fulfilment, and require an independently accepted blast-radius amendment before Phase 3 code resumes. |

## Intake rule

Every new entry must include reproducible evidence, consequence, an owner and an
owning gate. Repair safe in-scope papercuts immediately. A deferred papercut must
be copied into the next affected launch packet and may not be described as closed
until the repair or accepted limitation is evidenced.

## Review cadence

- Phase driver: review at leaf integration and internal handoff.
- Acceptance reviewer: add semantic, evidence and tooling failures discovered.
- Program manager: deduplicate after each verdict and promote repeated papercuts
  into mandatory counterexamples or release checks.
- Root close: zero unowned `OPEN` items that affect an L3 gate.
