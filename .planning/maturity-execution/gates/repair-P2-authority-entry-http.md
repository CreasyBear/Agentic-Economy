# Gates: Phase 2 authority-entry Convex HTTP

Scope: Bind the seven actual Convex HTTP actions/routes through thin canonical adapters and real router execution.

- [ ] G1: The anonymous chat route is structurally public/bounded and the six protected callback routes execute their existing signed/workload/agent authority before body-dependent consequences.
  EVIDENCE: pending

- [ ] G2: `HttpRouter.lookup` binds every exact path/method to its intended production handler and actual `fetch(path,{method,...})` drives every case; `_handler` and hand-built contexts are absent.
  EVIDENCE: pending

- [ ] G3: Missing, malformed, replayed, stale, wrong-Account and caller-shaped callback proofs fail closed with no protected effect or secret disclosure.
  EVIDENCE: pending

- [ ] G4: Valid provider-consequence, secret-lifecycle and anonymous-chat HTTP outcomes remain compatible; unknown external effects retain durable ambiguity-safe journals and no blind retry.
  EVIDENCE: pending

- [ ] G5: Focused tests, typecheck, lint, imports and exact changed-path 100% coverage pass; secret material remains callback-memory-only.
  EVIDENCE: pending

- [ ] G6: Worker edits only owned handler modules; the driver alone composes `convex/http.ts` after stable refs and reruns route checks.
  EVIDENCE: pending

- [ ] G7: Four Unlazy passes find no further correctness, hostile-case or free-polish improvement; worker explicitly stops.
  EVIDENCE: pending
