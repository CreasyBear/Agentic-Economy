/**
 * Live keyless Wikipedia execute lived on Cluster A. That catalog is evicted.
 * Facilitator-admitted operations are the product path; paid /call is parked.
 */

export async function executeWikipediaSummary(
  _input: Readonly<Record<string, unknown>> = {},
): Promise<never> {
  throw new Error('toolcall_cluster_catalog_evicted')
}
