# Archive and recovery index

Archived on 2026-09-08 for rebaseline preparation, from source commit `7265d803d`.
The [original-path and hash inventory](rebaseline-2026-09-08.json) covers **156
files**, including 72 local-only records. Original bytes and file modes were
verified by extraction before retirement. The inventory also captures the former
planning README and Package 7 handoff before their maintained versions changed.

The data archive is local, outside the shared source tree:
`~/Documents/Coding/Backups/Agentic-Economy/closeout-2026-09-08/rebaseline-preparation/`.
It contains `files.tar.gz`, `manifest.json`, `SHA256SUMS` and `RECOVERY.md`.
Extract into a separate directory, verify hashes and restore recorded file modes
before recovering selected records. Previously tracked originals also remain in
Git history at the base commit. Local-only records require the local archive.

## Vocabulary source acceptance

The source refactor was accepted at `5f313129775c91de88e6cef819800bd116e31168`,
with governance receipt `d5fb0a2209f5936eb7613d9a3a4c49a2d1d4c581`. The complete
work record, implementation plan, cold-review register and all 36 original audit
files are preserved byte-for-byte in this archive. They are historical evidence,
not a new implementation queue.

The accepted check scope retained four integration skips and the 26-finding
standards baseline. Installed commercial execution, hosted journeys, retained-data
cutover/restore and production acceptance remain separate. Current operational
limits remain in the [cutover preflight](../operations/vocabulary-cutover-preflight.md)
and [release records](../README.md).

## Wayfinder and closeout

Archived the Wayfinder procedures, skill source, installer, validation, lessons,
completed workflow records, earlier kernel Wayfinder design and housekeeping
receipts. Archival does not uninstall or modify a separately installed global
skill. These project-local procedures no longer direct new repository work.

Earlier recovery archives remain in the parent `closeout-2026-09-08/` directory.
Their full inventory and recovery explanations are preserved here as
`docs/workflow/work/WF-20260908-closeout.md` and
`docs/workflow/work/WF-20260908-closeout-archives.json` in the captured files.

The [Package 7 handoff](../designs/package-7-handoff.md) remains active outside
the old workflow directory. Package 7 implementation still requires its own
reconciliation, focused review and approval. The separate marketing task was
excluded from archival.

## Package 6 reviews

The four dated Package 6 review reports are archived as evidence. Their findings
are not automatically current failures or completed fixes. Use the [release
guide](../guides/package-6-plugin-release.md) and [existing holds](../../TODOS.md)
when revalidating native clients, authenticated journeys and hosted release.

## Planning snapshots

Archived prior codebase/design maps, candidate research, old ADRs, forensics,
local benchmark corpora and earlier planning evidence. Their dates and original
assertions are preserved. Current product/design authorities and retained research
summaries remain available; the next baseline must use current source/tests.

The adopted [Formance qualification spike](../../.planning/spikes/001-formance-ledger-package4/README.md)
remains in place with its pinned SDK provenance, scripts, evidence and dependencies.
It is an active dependency qualification record, not disposable sprint scratch.

## Onboarding runbook

`X402_SELLER_ONBOARDING.md` is archived as a historical runbook. The Provider
screen retains its in-page source-fit guidance and no longer links to the retired
document. Current admission behavior is defined by source and the returned
preview/status contract; archival does not change publication or payment rules.

## Cleanup verification

The focused Provider-screen suite passed all three tests; targeted lint and the
installed React Doctor staged scan passed with no diagnostics. Maintained document
targets and archive anchors were checked against the committed-file inventory.
The deployment registry changed only one evidence pointer; its operational facts
are unchanged. Package 7's current design digest was refreshed for link relocation,
without changing its implementation hold. SDK qualification files and the active
marketing record were verified unchanged. No hosted test, deployment, database
operation or global skill installation was performed.
