# Well 7 swarm triage — wave 1 (source and design)

**Date: 2026-09-10**

Five read-only goblins (copy/positioning, design/taste, hostility in forms and errors, transitions and route states, machine surfaces CLI/MCP/HTTP/docs) reviewed source on branch well-7/app-shell. Wave 2 (behaviour on the local stack) follows.

## Counts by persona and severity

| Goblin | P1 | P2 | P3 | Total |
|--------|----|----|----| ----- |
| G1 (Copy) | 4 | 3 | 2 | 9 |
| G2 (Design) | 2 | 2 | 3 | 7 |
| G3 (Hostility) | 1 | 2 | 3 | 6 |
| G4 (Transitions) | 3 | 8 | 14 | 25 |
| G5 (Machine) | 2 | 2 | 1 | 5 |
| **Total** | **12** | **17** | **23** | **52** |

## All findings (severity order)

| Id | Sev | Persona | Module | Where (file:line) | What the user meets | Why (reference) | Smallest mature fix | Owner module | Status |
|----|----|---------|--------|------------------|-------------------|-----------------|-------------------|--------------|--------|
| W7-001 | P1 | Developer wanting to sell Tool | copy | src/components/ae/supply/AeSupplyLanding.tsx:70,80,103,105,112,23,26,30,33 | Hero says "List a service", buttons "List a service", "Choose one service", "one service you are authorised to list" | glossary.md:15-17 defines Service as catalogue fact, not callable Tool; DESIGN.md:64 "must not invent a simpler commercial model" | Replace "service" with "Tool" throughout AeSupplyLanding copy; reserve "service" for upstream capability before it becomes a Tool | M7 (supply UX) | open |
| W7-002 | P1 | Developer wanting to sell Tool | copy | src/routes/for-providers.tsx:12 vs src/components/ae/supply/AeSupplyLanding.tsx:70 | Meta description "Publish one bounded job" but page body says "List a service" | Same feature uses three different nouns (Tool/job/service) for one object; DESIGN.md:64 "must not invent a simpler commercial model" | Pick one noun (Tool) and use in title, meta description and body | M7 (supply UX) | open |
| W7-003 | P1 | First-time visitor | navigation | src/components/ae/layout/AeAppShell.tsx:124 and src/lib/public/website-nav.ts:31 | Header shows both "For Providers" nav link and separate "Publish" button, both pointing to /for-providers | DESIGN.md:203 "Do not place duplicate actions with the same intent in one normal page state" | Drop the "Publish" header button and let "For Providers" nav item carry intent; or fold "For Providers" out of primary nav | M1 (navigation) | open |
| W7-004 | P1 | First-time visitor | layout | src/components/ae/about/AeAboutPage.tsx:46-51,72,81 | Hero renders "Connect your agent" + "Publish a Tool" buttons, then about-doors section renders identical CTAs again | DESIGN.md:203 hard constraint on duplicate actions with same intent in one normal page state violated twice for two different CTAs | Remove the hero pair or the doors-section pair; keep exactly one instance of each CTA on /about | M1 (navigation) | open |
| W7-005 | P1 | Provider unlucky reason code | hostility | src/components/ae/supply/AeSupplySourceNativeStart.tsx:525 | User meets raw snake_case reason code interpolated straight into copy: "AE could not submit this Tool ($\{reason\}). Review the source and try again." | Typed reason contract already honored in other branches; falling through to raw code breaks pattern (Stripe/GitHub never show error.code verbatim) | Extend existing publicationError switch with remaining known reasons instead of raw-code fallback; reuse pattern from other four branches | M7 (supply UX) | open |
| W7-006 | P1 | Buyer unlucky/expired listing | hostility | src/routes/tools.$toolRef.tsx:170 with src/modules/discovery/tool-inspector-model.ts:89-91 | User meets "AE reports $\{toolLabel(result.reason)\} for this exact reference" with enum-derived slug like "withdrawn by provider" | Enum slug substituted for written copy; same "raw code as message" smell as W7-005; RFC 9457 wants human detail not slug | Give toolLabel explicit copy per known reason instead of generic underscorize transform | M4 (market) | open |
| W7-007 | P1 | Agent using OAuth | state | src/routes/_operator/owner.supply.connections.new.tsx:63,139,154,249 | User hits oauth-callback, connection loads, then page does window.location.assign() four times — breaks SPA, loses scroll position, skips Router state sync, bypasses beforeLoad hooks, back button then broken | TanStack Router pattern: window.location breaks SPA contract; should use navigate({ to, replace: true }) or redirect() in beforeLoad | Remove all window.location.assign calls; return redirect({ to: connectionReturnUrl }) from loader instead of side effect in component | M7 (supply UX) | open |
| W7-008 | P1 | Agent developer | machine | src/lib/server/mcp-api.ts:58 vs tools/ae/cli.ts (no quote command); also G6, H5 | MCP instructions name ae_tool_quote; CLI has no quote verb: `node ae.js quote --help` → "Unknown command" | Tool/Quote/Call noun parity breaks between two machine surfaces (MCP spec favors names client can map 1:1 across transports); README.md:45 teaches pipeline tool.quote → tool.call | Expose `ae quote <tool-ref> --input '<json>'` as its own command (even if it folds into call logic); or state explicitly in `ae help call` and README that CLI folds quoting into call | M5 (machine) | open |
| W7-009 | P1 | Agent developer | docs | docs/workflow/simple-user-expectations.md:19 | Row 10 of Buyer/agent developer scoreboard: "Read an API reference and use an SDK — absent" | Project's own tracker admits no API reference or SDK, yet README.md and for-agents page onboard this persona with no caveat at point of entry | Add "no SDK yet, use `ae --help`/`docs/glossary.md`" pointer to README's agent quickstart and to `ae help` root output | M5 (machine) | open |
| W7-010 | P2 | First-time visitor | navigation | src/lib/public/website-nav.ts:29,30-31 and others | Same destination /market called Discover (nav), market (route title), catalog (about page), Live list (about eyebrow), Tools (fallback label) across five surfaces | Design contract "same object retains same name and state" (DESIGN.md:63) applies to destinations; first-time visitor cannot tell if Catalog/Discover/Tools/Live list are same page | Standardise on one noun (e.g. "Market" or "Tools") for nav, headings, CTAs and fallback labels | M1 (navigation) | open |
| W7-011 | P2 | First-time visitor | navigation | src/lib/public/website-nav.ts:30-31 | `{ label: 'For agents', ... }` next to `{ label: 'For Providers', ... }` | Inconsistent capitalisation of "For X" pattern sitting side by side in primary nav | Match case: either "For Agents" / "For Providers" or "For agents" / "For providers" | M1 (navigation) | open |
| W7-012 | P2 | Developer/first-time visitor | layout | src/components/ae/layout/AeAppShell.tsx:152 | Mobile drawer repeats nav list (including "For Providers") then adds separate "Publish" button below, same destination | Same hard-constraint issue as W7-003 repeated in mobile sheet | Remove standalone "Publish" button from drawer footer once W7-003 is fixed | M1 (navigation) | open |
| W7-013 | P2 | Buyer slow/expired Stripe redirect | state | src/routes/fund.$fundingSessionId.tsx:33-39 | State stays processing or awaiting_payment (webhook lag); user meets static description with no button, only path forward is manual browser refresh | No retry/"check again" affordance despite page owning fetch-on-mount; Stripe's own hosted status pages auto-poll or offer refresh | Reuse same useEffect fetch as setInterval or manual "Check again" Button (AeRouteStates retry-button pattern already exists elsewhere) | M2 (purchase flow) | open |
| W7-014 | P2 | User-opening-chat | state | src/routes/t.$threadId.tsx:1-30, src/components/ae/chat/Chat.tsx:45-130 | Route has no loader; Chat component renders on mount, then useQuery fires; user sees empty chat window, then message history populates 200ms+ later (empty-then-fill jank) | TanStack Router pattern: data-driven SSR should load in loader, not useEffect+useQuery; empty-then-fill creates perceived glitch | Add loader: `async ({ params }) => readThreadMessagesServer({ threadId })`, render ThreadSkeleton as pendingComponent | M6 (agent chat) | open |
| W7-015 | P2 | Provider viewing offerings | state | src/routes/_operator/owner.offerings.tsx:1-55 | Loader can fail (network, permission) but route defines pendingComponent only, no errorComponent; Router throws to defaultErrorComponent showing generic "Couldn't load this page" with no context | TanStack Router pattern: loaderDeps routes should pair pendingComponent with matching errorComponent; mismatches confuse about what state broke | Add errorComponent wrapped in AeOperatorPage (like OwnerOfferingsPending does), show "Tools couldn't load" with specific recovery | M7 (supply UX) | open |
| W7-016 | P2 | Buyer double-checking stale funding link | state | src/routes/fund.$fundingSessionId.tsx:30 | Transient network failure treated same as genuinely unknown/expired session; user gets "Funding link unavailable" copy with no distinction, no retry signal | Conflates "your network blipped" with "this link is dead"; Stripe distinguishes network vs terminal states | Keep null (not-found) case separate from catch-only network case; surface retry button for network case reusing same fetch effect | M2 (purchase flow) | open |
| W7-017 | P2 | User-provided anonymous chat | state | src/routes/t.new.tsx:1-20 | Route has no loader, renders Chat with threadId=null; Chat component has no loading skeleton while useQuery is pending; user sees empty layout then message history populates after data arrives | TanStack Router pattern: routes with client-only fetches should show skeleton via pendingComponent OR component must render loading UI; current: neither | Either add loader that pre-seeds initial data, or add loading skeleton to Chat component (render AePageSkeleton while results.length === 0 && isLoading) | M6 (agent chat) | open |
| W7-018 | P2 | User redirected settings | state | src/routes/_operator/owner/settings/connections.tsx etc | Each of connections, developers, payouts throws redirect({ to: '/owner/offerings' or '/for-agents' }) in beforeLoad; user navigates to /owner/settings/connections → redirect to /owner/offerings#supplier-connections → three separate redirects for settings subpages | TanStack Router pattern: single redirect level acceptable, but multiple subpage redirects should consolidate; creates flash of multiple pending states | Remove settings subpages, render as tabs/accordion in owner/offerings, or consolidate redirects into one beforeLoad gate at /owner/settings | M7 (supply UX) | open |
| W7-019 | P2 | Provider creating connection | state | src/routes/_operator/owner/supply/connections/new.tsx:1-330 | Loader specified but no errorComponent; if readConnectionAttemptServer fails (network, permission), Router throws to defaultErrorComponent then component still renders and crashes or shows broken UI | TanStack Router pattern: loaders must pair with errorComponent if they can throw | Add errorComponent: OperatorRouteError | M7 (supply UX) | open |
| W7-020 | P2 | Provider viewing shared chat | state | src/routes/s.$shareToken.tsx:1-20 | Defines errorComponent: SharedChatUnavailable only, no loader, no pendingComponent; if network is slow, Router shows default RoutePending skeleton, then Chat loads or error shows (pending and error inconsistent visually) | TanStack Router pattern: if error boundary is custom, pending should also be custom for matching tone/brand | Add pendingComponent: SharedChatPending that mirrors SharedChatUnavailable tone/layout | M6 (agent chat) | open |
| W7-021 | P2 | Agent viewing call status | state | src/routes/calls.$callRef.tsx:1-50 | Defines pendingComponent + errorComponent but no notFoundComponent; if call is deleted, loader throws 404, Router shows default RouteNotFound (plain "page not found" link) instead of "this call may have expired" context | TanStack Router pattern: detail routes should define all three: pending, error, notFound | Add notFoundComponent showing "Call #$\{callRef\} expired or does not exist. Calls archived after 90 days. Return to activity log." | M3 (activity) | open |
| W7-022 | P2 | Agent developer | machine | `node packages/cli/dist/ae.js quote --help` | Output is generic "Unknown command" / "Review the available commands" / "Next: ae help" (exit 1) | Developer who read glossary.md's "Quote" definition or README's tool.quote step has no reason to expect quote isn't a command; generic message gives zero hint that call subsumes it | Special-case retired/merged nouns in unknown-command path with pointer to actual command (unlike git's "did you mean" for near-miss) | M5 (machine) | open |
| W7-023 | P2 | Agent developer | docs | src/components/ae/agents/AeAgentDoorPage.tsx:15-45 | Entire for-agents page body is two buttons (Browse Tools, Read the skill) plus install-funnel component; no literal CLI command, MCP tool name, or curl example anywhere in source | Contradicts README's CLI-first "START HERE" flow (ae search, ae describe, ae connect); built-for-agents page gives no copy-pasteable command | Put one literal `ae search "<job>"` / `curl .../api/v1/tools/search` example directly on agent door page, matching README's START HERE block | M5 (machine) | open |
| W7-024 | P3 | First-time visitor | navigation | src/routes/tools.$toolRef.tsx:127,184 | Fallback back-button label is "Catalog" in one call and "Browse current Tools" in the other, from same marketReturnLabel helper on same route file | Two different fallback labels for same "go back to market" action reads as unplanned inconsistency not deliberate context distinction | Use one fallback label for both call sites unless two states are meaningfully different to user | M4 (market) | open |
| W7-025 | P3 | Developer | copy | src/content/brand-copy.ts AGENT_DOOR.cta vs AeAgentDoorPage.tsx:34 | Door CTA promises "Connect your agent" but landing on /for-agents first two buttons are "Browse Tools"/"Read the skill", not a connect action (connect only appears further down funnel) | CTA labels should say what happens next; "Connect your agent" sets expectation hero buttons don't fulfil immediately | Either retitle CTA ("Set up your agent") or lead page with connect funnel instead of browse/read buttons | M5 (machine) | open |
| W7-026 | P3 | Operator (any role) | layout | src/components/ae/layout/AeAppShell.tsx:177-184 and src/components/ae/layout/AeOperatorSidebar.tsx:122-141; also G8 | Every operator screen shows two AECON marks stacked in top-left within one viewport (header brand + sidebar brand) | DESIGN.md hard constraint "Do not place duplicate actions with same intent in one normal page state" applies to duplicate identity chrome | Reuse roleHomeHref/roleLabel text only in sidebar row and drop redundant mark image, or collapse app header for operator mode | M1 (navigation) | fixed in M1 |
| W7-027 | P3 | All personas | motion | src/components/ae/layout/AeAppShell.tsx:114,210 and others | Header elevation, nav-link hover and route progress bar animate on Tailwind's default duration-200/300 + generic ease-out, not token classes | DESIGN.md Motion: "Use existing 150, 220, 300ms ladder with cubic-bezier(0.22,1,0.36,1) or (0.16,1,0.3,1)" — plain duration-200/ease-out is off-ladder/off-curve | Swap to existing utilities in globals.css: duration-base ease-standard (220ms) for 200ms cases, duration-slow ease-standard for 300ms | M1 (navigation) | open |
| W7-028 | P3 | Provider | motion | src/components/ae/market/DirectoryToolCard.tsx:32 | Every card in directory grid gets translate-y hover lift, not just button ink wash DESIGN.md names | DESIGN.md Motion: "button hover is existing 260ms ink wash" is one named treatment; lift-on-hover for every card reads as decorative motion | Drop hover:-translate-y-1, keep shadow-only hover, move duration to duration-base | M4 (market) | open |
| W7-029 | P3 | Any | docs | DESIGN.md:97-110 vs src/styles/globals.css:146-163 | DESIGN.md states canvas #f4f4f4, near-black #1c1c1c, violet-blue #4a38f5, but globals.css defines chalk #e8e4da, forest-green #24543d, no #1c1c1c/#4a38f5 | DESIGN.md front-matter says chalk/forest supersedes older rules but "Surfaces, colour and elevation" section never updated — internally contradictory | Update Surfaces section's hex values to cite actual tokens (--ae-bg, --ae-brand) instead of stale literals so future work greps doc | M1 (navigation) | open |
| W7-030 | P3 | Any | docs | DESIGN.md:14-18 vs DESIGN.md:201 | Front matter: "Discovery uses cards... show useful service, Provider and price" directly followed by Hard Constraint: "Do not use generic card grids for dense market data" | Same document asserts both "use cards for discovery" and "don't use card grids for dense market" without reconciling which views count as which | Name boundary explicitly (e.g. "directory/discovery grid = cards; Tool comparison = rows") so implementers of AeX402Directory vs AeMarketPage aren't reading different halves as authoritative | M4 (market) | open |
| W7-031 | P3 | Operator (owner) | layout | src/components/ae/layout/AeRecordHeader.tsx:44 vs src/components/ae/layout/AePageHeader.tsx:21 | Operator title (font-sans text-2xl) correctly different from public (font-display text-3xl), but two headers share one AeAppShell frame with no visible rail/gutter change — title baseline may jump on tab from public to sign-in | Confirm px-gutter/md:px-related in AeOperatorPage.tsx:179 actually lines up under ae-rail public gutter | Verify real px mismatch via screenshot; no fix needed unless found | M1 (navigation) | open |
| W7-032 | P3 | User reading support | navigation | support.tsx | Multiple `<a href={SUPPORT_EMAIL}>`, `<a href={ISSUE_URL} target="_blank">` | If any internal links are `<a href>`, they cause full page reloads losing SPA state/cache/scroll | Audit all routes for `<a href="/path">` (internal) vs `<a href="http...">` (external); convert internal to `<Link>` | M1 (navigation) | open |
| W7-033 | P3 | User reading call details | navigation | src/routes/calls.$callRef.tsx line ~330 | `<a href="#receipt">View receipt</a>` hash anchor uses `<a>` instead of router scroll-to-id | Works but doesn't preserve router state/history; back button confusing | Use `<button onClick={() => element.scrollIntoView() }>` or `router.navigate({ hash: 'receipt' })` to preserve state | M3 (activity) | open |
| W7-034 | P3 | Provider viewing developers | navigation | src/routes/_operator/developers.discovery.tsx line ~N/A | `<a href={artifact.route}>` for artifact downloads; if relative path without domain, unexpected nav | Distinguish external (`<a>`) from internal (`<Link>` or `<button>` with navigate) | Verify artifact.route is absolute URL or API path, not relative ../../ link | M7 (supply UX) | open |
| W7-035 | P3 | User navigating home | state | src/routes/index.tsx:1-15 | beforeLoad throws redirect({ to: '/market', search: { window: '30d' } }); user sees flash of index pending (75ms + skeleton to 150ms) then market pending (another flash) | TanStack Router pattern: redirect chains add round-trip latency; single navigateTo cheaper | Either remove beforeLoad, detect in index component render, show market directly; or make redirect() happen during loader phase before pendingMs clock starts | M1 (navigation) | open |
| W7-036 | P3 | User navigating contact | state | src/routes/contact.tsx:1-15 and src/routes/help.tsx:1-15 | BeforeLoad throws redirect({ to: '/support' }); same flash-two-loads as W7-035 | TanStack Router pattern: beforeLoad redirect + no loader means pending skeleton shows for redirect route (wasteful) then new route pending | Remove beforeLoad redirect, render support content directly, or add loader that delegates | M1 (navigation) | open |
| W7-037 | P3 | Multiple routes | state | about, for-agents, for-providers, status, support, activity, admin.*, agent-access, owner.settings, owner.supply.$offeringRef and others | Loaders defined but no errorComponent specified; will fallback to defaultErrorComponent (generic RouteError) missing context | TanStack Router pattern: optional but improves UX; not blocker but misses context-specific error opportunity | Optional: add errorComponent to each; or accept defaults for non-critical routes | M1 (navigation) | open |
| W7-038 | P3 | User using market filters | state | src/routes/market.tsx (loaderDeps config) | defaultPreload: 'intent' means hovering category link prefetches market loader; if expensive (reads multiple catalogue endpoints), aggressive prefetch on every hover triggers loader multiple times (wasted calls) | TanStack Router pattern: prefetch on 'intent' aggressive; consider 'hover' (requires pointer enter) or 'none' for expensive routes | Measure loader cost; if > 500ms, reduce prefetch level or split loaders | M4 (market) | open |
| W7-039 | P3 | Provider using market compare | state | src/routes/tools.tsx:1-15 | beforeLoad throws redirect({ to: '/market', hash: 'tools' }); redirects to market + hash appended, creates second redirect (wasteful) | TanStack Router pattern: use route directly or consolidate with target | Remove /tools route, render as market tab/search preset (search.view: 'tools'), or consolidate beforeLoad to execute during loader | M4 (market) | open |
| W7-040 | P3 | Provider onboarding OAuth | state | src/routes/_operator/owner.supply.connections.oauth.callback.tsx line ~loader | Loader checks result.state, throws redirect({ to: '...' }) in afterLoad; works but creates micro-delay before redirect (loader runs, redirect happens, new route pending), not batched | TanStack Router pattern: use redirect() inside loader, not afterLoad, so Router batches redirect + new route into one transition | Move redirect from afterLoad to inside loader, return early | M7 (supply UX) | open |
| W7-041 | P3 | User on any route | ui chrome | src/components/ae/layout/AeRouteProgressBar.tsx:75ms delay | Progress bar shows after 75ms delay, pending skeleton after 150ms; if load is 100ms, no visible progress (bar enters at 75ms, load finishes at 100ms); if 200ms, both visible (redundant) | TanStack Router pattern: progress bar should sync with pending; 75ms + 150ms overlap wasteful | Lower progress bar delay to 30-50ms, or raise pending to 200ms so progress bar sole indicator for 75-150ms range | M1 (navigation) | open |
| W7-042 | P3 | User navigating any route | layout | src/styles/globals.css:316-321 | CSS @supports view-transition rule disables transitions for all elements except ae-app-header; ::view-transition-group(*) animation: none (instant swap, no visual continuity) | TanStack Router pattern: defaultViewTransition: true enables View Transitions API; CSS disables them (likely for old browsers or motion, unreconciled) | Audit globals.css @supports rule: add @media (prefers-reduced-motion) OR @supports (view-transition-api) guards; document why if no transitions wanted | M1 (navigation) | open |
| W7-043 | P3 | All routes | perf | src/router.tsx + globals.css | defaultViewTransition: true on every route, but CSS disables transitions; browser may prepare transition DOM (::view-transition-old, -new pseudo-elements) even though animations are `none` (wasted rendering) | TanStack Router pattern: disable at router level if not needed, or enable in CSS with @media guard | Measure: does disabling defaultViewTransition: false improve FCP/LCP? If yes, move decision to CSS guard | M1 (navigation) | open |
| W7-044 | P3 | User browsing tools | state | src/routes/tools.$toolRef.tsx vs src/routes/market.tsx | tools.$toolRef uses default pending (RoutePending, no shape) → ListSkeleton; market.tsx uses MarketSkeleton (shape='market'); two detail pages, different skeleton shapes | TanStack Router pattern: consistent pending UI across pages of same category improves perceived brand consistency | Define ToolDetailPending (similar to MarketPending), use shape='detail', add pendingComponent: ToolDetailPending | M4 (market) | open |
| W7-045 | P3 | Provider managing offerings | state | src/routes/_operator/owner/offerings.tsx vs new/.$offeringRef/settings subpages | owner.offerings.tsx has OwnerOfferingsPending (offering=\[\] loading=true) but new, .$offeringRef, settings subpages have no custom pending | TanStack Router pattern: offering-related routes should share pending UI (same shape/tone) | Add pendingComponent to owner.offerings.new, owner.supply.$offeringRef matching OwnerOfferingsPending style | M7 (supply UX) | open |
| W7-046 | P3 | Provider creating supply | state | src/routes/_operator/owner/offerings/new.tsx | Loader defined, no pendingComponent, no errorComponent; will show default RoutePending/RouteError (misses context for complex form) | TanStack Router pattern: multi-step forms should define custom pending/error to show form skeleton and form-specific recovery | Add pendingComponent (form skeleton, disabled composer), errorComponent (form-specific "save failed" retry) | M7 (supply UX) | open |
| W7-047 | P3 | Owner unlucky money-doc failure | hostility | src/routes/_operator/owner.credit.tsx:149,157 throws Error(result.code); AeOwnerCredit.tsx:160,174,221,236 `.catch(() => setDocumentAction('failed'))` | User meets generic "document could not be prepared" with no reason/correlation/retry; typed result.code is thrown then swallowed | Typed result.code refusal reason lost; same money layer gives typed reason+nextAction elsewhere (ConnectionProblemAlert) | Thread result.code/correlationRef through to documentAction state; reuse ConnectionProblemAlert/presentConnectionProblem mapping instead of single 'failed' flag | M2 (purchase flow) | open |
| W7-048 | P3 | Provider/owner typo mid-form | hostility | src/components/ae/agent-access/AeAgentAccessAuthorizeForm.tsx and AeSupplySourceNativeStart.tsx | Invalid/incomplete field (empty tool selection) → disabled submit + separate Alert/inline text, never aria-invalid/aria-describedby wired to control | WCAG 3.3.1/4.1.2 want error programmatically associated with field not just visually nearby | Add aria-invalid + aria-describedby pointing at existing error text ids (consent-expiry pattern already exists) on specific invalid control | M7 (supply UX) | open |
| W7-049 | P3 | User reading/reviewing funding link | state | src/routes/fund.$fundingSessionId.tsx (multiple fetch paths) | Two distinct error narratives conflated: "network blip" .catch() and "link actually dead" null — user has no way to tell if retry would help | Network vs terminal states not distinguished (Stripe does; RFC 9457 handles recovery guidance) | Keep null (not-found) separate from catch-only network case; surface retry button for network via same fetch effect | M2 (purchase flow) | open |
| W7-050 | P3 | Provider | machine | src/lib/server/mcp-api.ts and tools/ae/cli.ts | MCP instructions teach tool.quote → tool.call pipeline; CLI has no quote noun at all (already listed as W7-008 but G5 also flags it at different severity/detail level) | also G1 | See W7-008 for full context | M5 (machine) | open |
| W7-051 | P3 | Agent developer | machine | tools/ae/cli.ts:325-329 | Unknown-command handling intentionally "Never echo raw token: hostile/paste argv can embed secrets" so `ae help quote` and `ae help asdkjh` produce byte-identical output | Defensible security choice but legitimate near-miss (retired noun quote) indistinguishable from typo; no signal to consult glossary | Acceptable tradeoff as-is; only revisit if command-name UX complaints recur | M5 (machine) | open |
| W7-052 | P3 | Agent developer | cli | `node ae.js supply preview --help` | Cannot be invoked with any input — --input rejected as unsupported; omitting it rejected as required | Conflicting option requirements; only way to get market_supply:manage credential is interactive browser device-flow (W7-041 re-filed as W7-052) | also G3/G5 | Check if option gap fixed in cli.ts supply-preview branch; if not, wire --input option or fold into describe | M5 (machine) | open |

## Wave 2 (behaviour on the local stack)

**Date: 2026-09-10**

Four goblins walked the line on a hand-started local stack (app :3024, local Convex); the first pass was contaminated by a missing server credential env and those findings are labelled "environment" in the reports; only findings that reproduced on the verified stack count. The buyer and provider walkers observed a hard 503 on connect both before and after the reported fix, traced to missing server-side Convex credentials; that environment misconfiguration is excluded below. All other findings reproduced cleanly.

## Wave 2 counts

| Goblin | P1 | P2 | P3 | Total |
|--------|----|----|----| ----- |
| G6 (Buyer CLI) | 0 | 3 | 2 | 5 |
| G7 (Hostile HTTP) | 0 | 3 | 2 | 5 |
| G8 (Browser) | 1 | 4 | 6 | 11 |
| G9 (Provider) | 2 | 3 | 4 | 9 |
| **Total** | **3** | **13** | **14** | **30** |

## Wave 2 findings

| Id | Sev | Persona | Module | Where (file:line) | What the user meets | Why (reference) | Smallest mature fix | Owner module | Status |
|----|----|---------|--------|------------------|-------------------|-----------------|-------------------|--------------|--------|
| W7-053 | P2 | Buyer | market | `ae search "data"` / src/lib/server/api/market-tools.search.ts; also H3 | Query "data" → "No current Tools match" despite two seeded Tools titled "Chain Tx Hash" and "Chain Erc20 Transfers" with descriptions full of "data" keyword; H3 confirms via app UI: "Exa Search" not found in search despite visible in table | search index includes only curated tags/keywords, not title/description text (reference: npm search, gh search repos match description) | Include tool description/title tokens in the search index used by market_tools.search | M4 (market) | fix in progress |
| W7-054 | P3 | Buyer | machine | `ae doctor --json` before and after `local-connect` failure | Doctor byte-identical in both cases; no `lastConnectAttempt` or similar tracking field | No local record of failed connect attempt for debugging | Have ae connect/local-connect write timestamp + error code to CLI config dir that doctor can surface | M5 (machine) | open |
| W7-055 | P3 | Buyer | machine | `ae search "data"` / `ae search "zzzz"` / `ae list` / `ae describe` | search 2728ms and 1776ms vs list 889ms and describe 826ms/321ms for same catalogue (2-3x slower, no progress) | Possible extra round-trip in search vs list's single fetch | Profile whether search re-fetches full catalogue vs list's fetch pattern | M5 (machine) | open |
| W7-056 | P2 | Buyer | market | `ae describe <ref>` human and `--json` output | No environment field in output; mismatch only discovered at `ae call` time with "tool_unsupported, uses production, your agent uses sandbox" | Every catalogue Tool present (seeded x402 Tools) is production-only; sandbox-connected buyer discovers mismatch only after attempting to pay | Surface environment: production|sandbox as a field in describe output (human + --json) next to health/indicative price | M4/M5 (market + machine) | open |
| W7-057 | P2 | Developer | machine | GET /api/ready, GET /api/v1/release response bodies | /api/ready and /api/v1/release have no "schemaVersion" field; only /api/v1/catalogue-status has schemaVersion: "catalogue-status:v1" | Inconsistent envelope prevents version-check contract the way catalogue-status enables (internal convention) | Add schemaVersion literal to both /api/ready and /api/v1/release response schemas | M5 (machine) | open |
| W7-058 | P2 | Developer | machine | GET /api/ready, /api/v1/catalogue-status, /api/v1/release with Origin: https://example.com | No Access-Control-Allow-Origin header on any of the three public status probes | RFC 9110 Fetch CORS: without ACAO header, cross-origin browser fetch cannot read body even though server computed it, breaking third-party status widgets | Add permissive CORS handler (Access-Control-Allow-Origin: *) for these three explicitly-public GET routes | M5 (machine) | open |
| W7-059 | P2 | Operator | machine | POST /api/v1/market-tools/search | 60 back-to-back requests with valid body all returned 200; no 429 appeared | rate-limiter Convex component declared but not enforced/visible on public-read bucket | Lower/confirm public-read bucket threshold for market-tools/search or point probe at documented limit | M5 (machine) | open |
| W7-060 | P3 | Developer | machine | POST /api/v1/market-tools/search vs MCP ae_registry_tools_search | HTTP /api/v1/market-tools/search response has no "nextAction" field; MCP path injects one via toolSearchNextActionField() (src/lib/server/mcp-api.ts:140-145) | CLI/agent calling plain HTTP endpoint gets no "what to do next" guidance that MCP callers get for same logical action | Factor nextAction computation into shared action layer so HTTP route reuses toolSearchNextAction() too | M4/M5 (market + machine) | open |
| W7-061 | P3 | Developer | machine | GET /api/v1/registry Accept: text/html | Server ignores Accept and always returns application/json | RFC 9110 permits ignoring Accept; noted as informational only | No fix required (note only) | M5 (machine) | open |
| W7-062 | P1 | All personas | design | site-wide — logo mark, active tab underline, chart bar fills, keyboard-focus ring on nav links (1440x1100 shots) | Logo, focus ring, active tab, and chart fills all dark green (#24543D / rgb(36,84,61)) | DESIGN.md "Surfaces, colour and elevation" names violet-blue #4a38f5 as the one accent for links/selection/focus/information; the whole app themed around a green DESIGN.md never names | Swap the CSS var driving focus/selection/nav-active/logo from current green token to --ae-brand (#4a38f5) everywhere it currently reads green, or update DESIGN.md if palette intentionally changed | M1 (navigation) | open |
| W7-063 | P2 | Agent | transitions | /tools@375/768/1440 deep links | /tools returns 307 to /market?window=30d#tools, but rendered page byte-identical to / and /market (same MD5) — "Tools" tab never becomes active, Overview tab stays shown | URL hash #tools not read on mount; tab always defaults to Overview | Read #tools hash on mount and select the Tools tab instead of always defaulting to Overview | M4 (market) | open |
| W7-064 | P3 | Provider | design | /owner/offerings vs /owner/credit, /agent-access, /activity empty states (1440x1100 shots) | Tools shows first-run onboarding card; Calls shows populated stat tiles; Credit/Agents show bordered banner — conceptually same "nothing here yet" message, visually two different templates | Visual container differs for same underlying concept ("this is demo, no real backing data") | Consider shared empty-state component so Tools/Calls and Credit/Agents render with same visual weight | M7 (supply UX) | open |
| W7-065 | P2 | Provider | design | /owner/offerings, /credit, /agent-access, /activity @375 (375x812 shots) | Sidebar-toggle icon button clips page H1 — only "s" survives of "Tools", "...ding" of "Funding", only first letter visible | Systemic 375 layout bug in operator shell: toggle button overlaps page-title text | Give toggle button its own row or reserve left padding on H1 equal to toggle width + gap at 375 breakpoint | M1 (navigation) | open |
| W7-066 | P2 | Operator | design | /admin/audit-events forbidden screen (1440x1100 shot, console log) | Correct UI renders but throws 500 fetch, "cannot open requested workspace surface" error, route-match warning, React concurrent-rendering recovery pageerrors | Not a DESIGN.md rule, but real console-error trail behind a normal, frequently-hit auth state (wrong account visit) is a code smell | Short-circuit route/query before 500 round-trip when account clearly can't own workspace, so forbidden view renders without failed request or error boundary | M1 (navigation) | open |
| W7-067 | P3 | Operator | design | /admin/audit-events "You don't have access" card | Three actions: "Return to market" (filled), "Get help" (text), "Return home" (outlined) — two both just navigate away | DESIGN.md hard constraint "Do not place duplicate actions with same intent in one normal page state" | Drop "Return home" (market and home resolve to same route) or repurpose to distinct action | M1 (navigation) | open |
| W7-068 | P3 | Provider | transitions | /for-providers@375/1440 (facts JSON: h1Time 2513ms/3269ms) | Time-to-first-H1 is 2.5-3.3s vs 50-170ms on every other public page (15-60x slower) | Unknown blocker (likely heavier client-only import or unmemoized fetch) | Profile what's blocking hydration/first paint on this one route before shipping | M7 (supply UX) | open |
| W7-069 | P3 | Operator | a11y | / @1440 vs @375 focus ring on nav links (1440x1100__focus_tab5.png, 375x812__focus_tab5.png) | Desktop 2px box-shadow focus ring; mobile border-color shift with 0 blur/0 spread (visibly weaker) | Inconsistency across breakpoints | Reuse same box-shadow focus treatment across breakpoints for parity | M1 (navigation) | open |
| W7-070 | P3 | Agent | copy | 404 route (/this-page-does-not-exist-xyz) | Generic 404 path renders "No provider at this address" / "browse the catalogue" — Tool/Provider-specific language on site's catch-all | Path-based copy not generic | Use generic "Page not found" for non-tool routes, keep "No provider at this address" only for /tools/:ref-shaped 404s | M1 (navigation) | open |
| W7-071 | P3 | Operator | transitions | /owner/settings@375 (facts JSON: g8-facts-owner-rerun.json console entry) | React hydration-mismatch error on first load at 375 only: "A tree hydrated but some attributes of server rendered HTML didn't match client properties" (not reproduced at 768/1440) | Breakpoint-specific SSR/client mismatch — conditional render branching on viewport/media-query during SSR not deterministic | Find conditional render branching on viewport/media-query and make deterministic (render both states and toggle CSS, or defer to client-only with matching server fallback) | M1 (navigation) | open |
| W7-072 | P1 | Provider | console | GET /owner/offerings/new (SSR) | useClerk crash on server: "useClerk can only be used within the <ClerkProvider />" — falls back to client, non-JS client sees nothing | useClerk used outside ClerkProvider mount context | Wrap component tree so useReverification/useClerk only runs where ClerkProvider is mounted, or guard behind bypass condition used elsewhere | M7 (supply UX) | fix in progress |
| W7-073 | P2 | Provider | console | GET /owner/settings/payouts, /owner/settings/connections | Both 307-redirect into /owner/offerings#anchor (no dedicated routes) | Missing dedicated settings screens; settings pages fold into broken/empty state target | Ship dedicated settings routes or update docs/UI that imply these are real destinations | M7 (supply UX) | open |
| W7-074 | P1 | Provider | http | POST /api/v1/supply/earnings, POST /api/v1/supply/tools, POST /api/v1/supply/connections (all with valid provider credential) | All three return 503 {"kind":"UNAVAILABLE","code":"source_unavailable"} for real accountRef and bogus businessRef identically; same credential succeeds on POST /api/v1/supply/calls | Direct proof this is not credential/environment problem: supply/calls works, earnings/tools/connections do not; fresh un-onboarded provider and broken backend look identical to caller | Fix or restore Provider Tool source lookup backing /api/v1/supply/earnings|tools|connections; also map "no business yet" to 404/onboarding-needed rather than 503 | M2 (purchase flow) | fix in progress |
| W7-075 | P3 | Provider | copy | /owner/offerings and /owner/offerings/new empty-state copy | "The name agents will see after **an Tool** is verified and published." | Grammatical error (article agreement) | Fix article agreement: "a Tool" | M7 (supply UX) | open |
| W7-076 | P2 | Provider | machine | `ae supply --help` subcommands vs working routes | No `list` or `calls` subcommand exposed; POST /api/v1/supply/calls works (returns 200), but ae supply earnings/tools/connections all 503 | Working provider endpoint not exposed as CLI command; documented "see Calls and earnings" goal has no CLI path for Calls | Add ae supply calls <ref> (or document that Calls live only in console); also add calls subcommand since working API endpoint exists | M5 (machine) | fix in progress |
| W7-077 | P2 | Provider | machine | `ae search "sandbox reference" --json` | Returns {"kind":"INTERNAL","message":"/api/v1/market-tools/search returned 308","exitCode":1} | Route issues 308 redirect (confirmed via direct curl), CLI doesn't follow same-origin redirects, bare status-code string surfaces as only error detail | Make CLI HTTP client follow same-origin redirects (or fix route to not redirect), never surface bare status-code string in error | M4/M5 (market + machine) | open |
| W7-078 | P3 | Provider | http | GET /api/v1/supply/calls (no credential, no body) | Returns 405 Method Not Allowed before the expected 401, so naive REST client gets confusing method error first | API not discoverable; GET looks REST-like, auth error comes only after knowing to POST | Document required POST method next to any mention of route, or accept GET for read endpoint named "calls" | M5 (machine) | open |
| W7-079 | P3 | Provider | docs | README "Publish an x402 Tool" section | Promises "Start at `/for-providers` and follow the source preview" with no mention that /owner/offerings currently fails locally or SSR crashes | Known-broken state not documented | Note known-broken state in README/runbook (both /owner/offerings issues) or fix before advertising flow | M7 (supply UX) | open |
| W7-080 | P3 | Provider | copy | /owner/offerings/new page title | `<title>Add service | Agentic Economy</title>` while nav/breadcrumbs elsewhere say "Tool"/"Publish" | Inconsistent noun ("service" vs "Tool" used consistently across nav and README) | Rename page title to match "Tool" vocabulary used elsewhere (e.g. "Publish a Tool") | M7 (supply UX) | open |
| W7-082 | P2 | Agent developer | machine | MCP ae_registry_tools_search | Query "data" → 621ms (0 results), query "xyzzyzyxzyzzyx" → 56ms (0 results), 6x variance; time budget planning impossible | Backend search latency undocumented and query-dependent; no SLA, no cache strategy visible; suggests external API call or N+1 index lookup | Document search SLA (min/max/p99); cache empty results; mark search as async-only with polling if > threshold | M5 (machine) | open |
| W7-083 | P3 | Agent developer | machine | MCP ae_registry_tools_search (missing required arg) | Missing required argument returns `result.isError=true` with code in plaintext text field, not JSON-RPC `-32602` in error root | MCP spec schema validation (required fields) should return JSON-RPC -32602 before attempting tool call; server returns tool execution error format instead | Validate args against inputSchema before tool call; return proper `-32602 {"error": {"code": -32602, "message": "..."}}` for schema violations | M5 (machine) | open |
| W7-084 | P1 | Newcomer | chat | src/routes/t.$threadId.tsx & chat API | Chat history lost on page reload; API never returns threadId to client; no URL like `/t/{threadId}` to restore session | Anonymous chat violates WCAG + chat convention (Intercom, ChatGPT, Claude.com all persist across reload within session); newcomer loses context on refresh | Return threadId in first message response; store in URL `/t/{threadId}` or sessionStorage; hydrate history from server-side TTL session store (24h) on load; or document that anonymous=ephemeral upfront | M6 (agent chat) | open |
| W7-085 | P1 | Newcomer | chat | Chat.tsx and glossary | Chat correctly explains "Quote is a price-and-terms decision" but then states "I don't have access to tool.quote capability in this chat" — contradicts earlier "you don't need an account" | Vocabulary promises feature availability in current context then denies it; confusing branching in chat honesty (ChatGPT/Claude branch clearly: "to *fetch* a Quote you need signin; to *learn about* Quotes I can help") | Detect "what is X" glossary questions and branch: "A Quote is [definition]. To *fetch a live Quote*, you'd need to sign in. But I can *explain pricing* here. Which would help?" with explicit capability scope per action | M6 (agent chat) | open |
| W7-086 | P2 | Newcomer | chat | Chat step 3 (find blockchain tool) | Chat attempts `registry_tools_list` call; fails with `chat_tool_refused` reason code `tool_limit` (internal constraint, no explanation) | Agent promises "I can help you discover Tools" but internal tool limit error with no user-facing explanation; reference: Stripe/GitHub surface constraints as "I can help with X but there's a rate limit, try Y instead" | Catch `tool_limit` and return: "Tool discovery is temporarily limited. Please browse the catalog directly at /market or try /Find" with working link and fallback path | M6 (agent chat) | open |
| W7-087 | P1 | Newcomer | app (market) | src/routes/tools.$toolRef.tsx via /market?window=30d → click tool name → 04-tools-tab.png | Exa Search tool visible in table; search for "Exa Search" returns "No Tools matched"; tool names in table not clickable links | Search index excludes description text (W7-053 also H3); separate issue: table offers save/compare buttons but no navigate action (Stripe Dashboard, AWS Marketplace both have tool-name-as-link to detail) | Make tool names `<Link to="/market/tools/{toolRef}">` or add explicit "View details" button; verify searchability fix (W7-053) for "Exa" | M4 (market) | open |
| W7-088 | P2 | Owner | navigation | src/lib/public/website-nav.ts:30-31 & h3-shots/21-command-palette.png | Shortcut ⌘K opens "Find Tools" modal labelled as searching "the Tool catalog by job, provider, or capability"; user expects command palette (navigate to pages, run commands) like Vercel/Stripe/LinearApp | Command palettes are for navigation and actions, not tool search; mislabeled expectation; typing "credit" to jump to /owner/credit doesn't work (tool search only) | Rename "Find Tools" button copy to "Search Tools" or "Browse Catalog"; relabel as "Searches Tools catalog only—use nav to jump to pages" or add full command palette (route-finding + tool search) | M1 (navigation) | open |
| W7-089 | P2 | Newcomer | copy | src/routes/activity.tsx & h3-shots/12-calls-page.png | Empty state says "Find a capability and complete one call" with button "Discover capabilities" | "Capability" is vague (Tool? Agent? Service?); Stripe says "Find a product to sell" or "Browse plans"; copy should clarify next action | Change to "Find a Tool to call" or "Browse available Tools"; update button "Browse Tools" to match | M7 (supply UX) | open |
| W7-090 | P2 | Owner | app (nav) | src/routes/market.tsx & h3-shots/22-command-credit.png | User types "credit" in ⌘K expecting navigation to /owner/credit; search closes, no navigation happens | Modern unified search (Linear, Vercel) supports both resource search (tools, docs) and navigation (go to page X). Current ⌘K is tool-search-only | Decide: (a) keep Find Tools as tool-search-only, add separate ⌘L navigation palette; or (b) extend Find Tools to include quick routes (Credit/Agents/Help) as top results before tool matches | M4 (market) | open |
| W7-091 | P3 | Owner | transitions | src/routes/for-providers.tsx & h3-shots/11-publish-loaded.png | Navigate to /for-providers; skeleton screens appear then page renders as blank white (about:blank) | Route loads but renders nothing; likely failed data fetch or error boundary not firing; similar to W7-068 (slow load) but this is complete blank | Add error boundary to page; log network/API errors; ensure /for-providers endpoint returns HTML or redirects to /owner/offerings | M7 (supply UX) | open |
| W7-092 | P3 | Owner | layout | src/components/ae/layout/AeOperatorSidebar.tsx:122-141 | Sidebar "Tools" link under Supply context for publishing; earlier marketplace table at /market?view=tools also says "Tools" | Same label "Tools" means two different things (marketplace browse vs. provider publish); user clicking "Tools" in sidebar expects to see marketplace, not publisher form (Stripe: "APIs" vs. "Products") | Rename sidebar "Tools" to "Publish Tool" or "Provider Workspace"; or add icon/badge distinguishing from marketplace "Tools" | M7 (supply UX) | open |
| W7-093 | P1 | Developer | docs | README.md line 70-74 & missing discovery section | Documentation mentions "Current source entrances" but /.well-known/ucp (canonical API discovery manifest) not listed; only discoverable via /llms.txt comment "Deployment manifest: http://127.0.0.1:3024/.well-known/ucp" | Developers must reverse-engineer discovery from machine file (/llms.txt) instead of human docs; breaks principle that documentation is first reference | Add to README "## Discovery": "All API endpoints are listed in `/.well-known/ucp` (OpenRPC 2.0 service metadata). Start there for endpoint list, auth requirements, contract versions, and MCP info." | M5 (machine) | open |
| W7-094 | P1 | Developer | docs | agent-operating-contract.md (no mention) & /api/v1/funding/quote | POST /api/v1/funding/quote requires nested `amount: {currency, units, exponent}` structure; not documented; only discovered via 400 error message | API response and error message reveal structure but docs should specify first; financial APIs need explicit amount encoding to prevent rounding errors | Document `/api/v1/funding/quote` request schema in agent-operating-contract.md with example: `{"amount": {"currency": "AUD", "units": "5000000", "exponent": 6}}` | M5 (machine) | open |
| W7-095 | P1 | Developer | docs | README.md (missing) & docs/designs/package-7-handoff.md (workflow file) | Device flow and authentication method (Clerk API key) not described in README or main API docs; only findable in workflow closeout document | Developers cannot complete authentication step without source archaeology; tribal knowledge required | Add section to README: "## Authenticating" explaining Clerk API key as bearer token, device-flow auth via CLI `ae connect`, and pointing to /.well-known/ucp as source of truth for auth requirements | M5 (machine) | open |
| W7-096 | P2 | Developer | http | POST /api/v1/funding/quote response | Response shape conflates metadata (quoteRef, expiresAt, binding) with financial breakdown (principalAmount, serviceFee, taxOnServiceFee, totalPayment) in flat object | Unclear nesting makes it easy to misattribute fields (e.g., think quoteRef is opaque but it's actually a message); RFC 9457 problem details wants clear recovery path | Clarify response structure in docs: `{"kind": "funding_quote", "quote": {"quoteRef": "...", "binding": false}, "breakdown": {"principalAmount": {...}, "totalPayment": {...}}}` | M5 (machine) | open |
| W7-097 | P2 | Developer | docs | glossary.md:92-99 | `freshness` field defined with two independent sources (x402_directory vs supply_projection) but docs never clarify which source appears in which API response | Developers may cache results using wrong freshness source or miss staleness signals | Add table to glossary: "| Endpoint | freshness.source | SLA | | /api/v1/market-tools/search | supply_projection | 3h stale-after |" | M5 (machine) | open |
| W7-098 | P2 | Developer | docs | README.md line 70 vs /.well-known/ucp | `/api/v1/market-tools/compare` endpoint exists and is discoverable via /.well-known/ucp but not mentioned in README.md or llms.txt; developers may not know feature exists | Incomplete discovery surface; developers miss tool comparison feature (needed for pre-Quote shopping experience like Stripe) | Add `/api/v1/market-tools/compare` to README line 70 under market-tools endpoints with description: "Compare tools side-by-side before requesting quotes" | M5 (machine) | open |
| W7-099 | P3 | Developer | http | POST /api/v1/market-tools/describe (invalid input) error message | Error reveals implementation detail regex pattern `/^operation:v1:[0-9a-f]{64}$/` (internal operation naming scheme, not documented API contract) | If naming scheme changes, error message breaks developer trust; patterns should be stable API contracts, not exposed implementation details | Document `toolRef` format explicitly in README: "A toolRef is an immutable identifier matching the pattern `operation:v1:{sha256_hex}` representing the unique tool operation." | M5 (machine) | open |
| W7-100 | P3 | Developer | docs | agent-operating-contract.md:149-158 | Response `nextAction` defined with enum values `call.status|tool.quote|call.cancel|call.reconcile` and structure, but no real examples shown with all fields present; `retry: "safe|never_resubmit"` mentioned but not defined | Developers may guess at valid actions for a given Call state; state machine unclear | Include state machine table in agent-operating-contract.md: `{"pending": [{action: "call.status"}, {action: "call.cancel"}], "completed": [{action: "none"}]}` | M5 (machine) | open |
| W7-101 | P3 | Developer | http | No X-RateLimit-* headers in responses; no rate-limit docs anywhere | Developers have no way to know if approaching limit; silent truncation possible but undocumented | Document rate-limit policy (or absence thereof) in README "## Rate limits and quotas" section; if limits exist, add X-RateLimit-Limit, -Remaining, -Reset headers | M5 (machine) | open |
| W7-102 | P1 | Newcomer | cli | tools/ae/cli.ts (ae call error path) | Running `ae call "Weather Forecast" --input '{}'` returns error "Tool input or identity does not match tool.quote:v2" — cryptic reference to Quote in error but no ae quote command exists | Error message mentions Quote (which newcomer read in README/glossary) but has no command to resolve it; refers to internal spec version (tool.quote:v2) not user-facing concept | Error should say: "Tool 'Weather Forecast' not recognized. Did you mean a tool ref like `operation:v1:...`? Use `ae search <job>` first, then `ae describe <ref>` to find it." Reference W7-008 + W7-106 pattern | M5 (machine) | open |
| W7-103 | P1 | Newcomer | docs | README line 148-149 & line 165-166 (local setup) | Follow setup, start stack on 127.0.0.1:3024, try `ae call` → doctor explicitly says "quoting: skipped" (loopback limitation); newcomer discovers they can't test paid flows locally only after attempting | Loopback limitation (can't execute Calls on localhost) buried in doctor output, not upfront; newcomer wastes time trying to buy a Tool on 127.0.0.1 | Add prominent warning to README before "Run locally" section: "**Loopback testing (127.0.0.1) proves Discovery and Connect only. Quote and Call require HTTPS hosted origin. To test paid flows locally, see docs/operations/local-stripe-test-mode.md (TBD).** | M5 (machine) | open |
| W7-104 | P2 | Newcomer | cli | README line 208 vs local-connect.mjs | README says use `npm run -s ae --` but local-connect.mjs wrapper suggests bare `ae` command; which is canonical? | `npm run` wrapper works in source repo, bare `ae` assumes installed CLI; both work locally but create confusion about which is authoritative | README "Machine quickstart" should note: "When running from source in this repo: use `npm run -s ae --`. When installed globally: use `ae`. For local testing: both work identically." | M5 (machine) | open |
| W7-105 | P2 | Newcomer | docs | README line 45 vs line 202-215 | README line 45 states pipeline "registry.tools.search → tool.quote → tool.call" as if auth required for first step; but line 208 `ae search` works unauthenticated | Contradicts flow documentation; newcomer sees search fail sometimes (when trying authenticated context) and succeed other times | Add to README: "Discovery (search, list, describe) is **public and requires no auth**. Authentication is required only for Call, history, and account operations. Quote is implicit in Call (no separate ae quote command)." | M5 (machine) | open |
| W7-106 | P2 | Newcomer | cli | tools/ae/cli.ts (unknown-command handler) | Typo `ae serch` instead of `ae search` → error "Unknown command" + "Review the available commands and try again" (no fuzzy match) | Most CLIs (git, stripe, vercel) offer typo tolerance: "Did you mean 'search'?"; current error gives zero hint | Add fuzzy matching or hardcoded near-miss suggestions: "Unknown command 'serch'. Did you mean 'search'?" (reference: git's error handling) | M5 (machine) | open |
| W7-107 | P2 | Newcomer | docs | README line 76 (CLI bullet point) | "Call history" bullet suggests `ae history` is a first-class command; `ae history` works and returns results, but doesn't document that it's credential-bound (one credential's calls only) | Switching credentials hides old history; newcomer may think history was lost rather than credential-switched | README: "`ae history` returns Call records for **this credential only**. Switching credentials or revoking a token hides older history. Run `ae account status` to see current credential." | M5 (machine) | open |
| W7-108 | P2 | Newcomer | cli | tools/ae/cli.ts & README line 45 | "Quote" is central to README's tool.quote → tool.call pipeline and glossary; CLI has no way to read it before calling: no `ae quote` command, no `ae status <call-ref>` pre-execution Quote output | Newcomer cannot inspect price/terms before calling despite glossary/README promising Quote as first-class concept | Add `ae quote <tool-ref> --input '<json>'` command returning only price/terms without executing; or expose Quote in `ae status <call-ref>` output showing pre-execution terms | M5 (machine) | open |
| W7-109 | P3 | Newcomer | docs | README line 214 example | Example `ae call ... --input '{"city": "Perth"}'` doesn't show how to discover that "city" is a required field | Newcomer must guess; docs mention `ae describe <tool-ref> --technical` exists but README doesn't point to it | Update README: "For required inputs, use `ae describe <tool-ref> --technical` to see input schema. Example: the Weather tool requires `{"city": "..."}`. | M5 (machine) | open |
| W7-110 | P3 | Newcomer | cli | tools/ae/cli.ts (ae supply) | No `ae supply calls` or `ae supply earnings` subcommand exposed; working API endpoint POST /api/v1/supply/calls exists (returns 200) but CLI doesn't expose it | Glossary defines Charges/Earnings; CLI exposes balance (buyer) + activity (buyer charges) + supply earnings (provider); but "Calls" (provider activity) not in CLI | Add `ae supply calls [--ref X]` command to expose provider's past calls; document in README that provider earnings live in /owner/credit console | M5 (machine) | open |
| W7-111 | P3 | Newcomer | docs | manifest schema (manifest.ts) | `coldLoop: ["search", "describe", "call", "history", "status", "wait"]` listed in manifest but not explained | Newcomer sees array but doesn't understand what it means or why these 6 commands matter | Add to manifest help: "coldLoop: the read-only, recovery-safe journey through one Tool purchase (no side effects, safe to retry without double-charging)." | M5 (machine) | open |

## Truth finding from the fix (2026-09-10)

W7-074 was two defects. The transport-level 503 is fixed (the `readAgentProviderEarnings` validator now accepts the shared source-write and tracing fields the server always sends; a convex-test validator test guards all three provider reads). Underneath it, the earnings handler itself is a stub that always returns `{ kind: 'error', code: 'source_unavailable' }` pending the Formance earnings read (introduced in the Formance cutover, 2026-09-02). So `ae supply earnings` is honest but empty for every provider, and the Well 6 closeout's "providers see their Calls and earnings" holds only for Calls. Filed as **W7-081 [P1] provider | money | convex/moneyLedger.ts readAgentProviderEarnings | earnings always `source_unavailable` | Formance owns monetary truth (decision 2026-09-01); read provider earnings from the ledger | owner M5 | open**.

## Wave 3 (haiku goblins, tool calls only)

**Date: 2026-09-10**

Five haiku-4.5 goblins used the product through direct CLI (ae search/ae help), curl JSON-RPC for MCP (direct json-rpc with proper headers), HTTP (curl against /api/v1/* and /.well-known/ucp), real browser (gstack browse at 1440×1000 then 375×812 mobile), and API-from-docs scenarios (no code, no fixtures), on the verified local stack (127.0.0.1:3024 with seeded tools and sandbox agent). No scripts, no test harnesses. 40 findings reported across goblins; 30 unique rows added to triage after merging duplicates with existing Wave 1/2 findings.

### Wave 3 counts

| Goblin | P1 | P2 | P3 | Total |
|--------|----|----|----| ----- |
| H1 (MCP) | 0 | 1 | 1 | 2 |
| H2 (Chat) | 2 | 1 | 0 | 3 |
| H3 (App) | 1 | 3 | 2 | 6 |
| H4 (API docs) | 3 | 3 | 3 | 9 |
| H5 (CLI) | 2 | 5 | 3 | 10 |
| **Total** | **8** | **13** | **9** | **30** |

## Decisions for Joel

1. **Accent colour palette — green vs violet-blue (W7-062):** DESIGN.md names violet-blue `#4a38f5` as the one accent but the app's brand token `--ae-brand` is dark green `#24543d` and drives logo, active tab, focus ring, and charts. Is DESIGN.md stale or is the app off-spec? Swap the accent or update DESIGN.md.

2. **Environment mismatch — sandbox vs production (W7-056):** Catalogue shows production-only Tools while a fresh `ae connect` binds a sandbox agent and `describe` has no environment field, so the mismatch surfaces only at `call` time. Show environment in describe/search, or bind production by default?

3. **Chat thread persistence — memory vs server-side (W7-084, W7-085):** Anonymous chat history lost on page reload (no thread ID returned to client). Options: (a) server-side temporary threads with TTL (24h) and threadId in URL `/t/{threadId}` for bookmark/share; (b) client-side sessionStorage with explicit "save this chat" before refresh; (c) hybrid (server backend, but require sign-in to persist beyond session). Quote vocabulary clarity also needs explicit "to *invoke* Quote you need auth, but I can explain it" branching.

## Already good

- No AI-slop filler found (seamless/unlock/empower/robust/delve/journey/ecosystem/leverage) across index, market, tools, for-agents, for-providers, help, support, about, AeAppShell, AeSiteFooter, brand-copy.ts, AeSupplyLanding, AeAboutPage, AeAgentDoorPage
- Home hero ("The marketplace built for agents. Discover, compare, and call.") leads with concrete, grounded action sequence rather than philosophy — matches Joel's positioning rule
- Nav label "Calls" (website-nav.ts:32) matches /activity content and glossary "Call" term rather than inventing synonym like "Orders" or "History"
- support.tsx does not promise anything the scoreboard marks absent (no refund or notification claims), consistent with docs/workflow/simple-user-expectations.md rows 6 and 9 both being "absent"
- /help correctly redirects to /support and footer "Get help" link matches page H1 "Get help" (support.tsx) — no title/label mismatch
- src/components/ae/market/AeToolCard.tsx uses rounded-none, hairline border-b, and duration-fast/duration-base + ease-standard tokens correctly — this is DESIGN.md-compliant row primitive
- src/components/ae/market/DirectoryToolTable.tsx collapses real table to divide-y rounded-xl border list on mobile (md:hidden) instead of inventing card grid — clean single-source responsive pattern
- globals.css correctly centralizes motion ladder (--motion-duration-fast/base/slow, --motion-ease-standard/emphasized) and both prefers-reduced-motion branches are wired for view-transitions
- AeOperatorPage.tsx's chrome-registration (Tier A/B context) keeps title/description/actions single-sourced across nested routes so breadcrumbs and headers can't drift out of sync
- AeCapabilityTile.tsx correctly avoids bordered card (rounded-none border-0 border-t) even in sm:grid-cols-2 layout, keeping row-not-card discipline for dense catalog data
- src/components/ae/layout/AeRouteStates.tsx: retry buttons show pending label ("Trying again…"), disabled while in flight, surface copyable correlation reference — no bare "something went wrong"
- src/components/ae/console/AeAgentOperatorConsole.tsx: revoke credential, disconnect agent, disconnect connection all route through shared destructive ConfirmDialog before firing — no accidental one-click destructive action
- ConnectionProblemAlert/presentConnectionProblem renders typed heading + outcome + nextAction + technicalReference for every consent failure mode (approved/denied/outcome_unknown/expired/rate_limited) — real implementation of "reason + nextAction" contract
- src/routes/_operator/agent-access.tsx: lifecycle issues (work_remaining, conflict, authentication_required) each get distinct specific copy and scoped "Retry cleanup" button tied to exact failed command
- src/lib/server/problem.ts: problem() enforces non-empty detail on every RFC 9457 body and always attaches request correlation id header — backstop against empty/bare error responses
- `ae search --json` with no query validates and fails fast (query_invalid, exit 1) before touching network
- Every CLI error (JSON mode) shares stable envelope — kind/code/message/exitCode/suggestion/nextCommand — across parse errors, connection_refused, option-not-supported
- `ae --version --json` returns real provenance: version/buildRevision/executable/runtime — no gap
- option-not-supported errors return detail.allowedOptions (exact valid flag list), not scold — genuinely actionable
- `ae doctor --help` states plainly it "never connects, funds, retries, reconciles, or changes provider state", matching observed behavior

## Grouping by root cause

### G1. Noun inconsistency (Tool vs Service vs job vs Offering)
Findings: W7-001, W7-002
Module owner: M7 (supply UX)
Issue: Same object described with three different nouns across title/meta/body (for-providers page); same object called Service throughout for-providers copy when glossary defines Service as non-callable catalogue fact; supply context uses "job" in SEO meta but "Tool" in route title.
Fix: Pick one canonical noun (Tool) across supply publishing surfaces; reserve Service for upstream capability before Tool listing.

### G2. Duplicate CTAs and navigation chrome
Findings: W7-003, W7-004, W7-007, W7-026
Module owner: M1 (navigation), M7 (supply UX)
Issue: DESIGN.md:203 hard constraint "Do not place duplicate actions with same intent in one normal page state" violated in three places: header has both "For Providers" nav + "Publish" button pointing to same destination; about page renders hero CTAs then identical doors-section CTAs; operator layout renders AECON mark twice (header + sidebar); OAuth callback uses window.location.assign() four times breaking SPA.
Fix: Single action per intent per page state; route OAuth through Router navigate with redirect(), not window.location.

### G3. Market/catalog destination vocabulary collapse
Findings: W7-010
Module owner: M1 (navigation), M4 (market)
Issue: Same destination /market called Discover, market, catalog, Live list, Tools across five surfaces; visitor cannot tell if these are same page.
Fix: Standardise on one noun (Market or Tools) for nav, headings, CTAs, fallback labels; apply DESIGN.md:63 "same object retains same name and state across all three modes."

### G4. Raw reason codes and missing typed error context
Findings: W7-005, W7-006, W7-047
Module owner: M7 (supply UX), M2 (purchase flow), M4 (market)
Issue: Raw snake_case reason codes (source_changed, withdrawn_by_provider) interpolated straight into user-facing copy instead of mapped to written context; typed reason contract honored elsewhere (Stripe, GitHub, ConnectionProblemAlert) broken in three surfaces; money-doc failure reason thrown then swallowed.
Fix: Extend existing publicationError and presentConnectionProblem patterns to remaining failure modes; thread typed reasons through to state instead of .catch() swallowing them.

### G5. Client-fetch jank and missing loaders
Findings: W7-014, W7-017, W7-044
Module owner: M6 (agent chat), M4 (market)
Issue: Chat and market detail routes have no loaders; component renders on mount, useQuery fires client-side, data populates 200ms+ later (empty-then-fill jank); pending skeletons inconsistent across detail pages (ListSkeleton vs MarketSkeleton).
Fix: Add loaders that run data fetches server-side; define consistent pendingComponent shapes (ToolDetailPending matching MarketPending) per route family.

### G6. Motion tokens and design spec consistency gaps
Findings: W7-027, W7-028, W7-029, W7-030
Module owner: M1 (navigation), M4 (market)
Issue: Header/nav/progress animations use Tailwind defaults (duration-200, ease-out) instead of DESIGN.md motion ladder (150/220/300ms + cubic-bezier); DirectoryToolCard adds decorative translate-y hover lift beyond named ink-wash treatment; DESIGN.md states two conflicting color palettes (old hex vs chalk/forest tokens); DESIGN.md asserts both "use cards for discovery" and "don't use card grids for dense market" without naming which surfaces are which.
Fix: Use globals.css existing utilities (duration-base ease-standard) for all motion; update DESIGN.md Surfaces section to cite actual tokens; name explicit boundary between discovery card grid (AeX402Directory) and dense row table (AeMarketPage).

### G7. Router state and pending/error coverage gaps
Findings: W7-015, W7-018, W7-019, W7-020, W7-021, W7-035, W7-036, W7-037, W7-038, W7-039, W7-040, W7-041, W7-042, W7-043, W7-044, W7-045, W7-046
Module owner: M1 (navigation), M2 (purchase flow), M3 (activity), M6 (agent chat), M7 (supply UX)
Issue: 17 state-related findings: missing errorComponent on loaders (offerings, connections, settings routes show generic defaultErrorComponent); client-side data fetches (chat) with no skeleton; redirect chains creating flash-of-multiple-pending (home → market, contact → support, settings subpages); inconsistent pending shapes across detail routes; missing notFound component on detail pages; progress bar (75ms) + pending skeleton (150ms) timing overlap wasteful; defaultViewTransition:true but CSS disables all transitions (confusion); prefetch='intent' on expensive loaders triggers wasted calls on every filter hover; OAuth callback has micro-delay before redirect.
Fix: Pair every loader with contextual errorComponent (using OperatorRouteError for operator routes); add loaders to client-fetch routes with server-side data pre-seeding; define all three: pendingComponent/errorComponent/notFoundComponent on detail routes; consolidate redirect chains (remove beforeLoad redirects, execute during loader); lower progress-bar delay or raise pending-delay threshold; resolve view-transition CSS guard; measure expensive loaders and reduce prefetch; batch OAuth redirect into loader.

### G8. Machine surface (CLI/MCP/HTTP/docs) naming and guidance
Findings: W7-008, W7-009, W7-022, W7-023, W7-025, W7-052; also G6-F3 (quote), G7-F4 (nextAction), G7-F1 (schemaVersion), G9-F5 (no calls), G9-F6 (search redirect), G9-F7 (supply/calls 405)
Module owner: M5 (machine)
Issue: CLI has no `quote` command despite MCP/README naming it in pipeline; glossary and docs define Quote/Tool/Call but README.md lacks API reference caveat at agent quickstart entry point; unknown-command handler gives no hint that retired verbs (quote) were merged into call (like git's "did you mean"); for-agents page has no copy-pasteable CLI example despite being landing for developers; agent CTA ("Connect your agent") doesn't lead to connect action immediately; supply preview --input option conflicts (rejected as unsupported OR required); HTTP endpoint missing nextAction; status routes lack schemaVersion and CORS; supply earnings/tools/connections 503 without clear error; search returns 308 redirect CLI doesn't follow; supply/calls endpoint returns 405 before 401 (method not discoverable).
Fix: Expose `ae quote` command or state explicitly in README that CLI folds quoting into `ae call`; add "no SDK yet" pointer to README quickstart and ae help root; special-case retired-command hints in unknown-command error; add literal `ae search` example to for-agents page; retitle agent CTA or reorder funnel; wire --input option to supply preview or document required flow; factor nextAction into shared action layer; add schemaVersion + CORS to status routes; fix supply endpoints or map "no business yet" to 404; make CLI follow redirects or fix route; document POST requirement or accept GET for supply/calls read.

### G9. Environment mismatch — sandbox vs production
Findings: W7-056
Module owner: M4 (market), M5 (machine)
Issue: Catalogue shows production-only Tools; fresh `ae connect` binds sandbox agent; `ae describe` has no environment field; mismatch only discovered at `ae call` time when refused with "tool_unsupported".
Fix: Surface environment field in `ae describe` output (human + --json) next to health/indicative price so mismatch is visible before call attempt; or bind production by default.

### G10. Mobile 375 layout and typography regressions
Findings: W7-065, W7-070
Module owner: M1 (navigation)
Issue: Sidebar toggle overlaps page H1 at 375 breakpoint (clips title text); hydration mismatch at 375 only (/owner/settings).
Fix: Reserve left padding on H1 equal to toggle width + gap; make SSR/client render deterministic across breakpoints.

### G11. Empty-state and error-state consistency
Findings: W7-064, W7-066
Module owner: M1 (navigation), M7 (supply UX)
Issue: operator screens split empty-state visual treatment (onboarding card vs stat tiles vs bordered banner); forbidden workspace page renders correctly but throws console errors (500, error boundary, hydration).
Fix: Share empty-state component template; short-circuit auth check before failed request for clearly-forbidden state.

### G12. Search index and discovery contract gaps
Findings: W7-053, W7-077
Module owner: M4 (market)
Issue: market_tools.search index excludes title/description text (only curated tags); CLI search returns 308 redirect CLI doesn't follow.
Fix: include tool description/title tokens in index; follow same-origin redirects in CLI or fix route to not redirect.

### G13. Operator chrome and design consistency
Findings: W7-062, W7-026 (fixed in M1), W7-067, W7-069, W7-071
Module owner: M1 (navigation)
Issue: App-wide accent is green (#24543d) but DESIGN.md names violet-blue (#4a38f5); stacked logos on operator pages (fixed in M1); duplicate CTAs on forbidden screen; mobile focus weaker than desktop; hydration mismatch at 375.
Fix: Align accent colour across app/DESIGN.md; consolidate CTAs; parity focus treatment; deterministic SSR.

### G14. Performance and time-to-interactive
Findings: W7-055, W7-068
Module owner: M5 (machine), M7 (supply UX)
Issue: ae search 2-3x slower than list/describe for same catalogue; /for-providers 2.5-3.3s to first paint vs 50-170ms on other public pages.
Fix: Profile search fetch pattern vs list; profile /for-providers hydration blocker.

### G15. Rate limiter and API contract visibility
Findings: W7-059, W7-061
Module owner: M5 (machine)
Issue: rate-limiter declared but not enforced (60 requests all 200); HTTP endpoints ignore Accept: text/html per spec (noted only).
Fix: Lower/confirm public-read bucket threshold or document limit; no fix required for Accept handling.

### G16. Chat persistence and reliability
Findings: W7-084, W7-085, W7-086
Module owner: M6 (agent chat)
Issue: Anonymous chat loses history on reload (no thread ID); vocabulary contradiction (Quote explained but then "no access to tool.quote"); tool discovery blocked by chat_tool_refused tool_limit error with no user-facing explanation.
Fix: Return threadId in first message, store in URL `/t/{threadId}`, hydrate from server TTL session store. Branch chat responses for "what is X" (explain concept) vs "how do I use X" (explain with auth requirement). Catch tool_limit and fallback: "Discovery temporarily limited; browse catalog at /market instead."

### G17. Market discovery and navigation
Findings: W7-087, W7-088, W7-089, W7-090, W7-091, W7-092
Module owner: M4 (market), M1 (navigation)
Issue: Tool names not clickable to detail pages (table has save/compare only); ⌘K labelled "Find Tools" but expects command palette (navigation); empty states use vague copy ("capability" instead of "Tool"); search doesn't navigate to app sections; /for-providers blank load after skeleton; "Tools" sidebar link ambiguous (publish vs. browse).
Fix: Make tool names links to `/market/tools/{toolRef}`. Retitle ⌘K to "Search Tools" (tool-search-only) or add full command palette with route matching. Use consistent "Tool" vocabulary and "Browse Tools" CTAs. Add error boundary and logging to /for-providers. Distinguish sidebar "Tools" (Supply) from marketplace "Tools" via label like "Publish Tool" or icon.

### G18. API documentation and developer experience
Findings: W7-093, W7-094, W7-095, W7-097, W7-098, W7-099, W7-100, W7-101, W7-103, W7-105, W7-109, W7-111
Module owner: M5 (machine)
Issue: /.well-known/ucp not in README (discoverable only via /llms.txt); funding quote request structure not documented; device flow auth not described; freshness field source undocumented; compare endpoint not in README; toolRef format pattern exposed in error (not documented); nextAction response structure not shown with examples; rate limits undocumented; loopback limitation buried in doctor output; search/describe auth requirements contradictory; input schema discovery not pointed out; coldLoop manifest not explained.
Fix: Add discovery section to README pointing to /.well-known/ucp. Document device flow and Clerk API key auth. Add request/response schemas for /api/v1/funding/quote with nested amount structure. Document freshness SLA per endpoint. Add compare endpoint to README. Document toolRef format as stable contract. Include Call state machine table with nextAction per state. Document rate limits or absence thereof. Add prominent loopback limitation warning upfront in README. Clarify auth: discovery public, call/history/account require auth. Point to `ae describe --technical` for input schema. Explain coldLoop in manifest.

### G19. CLI command naming and parity
Findings: W7-082, W7-104, W7-108, W7-110
Module owner: M5 (machine)
Issue: Search latency variance undocumented (6x, query-dependent); npm run vs bare ae command confusion; Quote not directly readable from CLI (no pre-execution inspection); supply earnings/calls not exposed as commands.
Fix: Document search SLA and cache strategy. Clarify in README which CLI invocation is canonical (npm run for repo, ae for installed). Expose `ae quote` command or state explicitly that CLI folds quoting into `ae call`. Add `ae supply calls` and `ae supply earnings` subcommands to match API endpoints.

### G20. Error messages and validation
Findings: W7-083, W7-102, W7-106
Module owner: M5 (machine)
Issue: Required argument validation returns plaintext error instead of JSON-RPC -32602; cryptic "tool input or identity does not match tool.quote:v2" error mentions Quote (no command for it); typo suggestions missing (ae serch → ae search).
Fix: Validate args against schema before tool call; return JSON-RPC -32602 for required-field errors. Improve call error to say "Tool not recognized; use ae search first". Add fuzzy matching for unknown commands: "Did you mean 'search'?"

