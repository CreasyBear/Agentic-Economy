# Well 3 — Swarm Field-Report Triage (2026-09-10)

Source: six persona field reports in `output/swarm/` (buyer-cli, buyer-mcp,
developer-http, newcomer, operator, provider), each probing the sandbox at
`http://127.0.0.1:3024` independently. This table triages every papercut
raised, records what was verified fixed live today, and routes everything
else to an owning well.

## Triage table

| # | Persona | Severity | Papercut (one line) | Status | Owner well | Evidence (file:line or repro command) |
|---|---|---|---|---|---|---|
| 1 | buyer-cli | P1 | Funding-gate refusal (`insufficient_balance`) gives no next step in human mode; JSON mode shows literal `"<redacted>"` continuation placeholders | fixed 2026-09-10 | — | `ae call operation:v1:845372f7…c66 --input '{"request":"hello aecon"}' --base-url http://127.0.0.1:3024 --json` |
| 2 | buyer-cli | P1 | `describe`'s human-mode "Next" step names `tool.quote`, an MCP protocol name, not a real CLI command | fixed 2026-09-10 | — | `ae describe operation:v1:845372f7…c66 --base-url http://127.0.0.1:3024` |
| 3 | buyer-cli | P2 | Invalid-ref error says refs must match `tool:v1:<64 hex>`; real refs are `operation:v1:<64 hex>` | fixed 2026-09-10 | — | `ae describe operation:v1:0…0 (63 hex) --json` |
| 4 | buyer-cli | P2 | Empty search query rejected with code `search-query-too-long` (wrong description for 0 length) | fixed 2026-09-10 | — | `ae search "" --base-url http://127.0.0.1:3024 --json` |
| 5 | buyer-cli | P2 | `ae status <bogus-ref> --json` returns `kind: refused` but exits 0, unlike every other failing command | fixed 2026-09-10 | — | `ae status call_doesnotexist123 --json; echo $?` → `EXIT:0` |
| 6 | buyer-cli | P2 | `status`'s `nextCommand` on failure is a verbatim copy of the command that just failed | fixed 2026-09-10 | — | same repro as #5 |
| 7 | buyer-cli | P3 | `search` reports `schemaVersion: registry-tools:v3`; `describe`/`list`/`compare` report `v2` for the identical tool | fixed 2026-09-10 (describe/compare bumped to registry-tools:v3) | — | `ae search` vs `ae describe/list/compare --json` `schemaVersion` field |
| 8 | buyer-cli | P3 | `tool_not_found` response omits the `nextCommand`/`suggestion` fields present on sibling errors | fixed 2026-09-10 | — | `ae describe operation:v1:<64 zero hex> --json` |
| 9 | buyer-cli | P3 | `ae account` (human mode) prints the full raw credential value; `account connections` explicitly avoids this | fixed 2026-09-10 | — | `ae account --base-url http://127.0.0.1:3024` → `credential ak_local_e2e_…` |
| 10 | buyer-mcp | P1 | Protected purchase tools (`ae_tool_quote`/`call`/etc.) are invisible in `tools/list`; only named in prose inside `initialize.instructions` | By design (Well 4, 2026-09-10): MCP spec 2025-06-18 (RFC 9728) — auth at transport (401 w/ WWW-Authenticate, 403 for scope); checks auth before args; maintains authentication_required (401) and scope_required (403) for account-enumeration prevention. Pinned: mcp-api-tools-list.test.ts, mcp-api-protocol.test.ts | — | anonymous `tools/list` vs `client.getInstructions()` |
| 11 | buyer-mcp | P2 | Auth failures on protected MCP tools throw a raw transport HTTP 401 exception, not a normal `{isError:true}` tool result | By design (Well 4, 2026-09-10): MCP spec 2025-06-18 (RFC 9728) — auth at transport (401 w/ WWW-Authenticate, 403 for scope); checks auth before args; maintains authentication_required (401) and scope_required (403) for account-enumeration prevention. Pinned: mcp-api-tools-list.test.ts, mcp-api-protocol.test.ts | — | `callTool({name:'ae_tool_quote', arguments:{toolRef, input:{request:'ping'}}})` anonymous |
| 12 | buyer-mcp | P3 | Auth is checked before argument-shape validation, so a malformed+unauthenticated call only ever reports the auth problem | By design (Well 4, 2026-09-10): MCP spec 2025-06-18 (RFC 9728) — auth at transport (401 w/ WWW-Authenticate, 403 for scope); checks auth before args; maintains authentication_required (401) and scope_required (403) for account-enumeration prevention. Pinned: mcp-api-tools-list.test.ts, mcp-api-protocol.test.ts | — | `callTool({name:'ae_tool_quote', arguments:{}})` anonymous → same 401 |
| 13 | buyer-mcp | P3 | `tools/list` returns identical `authentication_required` for no-token, garbage-token, and (per source) under-scoped-token cases | By design (Well 4, 2026-09-10): MCP spec 2025-06-18 (RFC 9728) — auth at transport (401 w/ WWW-Authenticate, 403 for scope); checks auth before args; maintains authentication_required (401) and scope_required (403) for account-enumeration prevention. Pinned: mcp-api-tools-list.test.ts, mcp-api-protocol.test.ts | — | fresh connection, `Authorization: Bearer not-a-real-key`, `listTools()` |
| 14 | buyer-mcp | P3 | MCP `search` result omits the `nextCommand` hint that the CLI's JSON `search` includes | Fixed in Well 4 (structured nextAction on MCP search results) | — | `ae_registry_tools_search` vs `ae -- search … --json` |
| 15 | developer-http | P1 | `GET /api/v1/registry?...&cursor=bad` returns 503 `registry_unavailable` instead of 400 for an invalid cursor | fixed 2026-09-10 (registry cursor now 400; sibling regex fixed too) | — | `curl -i "http://127.0.0.1:3024/api/v1/registry?query=weather&cursor=not-a-real-cursor"` |
| 16 | developer-http | P1 | `/api/businesses/search?query=...` silently ignores the unsupported `query` param and returns confident empty results instead of 400 | fixed 2026-09-10 | — | `curl -s "http://127.0.0.1:3024/api/businesses/search?query=sandbox"` |
| 17 | developer-http | P1 | `/api/businesses/search?q=...` returns zero results for terms verbatim present in `/api/businesses` list output | fixed 2026-09-10 (bootstrap/curated publish rebuilds search projection; legacy businesses published pre-fix remain unsearchable until rebuilt) | — | `curl -s "http://127.0.0.1:3024/api/businesses/search?q=sandbox"` |
| 18 | developer-http | P2 | Two disjoint catalogues (`/api/v1/registry`, 14,568 external x402 listings vs `/api/v1/market-tools/*`, 12 admitted Tools) answer the same query differently with no signposting | Fixed in Well 4 (see docs/workflow/well-4-catalogue-truth-closeout.md) | Well 4 — registry vs market-tools split | `/api/v1/registry?query=weather` (24 hits) vs `market-tools/search {"query":"weather"}` (`no_candidates`) |
| 19 | developer-http | P2 | RFC 9457 problem responses for content-type/JSON errors (415/400) omit the `detail` field other validation errors include | Fixed in Well 4 (see docs/workflow/well-4-catalogue-truth-closeout.md) | Well 4 (catalogue truth / API consistency) | `curl -i -X POST .../market-tools/search -H "Content-Type: text/plain" -d '{"query":"weather"}'` |
| 20 | developer-http | P2 | Two incompatible pagination idioms across sibling endpoint families (`continueCursor`/`isDone` vs `pagination:{limit,hasMore}`) | Fixed in Well 4 (see docs/workflow/well-4-catalogue-truth-closeout.md) | Well 4 — two catalogues/pagination idioms | `/api/v1/registry` & `/api/businesses` vs `/api/v1/market-tools/list` |
| 21 | developer-http | P3 | `/api/discovery/schema` and `/api/discovery/examples` report `generatedAt:0` while claiming `freshness.state:"current"` | Fixed in Well 4 (see docs/workflow/well-4-catalogue-truth-closeout.md) | Well 4 — generatedAt:0 | `curl -s http://127.0.0.1:3024/api/discovery/schema` |
| 22 | developer-http | P3 | Zod validation messages leak raw parser phrasing instead of naming the missing field | Fixed in Well 4 (see docs/workflow/well-4-catalogue-truth-closeout.md) | Well 4 (API consistency) | `curl -i -X POST .../market-tools/search -d '{}'` |
| 23 | developer-http | P3 | Cold-cache first request against `/api/v1/registry`/`market-tools/list` is 15–100x slower with no cache-state signal | Partially addressed in Well 4: freshness object on every catalogue response; cache-state signal itself still open | Well 4 — cold-cache latency | repeated `curl` timing on `/api/v1/registry`, `/api/v1/market-tools/list` |
| 24 | newcomer | P1 | README's "Machine quickstart" block (search/describe/call) omits `--base-url`, silently hitting the hosted prod origin instead of local | fixed 2026-09-10 | — | `README.md:199-204` |
| 25 | newcomer | P1 | README.md:156 `npm run ae -- doctor --json` line also omits `--base-url`; returns `discovery: fail` against hosted default as literally written | fixed 2026-09-10 | — | `README.md:156` |
| 26 | newcomer | P2 | AGENTS.md's opening rule sends every reader to 592-line `PRODUCT.md` before any action, unscoped to product work only | fixed 2026-09-10 | — | `AGENTS.md` opening rule |
| 27 | newcomer | P2 | Tracing where a Tool's price comes from takes a blind 4-hop grep chain with zero doc pointer | Fixed in Well 6 (see docs/workflow/well-6-operator-truth-closeout.md) | Well 6 docs track | `tools/ae/commands/describe.ts:46` → `src/modules/registry/tool-choice-contracts.ts:183` → `src/modules/capability-supply/internal/tool-project.ts:93,125` |
| 28 | newcomer | P2 | Adding a public API route has zero documentation; convention discoverable only by directory-listing `src/routes/` | Fixed in Well 6 (see docs/workflow/well-6-operator-truth-closeout.md) | Well 6 docs track | `src/routes/api.v1.catalogue-status.ts` |
| 29 | newcomer | P2 | `npm run gate` (README's headline "Useful check") takes ~4.5 min with no duration estimate and failed 2/50 e2e tests on this checkout with no interpretation guidance | filed | Well 3 closeout note — gate duration/interpretation | `tests/e2e/developer-discovery.spec.ts:33` |
| 30 | newcomer | P3 | Tool/Quote/Call/Provider/spending policy/x402 used as proper nouns in README's first paragraph before any definition; glossary lives only in `CONTEXT.md` | Fixed in Well 6 (see docs/workflow/well-6-operator-truth-closeout.md) | Well 6 docs track — glossary/jargon | `README.md:3-44` |
| 31 | newcomer | P3 | README.md:148-149 uses "sandbox" twice in one sentence for two different meanings, neither glossed | fixed 2026-09-10 | — | `README.md:148-149` |
| 32 | newcomer | P3 | README.md and AGENTS.md carry no "Revised:"/"Status:" freshness stamp, unlike `CONTEXT.md`/`DESIGN.md` | fixed 2026-09-10 | — | `README.md`, `AGENTS.md` headers |
| 33 | operator | P1 | Doctor's `Next:` line on an unreachable local server points at the remote production URL instead of a local port check | fixed 2026-09-10 | — | `ae doctor --base-url http://127.0.0.1:3999` → `Next: ae doctor --base-url https://agentic-economy-phi.vercel.app` |
| 34 | operator | P1 | `/api/ready` reports blanket `status:"ready"` while the market cannot complete a sale — no commercial-capability scope signal | Fixed in Well 6 (see docs/workflow/well-6-operator-truth-closeout.md) | Well 6 — /api/ready sellability semantics | `curl -s http://127.0.0.1:3024/api/ready` |
| 35 | operator | P1 | Doctor's `market_requests` check is labeled `state:"pass"` while its own summary describes an unresolved gap | fixed 2026-09-10 | — | `ae doctor --json` → `market_requests` check |
| 36 | operator | P2 | `/status` is a client-hydrated SPA; curl only ever returns the frozen loading skeleton | Fixed in Well 6 (see docs/workflow/well-6-operator-truth-closeout.md) | Well 6 — /status and /owner/supply SPA skeletons | `curl -s http://127.0.0.1:3024/status \| sed 's/<[^>]*>/ /g'` |
| 37 | operator | P2 | `ae account --json` never surfaces the balance figure the funding-gate diagnosis depends on | filed | Well 2 (money) — funding step guidance beyond the next command | `ae account --base-url http://127.0.0.1:3024 --json` |
| 38 | operator | P2 | `ae status` demands a call-ref with no example and no pointer to `ae history` when history is empty | Fixed in Well 6 (see docs/workflow/well-6-operator-truth-closeout.md) | Well 6 (support/operations) | `ae status --base-url http://127.0.0.1:3024 --json` → `Usage: ae status <call-ref>` |
| 39 | operator | P3 | Doctor's human `Next:` line and JSON `nextCommand` disagree by one flag (`--json`) | fixed 2026-09-10 | — | human `Next: ae fund --base-url http://127.0.0.1:3024` vs JSON `nextCommand: … --json` |
| 40 | provider | P1 | `ae supply preview` cannot be invoked with any input — `--input` rejected as unsupported, omitting it rejected as required | fixed 2026-09-10 (supply preview option gap in cli.ts) | — | `ae supply preview --input '{...}'` → `option-not-supported`; without `--input` → `supply-input-required` |
| 41 | provider | P1 | Only way to obtain the `market_supply:manage` provider credential is an interactive browser device-flow; all 12 other `ae supply` subcommands are unusable without it | fixed 2026-09-10 (connect:local --provider) | — | any `ae supply <sub>` → `agent_access_key_required` / `ae connect --provider` |
| 42 | provider | P2 | Seeded reference listing shows contradictory prices: CLI reports `AUD 1.000000`, public business page renders "Price unknown" | fixed 2026-09-10 (business page price fact seeded) | — | `ae describe operation:v1:845372f7…c66` vs `curl http://127.0.0.1:3024/sandbox-aecon-reference` |
| 43 | provider | P2 | `describe`'s own tool-ref-invalid error documents `tool:v1:<64 hex>`; real refs are `operation:v1:<64 hex>` (same bug as #3) | fixed 2026-09-10 (same fix as buyer-cli #3) | — | `ae describe sandbox-aecon-reference` |
| 44 | provider | P2 | 10% platform fee and rake mechanics are undocumented on every provider-facing page; only visible inside a 774KB `ae manifest --technical --json` blob | filed | Well 2 (money) — fee disclosure and AUD pricing/terms docs | `/for-providers`, `/about`, `/owner/offerings` (no "fee"/"commission"); `src/modules/capability-supply/internal/supply-funnel/pricing-port.ts` (`DEFAULT_RAKE_BPS`) |
| 45 | provider | P2 | "Is my Tool listed?" is answered by five overlapping, unreconciled status vocabularies plus a sixth pair on the public page | Fixed in Well 4 (see docs/workflow/well-4-catalogue-truth-closeout.md) | Well 4 — five status vocabularies | business `publicStatus`, `ProviderToolState`, publication `lifecycle`, `sourceAuthorityState`, offering `admission` |
| 46 | provider | P2 | `ae help supply publish\|withdraw\|recheck\|republish\|connect\|reconnect\|revoke` don't list required `--input` fields; `{}` only returns an opaque schema-version mismatch | fixed 2026-09-10 (schema-derived required-input help) | — | `ae supply publish --input '{}'` → `supply-input-invalid` / `supply.publish:v2` |
| 47 | provider | P2 | Providers have no way to see individual Calls/Quotes — only anonymized aggregate delivery counts and currency-level earnings totals | Fixed in Well 6 (see docs/workflow/well-6-operator-truth-closeout.md) | Well 6 — provider Quote/Call visibility | no `calls`/`quotes` subcommand under `ae supply`; `ae supply earnings <currency>` aggregate-only |
| 48 | provider | P3 | `/owner/supply` SSR payload is just a "Loading your latest marketplace activity" shell, unlike `/owner/offerings` which SSRs real form state | Moot: /owner/supply now redirects to /owner/offerings (Well 6) | Well 6 — /status and /owner/supply SPA skeletons | `curl` (Clerk bypass session) `http://127.0.0.1:3024/owner/supply` |
| 49 | provider | P3 | `ae doctor <businessId> --provider` silently ignores the businessId — output byte-identical regardless of which ID (or none) is passed | fixed 2026-09-10 (doctor --provider contract text) | — | `ae doctor <businessId> --provider` for two different business IDs |

## Counts

Total: 49 papercuts. By severity: P1 = 13, P2 = 21, P3 = 15. By status: fixed 2026-09-10 = 26, filed = 23, wontfix = 0.

## Verdicts (verbatim, one per persona)

**buyer-cli**: "No — with only discovery commands, this persona could fully understand the tool, its exact price (AUD 1.00), and its terms, but could not get any closer to an actual purchase than the `insufficient_balance` refusal, and the CLI's own guidance at that exact moment (in both human and JSON mode) does not make the real next step (fund via the owner browser surface) discoverable without already knowing to run `ae doctor` first."

**buyer-mcp**: "An agent that reads and follows the server's `initialize.instructions` (not just `tools/list`) and already holds a scoped API key could plausibly complete a quote-to-call purchase without a human — but an agent that relies on `tools/list` alone would never discover that purchasing is possible, and every agent hits an unauthenticated 401 whose error shape differs from every other failure mode it will otherwise see from this server."

**developer-http**: "Yes, I would build on this: the self-description layer (UCP manifest, RFC 9727 linkset, RFC 9457 problem envelopes, consistent Tool identity/price across every read surface) is unusually mature for a sandbox, and worth the day it would take to route around the broken business/service search and the registry-vs-market-tools split before shipping anything load-bearing."

**newcomer**: "For a competent engineer following only README.md and AGENTS.md: confirming the stack is healthy takes minutes once you know to add `--base-url`, but the docs' own copy-pasteable commands (search/describe/call and even `doctor`) default to the hosted origin and fail or silently do the wrong thing as literally written — that trap, plus the zero doc guidance on where price logic or route wiring live, means a genuinely new contributor should budget 45–75 minutes from a cold clone to landing a first trivial, correct change (e.g., a new `/api/v1/*` route returning real price data), most of it spent on jargon absorption and the origin-default trap rather than on the actual code change."

**operator**: "Time-to-diagnosis: under 10 minutes (doctor alone gets there in under 1 minute; a careful human triple-checking every surface takes ~9), and the single next action it names — `ae fund --base-url http://127.0.0.1:3024` — is correct. But the diagnostics would only get a tired human there reliably if they knew to skip `/api/ready` and `/status` and go straight to `ae doctor`; anyone trusting the two most obvious health-check surfaces first would be actively misled for several minutes."

**provider**: "No — a real business could not self-serve from 'I have an endpoint' to 'it's listed and quotable' without an engineer reading the source: the CLI's own first step (`supply preview`) cannot be invoked at all, the fee structure is undocumented outside a hidden debug flag, and the product surfaces at least five overlapping, unreconciled vocabularies for 'is it live yet.'"

## Patterns

- **The funding wall is the sharpest edge.** Buyer-cli's refusal message, buyer-mcp's funding-handoff tools, and operator's missing balance field all converge on the same gap: an agent can find, price, and quote a Tool with zero friction, but the moment money is required the product goes quiet.
- **Two stores for one fact, disagreeing.** Schema-version drift (buyer-cli), five status vocabularies for "is it listed" (provider), CLI price vs public-page price (provider), and registry vs market-tools catalogues answering the same query differently (developer-http) are the same underlying failure mode repeated across four surfaces.
- **Guidance that names the wrong command.** Doctor's `Next:` pointing at production instead of a local port fix (operator), `describe`'s "Next" naming a non-existent CLI verb (buyer-cli), and error messages documenting a ref format no real ref uses (buyer-cli, provider) all erode trust in the "next step" fields the product otherwise does well.
- **Pages that are skeletons over curl.** `/status` and `/owner/supply` both require a browser to show anything real; `/api/ready` compounds this by reporting "ready" with no commercial-capability scope, so the two surfaces a human reaches for first under pressure are the least useful.
- **Money is the least-documented subject.** The 10% platform fee is buried in a 774KB `--technical` manifest (provider), account balance is invisible from `ae account` (operator), and funding continuation fields ship as literal `"<redacted>"` placeholders (buyer-cli).
- **Purchase capability is invisible outside prose.** MCP's protected tools exist only in `initialize.instructions`, not `tools/list` (buyer-mcp), mirroring newcomer's finding that core vocabulary (Tool/Quote/Call/x402) is used before definition anywhere a newcomer is told to look first.
