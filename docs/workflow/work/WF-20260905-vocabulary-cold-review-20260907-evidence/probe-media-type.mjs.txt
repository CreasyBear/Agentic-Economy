import { startVitest } from '/Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/node_modules/vitest/dist/node.js'
const probe = "\nit('cold audit wrong media type reaches call service', async () => {\n const executor = service({ kind: 'refused', toolRef: operationRef, code: 'authority_required', retryable: false })\n const response = await handleCallPost(new Request('https://ae.example/api/v1/tools/call', { method: 'POST', headers: { 'content-type': 'text/plain' }, body: JSON.stringify(invokeBody('cold-media')) }), { authenticate, callService: executor })\n console.log('COLD_MEDIA_TYPE_RESULT', JSON.stringify({ status: response.status, serviceCalls: executor.callTool.mock.calls.length }))\n expect(executor.callTool).toHaveBeenCalledTimes(1)\n expect(response.status).not.toBe(415)\n})\n"
const context = await startVitest('test', ["tests/unit/server/call-api.test.ts"], {watch:false, fileParallelism:false, testNamePattern:"cold audit wrong media type reaches call service", reporters:['default']}, {plugins:[{name:'cold-memory-probe',enforce:'pre',transform(code,id){if(id.endsWith("/tests/unit/server/call-api.test.ts")) return {code:code+probe,map:null}}}]})
await context.close()
