import { startVitest } from '/Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/node_modules/vitest/dist/node.js'
const probe = "\nit('cold audit late observation historical digest conflict', async () => {\n const { recordLateObservation } = await import('../../../convex/actionExecutionControl')\n const { canonicalDigest } = await import('@/modules/common/canonical-digest')\n const args = { executionRef: 'execution:cold', commandId: 'command:cold', effectGeneration: 1, actorRef: 'actor:cold', sourceEvidenceRef: 'evidence:cold', release: 'released', evidenceDigest: 'sha256:' + 'a'.repeat(64), recordedAt: '2026-09-07T00:00:00.000Z' }\n const material = { effectGeneration: args.effectGeneration, actorRef: args.actorRef, sourceEvidenceRef: args.sourceEvidenceRef, release: args.release, evidenceDigest: args.evidenceDigest }\n const historicalDigest = canonicalDigest({ invocationRef: args.executionRef, ...material })\n const currentDigest = canonicalDigest({ callRef: args.executionRef, ...material })\n let priorDigest = historicalDigest\n const db = { query: (table) => ({ withIndex: () => ({ unique: async () => table === 'actionExecutionControls' ? { executionVersion: 3 } : { commandDigest: priorDigest, executionVersion: 3 } }) }) }\n const handler = recordLateObservation._handler\n const historicalReplay = await handler({ db }, args)\n priorDigest = currentDigest\n const currentReplay = await handler({ db }, args)\n console.log('COLD_LATE_DIGEST_RESULT', JSON.stringify({ digestsEqual: historicalDigest === currentDigest, historicalReplay, currentReplay }))\n expect(historicalReplay).toEqual({ kind: 'refused', code: 'command_identity_conflict' })\n expect(currentReplay).toEqual({ kind: 'duplicate', executionVersion: 3 })\n})\n"
const context = await startVitest('test', ["tests/unit/convex/source-write-admission.test.ts"], {watch:false,fileParallelism:false,testNamePattern:'cold audit late observation historical digest conflict',reporters:['default']}, {plugins:[{name:'cold-memory-probe',enforce:'pre',transform(code,id){if(id.endsWith("/tests/unit/convex/source-write-admission.test.ts")) return {code:code+probe,map:null}}}]})
await context.close()
