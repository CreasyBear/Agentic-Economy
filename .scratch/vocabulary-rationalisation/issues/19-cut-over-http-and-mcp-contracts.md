## Public review requires three contract corrections — 2026-09-07

Independent review of the complete Public candidate requires correction before
acceptance. Report: `/tmp/ae-public-independent-review-20260907.txt`.
The same Public owner is addressing one consolidated batch:

- Acquire a Quote and preserve its reference and command identity across HTTP/MCP Call replay.
- Consume the actual public search v3 and describe v2 projections, including withdrawn readback.
- Unwrap hosted MCP structured results and handle MCP errors explicitly.

The correction includes tests against the actual contracts. The earlier candidate
and review remain the baseline; only the material corrective delta returns for
review. No public API widening or hosted execution is authorized by this return.
Public and final artifact acceptance remain open.

## Stable Public source candidate; integrated dependencies remain — 2026-09-07

Immutable `/tmp/ae-public-review-candidate-20260907/manifest.json`:1800files,
zerochangedDuringCapture,83netPublicpaths. Independentoversightreceivedthe
completeexistingPublicinventory plusallcontinuation/rootreceipts; reviewis
pending, notPublicorartifactacceptance. Fullcompiler67diagnostics16earlierowner
files,zeroPublic; log `/tmp/ae-public-final-coherent-checkpoint-20260907.log`.
Lasttwoassertion-onlytests23/23PASSafterthatmeasurement. Candidateincludes
package/rootartifactevidence andqualifiedsupplementalREADME/pluginpreimages.

`pack:cli:public`PASS (existingCLIbuild+threefilearchive). Existing
assertCliPackIntegritywithfreshnpmreportPASS; actualarchiveSHA1/SHA512match.
ArchiveSHA256fe6499e06f0a9d2f1f5d5d0eb2d6ae661549c70ce8f2e4c3fbbbfab770d708d6.
CompiledbundlehelpJSONPASS. Node20/22package-matrixNOTRUN,pendingexplicitpolicy;
thesechecksarenotafullinstalled-packageharnesspass.

Importbatch46PASS3FAIL; twoexistingmanifestpathupdatesresolveToolviewmodeland
unrestricted-test-onlyreferences. Narrowmodule-boundaries9PASS2FAIL nowonly
Connectionagent-access-console→capability-supplytypeedge andCallidentitytest
privatecall-admitimport. RootdidnotaddDAGedges/entries/exceptions; sourceowners
mustreturntheboundaryreceipt. Conformance388PASS3FAIL; Publicmanifestassertion
fixedby23/23return, remaining2Providerfailures initsrecordedreturnpaths.
No fullimports/conformanceacceptance claimed. Exactlogs inpriorentries.

Publicsourcewriterreturned; samePolicyowner startsits2consumerreturn while
oversightreviewsPublic. Earlierexistingownersretainall67errorsandtwoimport
findings. Thisisdependencyresolutionthroughsameownership, notanewqueue orroot
fallback. DocsheldacceptedPublic/artifactreceipts; commits/integratedacceptance
andparkedruntimeworkremainopen.

## Public coherent correction return — 2026-09-07

Same04owner fixed13Publicconsumerpaths and returnedat187472inputafter1compaction.
Rootcorrectedglobalcompiler70diagnostics17files:67existingupstreamreturns plus
3newlyexposedSDKmessagefixtureexcess-propertyerrors in chat-durable-messaging-share.
Rootchat-provider-contract existing sockettest2/2PASS; chatshare runtime7/7PASS.
Currentlogs `/tmp/ae-public-corrected-coherent-checkpoint-20260907.log` and
`/tmp/ae-public-root-chat-provider-20260907.log`. Earliergenerator/types/audit
results remain unchanged. Public05 continuesSAMEcompleteownership tocorrect
SDKfixturetypes andfinishexactcli-errors-operation-read→cli-errors-tool-read
filenamecutover; no newinventory or rootfallback. Packaging/reviewheld. Earlier
67diagnostics16filesretainexistingmoduleowners. Fullsourceacceptanceremainsopen.

Receiptqualification: the first affected12filebatch had108PASS3FAIL, followedby
chatshare7PASSandrootsocket2PASS; counts overlap. The worker's10files108wording
is not an independent aggregate. Exactruntimefailureswere resolved; compiler
fixturetypes still requirecorrection. No blanketgreenmoduleclaim.

## Public03 returned; same owner04 continues — 2026-09-07

Public03 paused at184494input after1actualcompaction; return-only receipt
`/tmp/ae-public-continuation03-return-result-20260907.txt` and exact15changed
paths `/tmp/ae-public-continuation03-changed-paths-20260907.txt`. Root ran the
existing socket harness: origin28/28PASS and account/history27/27PASS. Logs
`/tmp/ae-public-root-origin-20260907.log` and
`/tmp/ae-public-root-account-history-20260907.log`. No harness changes.

Other03results: discovery20,Call15,help16,coldloop29,wait4,compare7,supply10
PASS; recovery17/18 then final manifest assertion corrected but not rerun.
Counts overlap. Remaining MCP5file10PASS14FAIL and release earnings/callhelper
coverage stay with the same whole Public owner. Root identified opaque fixture
values incorrectly renamed invocation:*→call:* in coldloop; owner04 must restore
values while retaining current fields/actions/routes and rerun affected tests.
No stable candidate, globalcompiler, artifacts or Publicacceptance yet;8/10.

## Public continuation03 — implementation active, 2026-09-07

Same complete Public19/20/21 ownership continues from the prior receipt, with
one actual context compaction. No stable candidate or acceptance yet. Focused
results so far: discovery parity/SEO/page-markdown20/20PASS; CLI Call15/15PASS;
CLI help/error16/16PASS. These are separate focused results, not whole-module
acceptance. An earlier eight-suite terminal checkpoint had43PASS/60FAIL; the
owner is correcting its stale current-contract fixtures and direct consumers.
Cold-loop, recovery, wait/account/compare/history/supply, MCP and shared release
consumer verification remain with that owner; no reactive new inventory.

Root22 prepared only three exact accepted Call importer path replacements in
module-boundaries and the existing audit's `listCallRouteDescriptors` export
call. Checks/artifacts remain held for coherent Public source. Supplemental
review baseline now also includes `tools/ae/README.md` reconstructed from three
identical full pre-edit read outputs; this is an ignored local guide, distinct
from the packaged `packages/cli/README.md`. Provenance is explicit in
`/tmp/ae-public-additional-baseline-20260907/manifest.json`.

Eight groups remain source accepted. Docs inventory is complete and its source
updates remain held for accepted Public/artifact receipts. No backend, hosted
smoke, data, deployment, package publication or source commit occurred.

# Cut over HTTP and MCP contracts to the approved Tool and Call vocabulary

## Public02 handoff — partial, continued same owner

Root qualification: do NOT change correct providerTool assertions back to providerOperation. Discovery currentproducer/types still have ordinary unsupported.providerOperation:false; these are AE-owned currentDTOs and must become providerTool:false, preserving nonAuthority/falsecapability semantics. Existing tests already assert providerTool. PublicboundedREADMEquickstart/toolsREADME/ABOUTcopy edits carryforward into laterDocs receipt; onlycurrentcontractexamples/copy, no statuspromotion. Root supplementalREADME baseline is originaldirtypickupcomparison, noted in `/tmp/ae-public-additional-baseline-20260907/manifest.json`. No sourceacceptance yet,8/10.

## Handoff — Public19/20/21 owner02

Formal acceptance remains 8/10. Public remains unaccepted. Money and storage acceptance are preserved.

### Actually edited in this continuation

- CLI contract/help: `tools/ae/cli.ts`, `tools/ae/commands/{manifest,call,status,wait,cancel,recover,connect,account}.ts`
- CLI docs/metadata: `tools/ae/README.md`, `packages/cli/README.md`, `packages/cli/package.json`
- Public copy: `README.md`, `src/content/brand-copy.ts`
- Installed skill: `plugins/agentic-economy/skills/use-agentic-economy/SKILL.md`
- Release/package5: `tools/release/package5-provider-operations.ts`
- Release tests/imports:
  - `tests/unit/release/package5-provider-operations.test.ts`
  - `tests/unit/release/tool-gateway-production-smoke-{config,discovery,earnings,receipt}.test.ts`
- Direct contract tests:
  - `tests/unit/market-terminal/manifest-oauth.test.ts`
  - `tests/unit/actions/{registry,tool-contract}.test.ts`
  - `tests/unit/capability-supply/supply-actions.test.ts`
  - `tests/integration/api-catalog-route.test.ts`

### Applied mappings

- Manifest protocol: `agentic-economy.tool-terminal.v1`
- Manifest `operationReads` → `toolReads`; `disconnectSupplier` → `disconnectProvider`
- CLI help/commands use Tool, Quote, Call, `toolRef`, `callRef`, `quoteRef`, `market_tools:call`
- `ae describe`, `ae call`, current `/market-tools`, `/tools/quote`, `/tools/call`, `/calls/{callRef}` routes
- Retained `ae supply operations`; no alias or new hierarchy
- Call recovery compares CLI `callRef` with protected evidence `invocationRef`
- Package5 uses `toolsList`, `supplyToolsListResultSchema`, `/api/v1/supply/tools/list`
- Release discovery tests use `tools`, `toolRef`, `discoverTool`, `matchGatewayServiceTool`
- Action/MCP test expectations use `registry.tools.*`, `tool.quote`, `tool.call`, `call.*`, `supply.tools.list`
- Installed skill uses `ae_tool_quote`, `ae_tool_call`, `ae_call_status`, `ae_call_cancel`, `ae_call_reconcile`
- Package dependencies, version, engines, and import surface unchanged.

### Protected material preserved

- Accepted Money receipt encoder, snapshot, and two-file `readBooking` correction
- Historical receipt fields/mappings: `invocationRef`, `operationRef`, supplier evidence fields
- Opaque `operation:v1:*` references and descriptor `operationId`
- Existing `operation.invoke` canonical hash bytes
- Upstream OpenAPI `operationId`, MCP/OAuth/x402 fields
- External financial namespaces
- Historical `/api/v1/release/operation-gateway` evidence boundary

### Verification

Original bounded 32-file post-source run: 255 tests — 147 passed, 108 failed; 9 files passed, 23 failed. This was before the later direct-test repairs above.

Later focused check passed:

```text
4 files, 15 tests passed
```

Covered manifest OAuth, release discovery, release status, and Package5.

A subsequent larger named run was aborted after `tests/unit/market-terminal/cli-errors-origin.test.ts` reached its timeout. No final result count exists for that run. No post-change Oxlint or `git diff --check` was run.

### Remaining work

Unrepaired or not re-run:

- `tests/unit/market-terminal/{call,cold-loop,recovery,wait,account,compare,cli-errors-help,cli-errors-origin,history,supply}.test.ts`
  - Must consume current Tool/Quote/Call routes and DTOs; `call.test.ts` must import `runCallCommand`, with no `runInvokeCommand`.
- `tests/unit/server/{mcp-api-tools-list,mcp-api-operation-invoke,mcp-api-account,mcp-api-supply,supply-action-api}.test.ts`
  - Must consume current MCP names, `market_tools:call`, `callService`, `quoteRef`, `callRef`, and `toolsList`.
- `tests/unit/discovery/{page-markdown,developer-discovery-parity,cli-distribution}.test.ts`
  - Must consume current Tool copy/routes, `providerOperation`, and current OAuth scopes.
- `tests/unit/release/tool-gateway-production-smoke-earnings.test.ts`
  - Must consume current Provider earnings helper/fields.
- Direct current call-helper coverage still needs completion against `tool-gateway-production-smoke-call.ts`; no hosted runner.
- Re-run all repaired tests, then scoped Oxlint and `git diff --check`.

### Root-owned follow-up

Do not edit these from this ownership:

- `src/modules/module-boundaries.ts`
- root `package.json` scripts
- native route generation
- CLI `dist/ae.js`
- public CLI tarball/integrity under `public/downloads`
- global compiler, trackers, serialized commits

Root should verify script/path references for:

```text
tests/unit/release/tool-gateway-production-smoke-{config,discovery,earnings,receipt,status}.test.ts
tests/unit/release/tool-gateway-production-smoke-harness.ts
```

Relevant root-owned script consumers include release architecture/source checks, gateway production smoke, and import checks. Native generation must use actual current descriptor routes, not RPC export renames. Root must run the existing socket/installed-CLI harness and global checks after this public module is complete.

## Public continuation handoff — implementation partial

Paused before second actual compaction; same wholePublicowner continues from precise receipt below. Root qualification: src/modules/actions/index.ts and ordinary public exports remain Public owner, not root fallback. Root owns only shared module-boundaries.ts/root scripts/nativegeneration/package artifacts. Supplemental untouched plugin baseline `/tmp/ae-public-additional-baseline-20260907/manifest.json` captured before anyPublicedit to pluginSKILL. No postchange testpass yet. Formal sourceacceptance8/10.

## Handoff — partial, not accepted

Completed inventory preserved; no reinventory performed.

### Actually applied

- `src/modules/capability-supply/supply-actions.ts`
  - `supply.operations.list` → `supply.tools.list`
  - `operationsList` schemas/service/action → `toolsList`
  - route → `/api/v1/supply/tools/list`
- `src/modules/actions/index.ts`
- `src/lib/server/supply-action-api.ts`
- Market-tool route body-limit locals:
  - `src/routes/api.v1.market-tools.{list,search,describe,compare}.ts`
  - `MAX_OPERATION_*` → `MAX_TOOL_*`
- `tools/ae/commands/supply.ts`
  - target action/route/output names applied
  - retained CLI verb `ae supply operations`
- `tools/ae/commands/doctor.ts`
  - Provider naming and Call recovery checks applied
- `src/lib/server/mcp-api.ts`
  - Quote → Call instructions and current MCP names
- Release adapters:
  - `tools/release/tool-gateway-production-smoke-call.ts`
  - `tools/release/tool-gateway-production-smoke-discovery.ts`
  - `tools/release/tool-gateway-production-smoke.ts`
  - `tools/release/tool-gateway-production-smoke-hosted-runtime.ts`
  - `tools/release/tool-gateway-production-smoke-hosted-owner.ts`
  - Current `toolRef`/`callRef`, `/api/v1/tools/call`, `/api/v1/calls/*`, `/api/v1/market-tools/*`, and `ae_tool_call` mappings applied.
- Release test files moved to `tool-gateway-production-smoke-*` stems:
  - config, discovery, earnings, harness, receipt, status
- `tests/unit/release/tool-gateway-production-smoke-harness.ts`
  - current Tool/Call fixtures and `ae.toolRef` service links
- `tests/unit/release/tool-gateway-production-smoke-status.test.ts`
  - current Call status fields and renamed helper usage

### Verification

Baseline focused 20-file suite before these edits:

- 150 tests
- 92 passed
- 58 failed
- 12 files failed

Failures were primarily stale target expectations, old supply fake fields/scopes, discovery assertions, and release status helpers importing removed schemas.

No tests or checks ran after the edits.

### Protected and unchanged

- Accepted Money receipt encoder and preimage mappings.
- Historical receipt keys: `operationRef`, `invocationRef`, supplier fields, and related evidence material.
- Protected opaque `operation:v1:*` values and descriptor `operationId`.
- Canonical `operation.invoke` hash material in `src/lib/server/call-api.ts`.
- Historical `/api/v1/release/operation-gateway` evidence/path material.
- Convex/backend producers, generated files, package scripts, dist/tarball, trackers, and commits.

### Remaining owned work

- Complete `tools/ae/commands/manifest.ts`, `cli.ts`, `call.ts`, `status.ts`, `describe.ts`, `account.ts`, package README/metadata.
- Update installed plugin instructions:
  - `plugins/agentic-economy/skills/use-agentic-economy/SKILL.md`
- Finish discovery copy and direct tests:
  - page markdown, CLI distribution, developer parity, API catalog route tests.
- Update action/MCP/supply/cold-loop/market-terminal direct tests to current IDs, scopes, routes, and fields.
- Fix moved release-test imports still referencing the old harness stem.
- Update release discovery tests to `tools`, `toolRef`, `discoverTool`, and `matchGatewayServiceTool`.
- Update `tools/release/package5-provider-operations.ts` from `.operationsList` to `.toolsList`.
- Add/finish direct current-contract tests for call-helper request, response, status, replay, and metadata behavior.

### Root-owned handoff

Root must reconcile:

- shared `module-boundaries.ts` ownership/path entries;
- root export/index references for `supplyToolsListAction` and target route stems;
- native route generation from actual target route inputs;
- any package test-script references after test renames;
- CLI build, dist/tarball, global compiler, and existing socket-based harness.

No whole-module readiness claim: Public remains unaccepted and partial.

## Group9 implementation ACTIVE — accepted Money baseline

Same whole Public19/20/21owner continues from completed inventory and allten completion reads. Source baseline is acceptedMoneycorrectedcandidate; root22 owns sharedindex/scripts/generation/packageartifacts. No reinventory, sourcecompiler-driven fragment or hostedwork. Exact prompt `/tmp/ae-public-implementation-prompt-20260907.txt`. Known publicrelease-discovery/callhelper gaps are included; currentsource/protocol distinctions remain binding.

## Group9 inventory COMPLETE — implementation HELD for Money

All ten targeted completion files read; no unread targets remain. Same whole19/20/21ownership is ready for implementation after Money acceptance. Root22 shared ownership unchanged. Root qualification: AE-owned current public routes/action IDs/schema labels follow the approved cutover; calling something public or a protocol does not itself protect it. Preserve only actual external protocol, canonical evidence, opaque identifiers and explicitly protected versioned material. Earlier source ownership qualifications remain binding.

<details><summary>Completion addendum</summary>

Complete: all 10/10 targeted files read; unread targets: none. Receipt read once. No edits, tests, builds, generation, network/live smoke, commits, AgentMux, or subagents.

Environment verified: branch `codex/vocabulary-rationalisation`; Node `v22.22.0`; npm `11.5.1`.

| File | Additional finding |
|---|---|
| [route projection](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/src/modules/discovery/internal/developer-discovery-route-projection.ts>) | Group9 public discovery projection. Offering/Service facts remain distinct from Tools. Public schema/route identifiers are protocol; `publicServiceId` is a distinct telemetry/domain field. |
| [support matrix](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/src/modules/discovery/internal/developer-discovery-support-matrix.ts>) | Group9 support/kill-gate owner later. `canonicalDigest`, owner/admin refs, correlation IDs, and evidence refs are protected hash/opaque evidence. |
| [discovery types](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/src/modules/discovery/internal/developer-discovery-types.ts>) | Public DTO/schema boundary. Schema versions, route labels, `SourceHash`, and opaque identifiers retain exact meaning; no blanket compatibility exemption. |
| [offering manifest](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/src/modules/discovery/internal/offering-manifest.ts>) | AE-hosted UCP projection. `ae-ucp:v2`, catalog schema ID, `generatedHash`, and `offeringRef` are protocol/hash/Offering identity; do not collapse Offering into Tool. |
| [discovery schema route](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/src/routes/api.discovery.schema.ts>) | Read-only Group9 route/schema owner later. Expected route/schema values are exact public evidence. No live route execution was performed. |
| [call API](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/src/lib/server/call-api.ts>) | Target `tool.quote`, `tool.call`, and Call recovery routes are already cut over. `operation.invoke`, `commitmentRef`, and `invocationRef` inside canonical operation-key material are protected. Current telemetry `operationRef`/`invocationRef` is not blanket-exempt; producer dependency recorded. |
| [call contracts](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/src/modules/capability-execution/call-contracts.ts>) | `operation-commitment:v1` is an opaque/protocol identifier. Emitted wire refusal/receipt values preserve protocol/history; surrounding ordinary DTO names remain future cutover candidates. |
| [CLI manifest](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/tools/ae/commands/manifest.ts>) | Public/installed action descriptors and supply wrapper are Group9 ownership later, not permanent upstream exceptions. Preserve exact manifest protocol, recovery evidence/digest material, identity rules, and opaque refs. Money/Call dependencies are read-only consumers. |
| [official MCP client test](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/tests/unit/server/mcp-api-official-client.test.ts>) | Old `ae_operation_*` names and `OperationInvokeService` are public adapter/test dependencies for later Group9 migration. Opaque Tool/Call identities and actual MCP transport protocol remain protected. |
| [MCP protocol test](</Users/joelchan/Documents/Coding/App-Dev/live/01. Pre-Implementation/Agentic-Economy/tests/unit/server/mcp-api-protocol.test.ts>) | MCP JSON-RPC methods and error semantics are protected protocol. Embedded AE `ae_*` instruction/action descriptors are later Group9 migration scope, not permanent exceptions. |

Status updates:

- Whole ownership for 19/20/21 persists; root22 remains shared.
- Call remains independently source-accepted at 6/10.
- Money remains 18/26 with the sole-writer boundary active.
- Group9 implementation remains held until Money is accepted.
- Call/Money/MCP producer dependencies were recorded only; producers were not changed.
- Accepted Call baseline was treated as metadata-only; no hashes were dumped.
</details>


## Group9 inventory handoff — ten targeted reads remain; implementation HELD

Rootqualifications: CallnowSOURCEACCEPTED6/10, Moneysolewriteractive; allMoneyproducerseamsheld. Publicnamespaceddescriptors/supplylistwrapperAREgroup9owned, notindefinitelyupstreamheld. Protectexactcanonicalhash/signature/protocol/opaque/historicalevidenceonly; no blanketcurrenttelemetryorordinaryDTOcompatibilityexception. Currentpublicroute/actionIDsfollowapprovedcutover, historicalrecordedroutesremainhistoricalevidence. No hosted/live smoke authorized; listed production smokecommandisNOTpermissiontorun. Rootowns22nativegeneration/index/scripts/artifactsandNode22runtime; noNode20download/workaround. WorkerreadOnlycontinuedtocompleteexact10unreadfiles, nofreshinventory.

<details><summary>Read-only inventory receipt, qualified above</summary>

## Group9 inventory receipt — read-only

Status: inventory handoff complete enough for ownership; implementation has not advanced and nothing was released.

- Checkout: `codex/vocabulary-rationalisation`
- Node: `v22.22.0`
- npm: `11.5.1`
- No source, tracker, generated file, fixture, dependency, or lockfile was edited.
- No tests, compiler, build, generation, network, AgentMux, subagent, or commit was run.
- Existing dirty worktree changes were preserved.

Guidance read: `AGENTS.md`, `PRODUCT.md`, `CONTEXT.md`, vocabulary map, issues 19–22, and Convex AI guidelines.

### Ownership boundary

| Owner | Complete conceptual boundary |
|---|---|
| Issue19 | Public action registry, Tool/Quote/Call route descriptors, HTTP adapters/routes, MCP projection, Provider Tool-list public adapter, associated server/import/conformance tests |
| Issue20 | CLI source, retained command semantics, help/action adapters, local profiles, package README/manifest, CLI terminal tests |
| Issue21 | Discovery projections, `llms.txt`, `/SKILL.md`, UCP/API catalog, developer discovery, installed plugin, parity/release gateway harness source/tests |
| Issue22/root | Route-tree generation, Convex generated API, CLI `dist`, public CLI tarball, root/package scripts, shared indexes and native generation |
| Held upstream | Tool source/publication/storage, Provider supply internals, Quote/Call/Money producers and durable records |

This remains one whole group9 module owned through later implementation and corrective work. No micro-project split is implied.

### Canonical public action spine

Current definitions are concentrated in:

```text
src/modules/common/action.ts
src/modules/actions/contract.ts
src/modules/actions/index.ts
src/modules/actions/tool-contract.ts

src/modules/capability-supply/public.ts
src/modules/capability-supply/tool-projection.ts
src/modules/capability-supply/tool-schemas.ts
src/modules/capability-supply/internal/tool-projection-types.ts
src/modules/capability-supply/internal/tool-projection-wire.ts
src/modules/capability-supply/internal/tool-projection-wire-serialize.ts
src/modules/capability-supply/internal/tool-projection-wire-deserialize.ts
src/modules/capability-supply/internal/tool-project.ts
src/modules/capability-supply/internal/tool-search.ts
src/modules/capability-supply/internal/tool-detail-compare.ts

src/modules/registry/tool-action-contracts.ts
src/modules/registry/tool-choice-contracts.ts
src/modules/registry/tools.actions.ts
src/modules/registry/tool-entry.ts
src/modules/registry/tool-paths.ts
src/modules/common/market-tool-paths.ts
src/modules/registry/public.ts
```

The current Tool projection uses:

```text
CURRENT_TOOL_CALL_VIA = /api/v1/tools/call
```

Public action contracts:

```text
registry.tools.list       POST /api/v1/market-tools/list       registry.tools.list:v1
registry.tools.search     POST /api/v1/market-tools/search     registry.tools.search:v3
registry.tools.describe   POST /api/v1/market-tools/describe   registry.tools.describe:v1
registry.tools.compare    POST /api/v1/market-tools/compare   registry.tools.compare:v2

tool.quote                POST /api/v1/tools/quote             tool.quote:v2
tool.call                 POST /api/v1/tools/call              tool.call:v1
call.list                 GET  /api/v1/calls                   call.list:v1
call.status               GET  /api/v1/calls/{callRef}          call.status:v1
call.cancel               POST /api/v1/calls/{callRef}/cancel  call.cancel:v1
call.reconcile            POST /api/v1/calls/{callRef}/reconcile call.reconcile:v1
```

Definitions flow as:

```text
Action/schema definitions
  -> describeActionForAgent / JSON schema
  -> action registry
  -> HTTP and route descriptors
  -> MCP descriptors/tool names
  -> CLI adapters and machine manifest
  -> discovery projections and installed consumers
```

MCP names are mechanically derived in `src/modules/actions/index.ts`:

```text
ae_registry_tools_list
ae_registry_tools_search
ae_registry_tools_describe
ae_registry_tools_compare
ae_tool_quote
ae_tool_call
ae_call_list
ae_call_status
ae_call_cancel
ae_call_reconcile
```

No hand-maintained alias map or parallel API is authorized.

### HTTP/MCP producer and consumer paths

Public HTTP routes:

```text
src/routes/api.v1.market-tools.list.ts
src/routes/api.v1.market-tools.search.ts
src/routes/api.v1.market-tools.describe.ts
src/routes/api.v1.market-tools.compare.ts

src/routes/api.v1.tools.quote.ts
src/routes/api.v1.tools.call.ts
src/routes/api.v1.calls.ts
src/routes/api.v1.calls.$callRef.ts
src/routes/api.v1.calls.$callRef.cancel.ts
src/routes/api.v1.calls.$callRef.reconcile.ts

src/routes/mcp.ts
src/routes/api.v1.supply.tools.list.ts
```

Adapters and dispatch:

```text
src/lib/server/call-api.ts
src/lib/server/mcp-api.ts
src/lib/server/tool-read-request.ts
src/lib/server/tool-read-problem.ts
src/lib/server/supply-action-api.ts
src/modules/capability-supply/supply-actions.ts
```

`call-api.ts` owns the public adapter boundary and calls current backend source actions:

```text
capabilityQuotes:quote
capabilityCalls:call
capabilityCalls:listCalls
capabilityCalls:readCallStatus
capabilityCalls:cancelCall
capabilityCalls:reconcileCall
```

Those backend names and Call/Money semantics remain held upstream.

MCP uses the installed official SDK, action-derived schemas, annotations, authentication filtering, JSON-RPC handling, and structured tool results. Preserve official MCP methods, OAuth behavior, x402 fields, and SDK protocol errors.

### Tool wire and registry boundary

Tool source and serializers:

```text
src/modules/capability-supply/internal/tool-projection-wire-serialize.ts
src/modules/capability-supply/internal/tool-projection-wire-deserialize.ts
```

Exports are re-exposed through:

```text
src/modules/capability-supply/tool-projection.ts
src/modules/capability-supply/public.ts
src/modules/registry/public.ts
```

Registry actions project source Tool records through:

```text
readCapabilityToolSearch
readCapabilityToolDetail
readCapabilityToolCompare

projectToolListChoices
projectToolSearchChoices
projectToolDescription
projectToolCompareChoices
```

Underlying Tool choice output versions are currently:

```text
registry-tools:v3  list/search
registry-tools:v2  describe/compare
```

Underlying Tool projection/wire compatibility remains `registry-tools:v1`.

### Quote/Call dependency edge

Public contract files:

```text
src/modules/capability-execution/quote.ts
src/modules/capability-execution/quote.actions.ts
src/modules/capability-execution/call-entry.ts
src/modules/capability-execution/call.actions.ts
src/modules/capability-execution/call-contracts.ts
src/modules/capability-execution/call-history.actions.ts
src/modules/capability-execution/call-recovery-contracts.ts
src/modules/capability-execution/call-recovery.actions.ts
src/modules/capability-execution/call-authority.ts
```

Group9 consumes the public contract edge. Call dispatch, identity, durable lifecycle, and Money producers remain held.

The exact Call candidate receipt is preserved at:

```text
/tmp/ae-call-review-candidate-20260906/manifest.json
/tmp/ae-call-module-owned-paths-20260906.txt
/tmp/ae-call-final-coherent-checkpoint-20260906.log
```

The manifest captured 1,789 files with no changes during capture and records the exact 67-path Call boundary. Do not re-scope from the reported compiler error list.

### CLI ownership and installed package

CLI source boundary:

```text
tools/ae/cli.ts

tools/ae/lib/args.ts
tools/ae/lib/config.ts
tools/ae/lib/continuation-command.ts
tools/ae/lib/help.ts
tools/ae/lib/output.ts
tools/ae/lib/policy.ts
tools/ae/lib/suggested-continuation-adapter.ts
tools/ae/lib/tool-format.ts
tools/ae/lib/tool-read-failure.ts

tools/ae/commands/account.ts
tools/ae/commands/action-adapters.ts
tools/ae/commands/call.ts
tools/ae/commands/cancel.ts
tools/ae/commands/compare.ts
tools/ae/commands/config.ts
tools/ae/commands/connect.ts
tools/ae/commands/describe.ts
tools/ae/commands/doctor.ts
tools/ae/commands/fund.ts
tools/ae/commands/history.ts
tools/ae/commands/list.ts
tools/ae/commands/manifest.ts
tools/ae/commands/market-tools.ts
tools/ae/commands/recover.ts
tools/ae/commands/request.ts
tools/ae/commands/revoke.ts
tools/ae/commands/search.ts
tools/ae/commands/status.ts
tools/ae/commands/supply.ts
tools/ae/commands/wait.ts
```

Package surfaces:

```text
packages/cli/package.json
packages/cli/README.md
src/lib/cli-distribution.ts
tests/unit/discovery/cli-distribution.test.ts
```

Retained CLI semantics:

- `--supplier` becomes `--provider`, without an alias.
- Anonymous `describe` remains the public Tool-detail command.
- `call` performs Quote acquisition followed by `tool.call`; no `invoke` alias.
- `history` maps to `call.list`.
- `status`, `wait`, `cancel`, and `recover` retain their existing recovery meanings.
- `market_supply:manage`, origin binding, HTTPS/loopback rules, OAuth, x402, idempotency, pending, unknown outcomes, and recovery behavior remain intact.
- Provider CLI command structure is retained where already established; the underlying Provider action becomes `supply.tools.list`.

Direct terminal consumers and fixtures include:

```text
tests/unit/market-terminal/call.test.ts
tests/unit/market-terminal/cold-loop.test.ts
tests/unit/market-terminal/compare.test.ts
tests/unit/market-terminal/doctor.test.ts
tests/unit/market-terminal/history.test.ts
tests/unit/market-terminal/manifest-oauth.test.ts
tests/unit/market-terminal/recovery.test.ts
tests/unit/market-terminal/supply.test.ts
tests/unit/market-terminal/suggested-continuation-adapter.test.ts
tests/unit/market-terminal/cli-errors-operation-read.test.ts
```

Current ordinary stale CLI surface remains in `tools/ae/commands/manifest.ts`, `tools/ae/cli.ts`, `tools/ae/commands/status.ts`, `tools/ae/commands/call.ts`, `packages/cli/README.md`, and `packages/cli/package.json`, including user-facing Operation/inspection/invocation language and internal `invoke` error labels.

### Chat consumers

Backend chat tool definitions and execution:

```text
convex/chatTools.ts
src/modules/chat/tool-card.ts
src/modules/chat/schema.ts
```

The canonical chat set is:

```text
registry.tools.list
registry.tools.search
registry.tools.describe
registry.tools.compare
tool.quote
tool.call
```

Chat uses Tool deserializers, registry projections, Quote/Call schemas, provider-safe action names, bounded tool-call counts, output sanitization, stored-card serialization, and explicit pending/refused/reconciliation states.

UI consumers:

```text
src/components/ae/chat/Chat.tsx
src/components/ae/chat/ChatComposer.tsx
src/components/ae/chat/ChatHeader.tsx
src/components/ae/chat/ChatHistory.tsx
src/components/ae/chat/ChatTranscript.tsx
src/components/ae/chat/SharedChat.tsx
src/components/ae/chat/ToolCard.tsx
src/components/ae/chat/presentation.ts
```

Relevant tests:

```text
tests/unit/chat/chat-agent-tools.test.ts
tests/unit/chat/chat-system.test.ts
tests/unit/chat/operation-call-handback.test.tsx
tests/unit/chat/ae-agent-json-affordance.test.tsx
tests/unit/chat/chat-provider-contract.test.ts
tests/unit/chat/chat-provider-boundary.test.tsx
```

Chat hash material, correlation prefixes, `operationRef`, `commitmentRef`, `invocationRef`, and generated idempotency identity are protected evidence/current compatibility where used for attribution or canonical identity. The local `operationSourceFilters` name is ordinary stale implementation vocabulary.

### Command-panel consumers

```text
src/components/ae/command-panel/market-tools-client.ts
src/components/ae/command-panel/pages/ToolsSearchPage.tsx
src/components/ae/command-panel/pages/ToolDetailPage.tsx
src/components/ae/command-panel/recent-tools.ts
src/components/ae/command-panel/command-panel-state.ts
src/components/ae/command-panel/CommandPanelProvider.tsx
src/components/ae/command-panel/AeCommandPanel.tsx
src/components/ae/layout/AePublicShell.tsx
src/components/ae/layout/AeOperatorShell.tsx
src/components/ae/agent-access/AeAgentAccessAuthorizeForm.tsx
```

The panel calls the canonical anonymous Tool search path and the canonical Tool detail server function. It stores only validated opaque Tool refs locally.

Related public Tool-detail consumers are already on the Tool route vocabulary:

```text
src/modules/registry/tool-detail-route.functions.ts
src/routes/tools.tsx
src/routes/tools.$toolRef.tsx
src/routes/market.tsx
src/components/ae/market/tool-detail/
```

Tests:

```text
tests/unit/command-panel/command-panel.test.tsx
tests/unit/layout/public-shell-command-panel.test.tsx
```

### Discovery and installed-plugin ownership

Discovery producers:

```text
src/modules/discovery/public.ts
src/modules/discovery/developer-discovery.ts
src/modules/discovery/site-manifest-version.ts

src/modules/discovery/internal/agent-skill.ts
src/modules/discovery/internal/api-catalog.ts
src/modules/discovery/internal/developer-discovery-route-projection.ts
src/modules/discovery/internal/developer-discovery-support-matrix.ts
src/modules/discovery/internal/developer-discovery-types.ts
src/modules/discovery/internal/discovery-files.ts
src/modules/discovery/internal/manifest-projection.ts
src/modules/discovery/internal/offering-discovery-file.ts
src/modules/discovery/internal/offering-manifest.ts
src/modules/discovery/internal/page-markdown.ts
src/modules/discovery/internal/schema-values.ts
src/modules/discovery/internal/site-manifest.ts
src/modules/discovery/internal/tool-contract.ts
```

The former `operation-contract.ts` boundary is currently represented by `tool-contract.ts`.

Served discovery routes:

```text
src/routes/[.]well-known/api-catalog.ts
src/routes/[.]well-known/ucp.ts
src/routes/SKILL[.]md.ts
src/routes/api.discovery.examples.ts
src/routes/api.discovery.schema.ts
src/routes/llms[.]txt.ts
```

Installed surface:

```text
plugins/agentic-economy/skills/use-agentic-economy/SKILL.md
```

`src/modules/discovery/internal/agent-skill.ts` imports the installed plugin content directly for `/SKILL.md`; they are one maintained instruction surface.

Discovery projections currently derive from canonical sources:

```text
DiscoveryPublicSurfacePaths
  -> llms.txt, API catalog, site indexes

listCallRouteDescriptors / callRouteExamples
  -> UCP manifest, page markdown, machine examples

listMcpActionDescriptors
  -> UCP MCP documentation

site manifest + Tool action contracts
  -> route/method/auth/schema projections
```

Current installed plugin remains stale in several places: `ae_operation_inspect`, `ae_operation_invoke`, old Commitment/Invocation wording, and old recovery tool names.

Discovery tests and parity consumers:

```text
tests/unit/discovery/api-catalog.test.ts
tests/unit/discovery/page-markdown.test.ts
tests/unit/discovery/offering-llms-index.test.ts
tests/unit/discovery/developer-discovery-parity.test.ts
tests/unit/discovery/developer-discovery-kill-rules.test.ts
tests/unit/discovery/site-discovery-manifest.test.ts
tests/unit/discovery/developer-discovery-route.test.ts
tests/unit/discovery/developer-discovery-support-matrix.test.ts
tests/unit/discovery/cli-distribution.test.ts

tests/integration/api-catalog-route.test.ts
tests/integration/developer-discovery.test.ts
tests/integration/discovery-route-parity.test.ts
tests/integration/discovery-routes.test.ts
tests/integration/discovery-llms-offering-parity.test.ts
eval/parity/check-parity.mjs
```

`eval/parity/check-parity.mjs` is static parity logic only; it was not executed.

Release gateway harness ownership under issue21:

```text
tools/release/tool-gateway-production-smoke.ts
tools/release/tool-gateway-production-smoke-discovery.ts
tools/release/tool-gateway-production-smoke-hosted-owner.ts
tools/release/tool-gateway-production-smoke-hosted-runtime.ts
tools/release/tool-gateway-production-smoke-hosted-money.ts
tools/release/tool-gateway-production-smoke-call.ts
tools/release/tool-gateway-production-smoke-money.ts
tools/release/tool-gateway-production-smoke-receipt.ts
tools/release/validate-tool-gateway-production-smoke-receipt.ts
```

Existing release test files still carry the former `operation-gateway-production-smoke-*` filenames and require the bounded mechanical move. `tests/unit/release/payout-provider-replay.test.ts` is a direct import consumer.

Retain the dated filenames:

```text
tools/release/package5-provider-operations.ts
tests/unit/release/package5-provider-operations.test.ts
```

### Approved vocabulary mapping

```text
registry.operations.*              -> registry.tools.*
operation.inspect                   -> tool.quote
operation.invoke                    -> tool.call
operation.list                      -> call.list
operation.status/cancel/reconcile   -> call.status/cancel/reconcile

/api/v1/market-operations/*         -> /api/v1/market-tools/*
/api/v1/operations/inspect           -> /api/v1/tools/quote
/api/v1/operations/call              -> /api/v1/tools/call
GET /api/v1/operations               -> GET /api/v1/calls
/api/v1/operations/{invocationRef}  -> /api/v1/calls/{callRef}
/cancel and /reconcile suffixes     -> same Call suffixes

market_operations:invoke            -> market_tools:call
inspect_only                        -> read_only
approve_each                        -> approval_required
bounded_mandate                     -> spending_policy
full_yolo                           -> unrestricted_test_only
mandate_eligible                    -> policy_eligible

customer_requests:* remains the
canonical request-authorization scope family.

supply.operations.list              -> supply.tools.list
supply.operations.list:v1           -> supply.tools.list:v1
/api/v1/supply/operations/list      -> /api/v1/supply/tools/list
operationsList dispatch             -> toolsList dispatch
```

No alias or parallel endpoint should be introduced.

### Protected exceptions

Retain these by meaning:

- Upstream OpenAPI `operationId`.
- Official MCP methods and SDK protocol fields.
- OAuth fields and standards.
- x402 payment fields and namespaces.
- Opaque ref prefixes currently used by evidence and compatibility:
  `operation:v1:`, `operation-commitment:v1:`, and `invocation:v1:`.
- Canonical hash/signature material, including `operation.invoke` contract labels, `operationRef`, `commitmentRef`, and `invocationRef` where they are part of identity, telemetry, reconciliation evidence, or historical records.
- Evidence kinds such as `action_invocation_reconciliation`, `operation-attempt`, and `operation-authority-use`.
- Chat canonical digest material and `chat-inspect-corr`, `chat-invoke-corr`, and `chat-invoke` prefixes.
- External registry `/api/v1/registry`, `api-registry:v1`, imported record/link fields, and external source vocabulary.
- `PublicToolDescriptor.operationId` as source identity.
- Generic IAM `Principal`, `Account`, `Business`, `User`, `Credential`, and `DelegationGrant`.
- Provider, Seller, payment recipient, Charge, Provider obligation, payable amount, Payout, delivery status, payment status, and Purchase status as separate facts.
- Historical release receipt filenames, generic `GatewaySmoke` names, and recorded `/api/v1/release/operation-gateway` evidence.

These protected values are not public aliases and must not be recursively renamed.

### Ordinary current AE names still requiring bounded work

Confirmed ordinary remaining vocabulary includes:

```text
src/modules/actions/index.ts
src/modules/capability-supply/supply-actions.ts
tools/ae/commands/doctor.ts
tests/unit/actions/registry.test.ts
tests/unit/server/mcp-api-tools-list.test.ts
tests/unit/server/mcp-api-operation-invoke.test.ts
tests/unit/server/mcp-api-supply.test.ts
tests/unit/market-terminal/compare.test.ts
tests/unit/market-terminal/history.test.ts
tests/unit/market-terminal/supply.test.ts
tests/unit/market-terminal/doctor.test.ts
tests/unit/discovery/page-markdown.test.ts
tests/integration/api-catalog-route.test.ts
tests/unit/discovery/cli-distribution.test.ts
tools/ae/commands/manifest.ts
tools/ae/cli.ts
tools/ae/commands/status.ts
tools/ae/commands/call.ts
packages/cli/README.md
packages/cli/package.json
plugins/agentic-economy/skills/use-agentic-economy/SKILL.md
```

Examples are old public descriptions, action IDs, supply paths, test fixture field names, CLI manifest labels, `invoke` command internals, `operationSourceFilters`, and route body constants named `MAX_OPERATION_*`. Each must be handled only under the approved mapping; no new vocabulary is inferred for unresolved refusal/evidence identifiers.

### Root/shared generated handoff

Root/issue22 exclusively owns:

```text
convex/_generated/api.js
convex/_generated/api.d.ts
convex/_generated/server.js
convex/_generated/server.d.ts
convex/_generated/dataModel.d.ts
src/routeTree.gen.ts
packages/cli/dist/ae.js
public/downloads/agentic-economy-cli-0.1.0.tgz
```

Generator/package inputs:

```text
scripts/build-cli.mjs
scripts/test-cli-package.mjs
tools/release/release-integrity.ts
.nvmrc
convex.json
package.json
packages/cli/package.json
```

Required root sequencing:

```text
issue20 CLI source/package
  -> issue21 discovery/release source
  -> build:cli
  -> pack:cli:public
  -> root-owned dist/tarball validation

accepted route sources
  -> native route-tree generation

accepted Convex sources
  -> approved Convex code generation
```

Relevant existing root commands, all not run:

```text
npm run build:cli
npm run pack:cli:public
npm run test:cli-package
npm run test:imports
npm run test:conformance
npm run test:chat:conformance
npm run test:release:architecture
npm run test:release:package5
npm run parity:check
npm run typecheck
npm run check:convex-codegen
npm run generate:convex
npm run smoke:gateway:production
npm run validate:release:gateway
```

The current root `package.json` already shows several target path references in the dirty checkout, but that is not acceptance evidence. No generated output was regenerated.

### Held upstream seams

- Call producer boundary: independent review, exact 67-path receipt above.
- Money group8 inventory: held for Call acceptance at `/tmp/ae-money-inventory-result-20260906.txt`, issue18/26.
- All Call and Money producer seams remain held.
- Provider supply list is still an upstream dependency: `supplyOperationsListAction` and `SUPPLY_ACTION_ROUTE_CONTRACTS.operationsList` remain in `supply-actions.ts`; group9 records the required public propagation but does not invent a second Provider action.
- Existing policy, connection, catalogue, Provider, and Quote work remains upstream-owned and is consumed as dependency truth.
- Storage, publication, durable record, and Convex producer contracts remain outside group9.

### Targeted unread items

The inventory boundary is established, but these bounded reads should precede implementation:

```text
src/modules/discovery/internal/developer-discovery-route-projection.ts
src/modules/discovery/internal/developer-discovery-support-matrix.ts
src/modules/discovery/internal/developer-discovery-types.ts
src/modules/discovery/internal/offering-manifest.ts
src/routes/api.discovery.schema.ts
src/lib/server/call-api.ts
src/modules/capability-execution/call-contracts.ts
tools/ae/commands/manifest.ts
tests/unit/server/mcp-api-official-client.test.ts
tests/unit/server/mcp-api-protocol.test.ts
```

These are targeted completion reads, not new ownership boundaries and not a reason to restart the inventory.

No implementation, test pass, compiler result, build result, generation result, or release claim is made.
</details>


## Group9 read-only preparation

Read-only Luna Max inventory of wholepublic/installedboundary19/20/21 started whileCallcandidateunderreview. No source/tests/build/generation changes; Call/MoneyproducersHELD, implementationnotreleased. Sameconceptualownerwillcontinueafterupstreamacceptance. Rootowns22sharedartifacts. CLI01a0774b-d68e-7003-81ec-d7dd6634337c; prompt /tmp/ae-public-inventory-prompt-20260906.txt.


Type: task
Label: wayfinder:task
Mode: AFK
Status: resolved
Assignee:
Assigned role: Public HTTP/MCP contract owner
Parent: ../map.md
Blocked by: 08, 10, 11, 12, 13, 14, 15, 16, 17, 18, 29, 30

## Outcome

### Bounded source handoffs and current claim — 2026-09-05

Quote/Call declarations were handed back by `vocab_call_contracts_01` in
seven assigned files: static contract/whitespace checks passed, one compaction.
Tool registry declarations and four direct callers have separate narrow-lint
receipts under issue 13. Parent acceptance remains open.

`vocab_recovery_actions_02` now claims exactly
`src/modules/capability-execution/call-recovery.actions.ts`,
`src/modules/capability-execution/call-recovery.functions.ts`,
`src/modules/actions/index.ts`,
`tests/unit/capability-execution/call-recovery-actions.test.ts`,
`tests/unit/server/mcp-api-call-recovery.test.ts` and
`tests/unit/market-terminal/recovery.test.ts`. Rename recovery action symbols
and direct consumers to Call terminology; restore central navigation export
to its declared Tool name. Preserve service methods and backend RPC names for
their owning next batch. Preserve signed evidence's original `invocationRef`
and `operationRef` keys. The recovery-actions test is the focused batch check;
the other two tests receive direct symbol propagation only and are not claimed
as verified by association. No generated or release work is assigned.

Update the AE-owned HTTP and MCP boundary after the Customer/Agent, action
execution, authority, Tool, Provider, Quote, Call, outcome and durable-field
owners have completed their serialized core work. The public action registry,
route descriptors, HTTP adapters, route files, MCP classifier and contract
tests must expose one exact target vocabulary and preserve existing behavior.
This ticket implements the public contract; generated packages, discovery
copy, screens and hosted proof remain downstream work.

## Fixed contract and protected values

Use the exact mappings in
`docs/designs/vocabulary-rationalisation.md:270-291`: `registry.operations.*`
to `registry.tools.*`; `operation.inspect` to `tool.quote`;
`operation.invoke` to `tool.call`; `operation.list` to `call.list`;
`operation.status/cancel/reconcile` to `call.status/cancel/reconcile`;
`/api/v1/market-operations/*` to `/api/v1/market-tools/*`;
`/api/v1/operations/inspect` to `/api/v1/tools/quote`;
`/api/v1/operations/call` to `/api/v1/tools/call`;
`GET /api/v1/operations` to `GET /api/v1/calls`; and the invocation route and
recovery suffixes to `/api/v1/calls/{callRef}`. Rename AE-owned fields
`operationRef`/`commitmentRef`/`invocationRef` to `toolRef`/`quoteRef`/`callRef`
only after their core owners make the corresponding schemas available.

Retain HTTP methods, `/mcp`, MCP JSON-RPC methods and official SDK semantics,
OAuth standard fields, x402 payment fields, upstream OpenAPI `operationId`,
opaque identifiers, hash/signature material, external financial namespaces,
portfolio Service APIs, market-request APIs and all role/financial/delivery
distinctions listed in the accepted plan. MCP tool names remain mechanically
derived from action IDs (`ae_registry_tools_*`, `ae_tool_quote`,
`ae_tool_call`, `ae_call_list/status/cancel/reconcile`); do not hand-maintain a
second name map. Keep `market_supply:manage` unchanged unless the coordinator
explicitly decides a replacement; it is not a free vocabulary choice here.

Authority values are AE-owned and must use the accepted mapping:
`inspect_only` -> `read_only`, `approve_each` -> `approval_required`,
`bounded_mandate` -> `spending_policy`, `full_yolo` ->
`unrestricted_test_only`, `mandate_eligible` -> `policy_eligible`, and
`market_operations:invoke` -> `market_tools:call` with the corresponding
`customer_requests:` mode suffixes. Preserve `offline_access` and OAuth
standard values. A Quote must not require a reusable spending policy when the
supported path is request authorization/Approval.

## Finite implementation allowlist

Only the following source and maintained contract-test paths are in scope. The
target-named route files are the explicit replacements for the listed current
route files; do not sweep adjacent directories.

### Public action and contract sources

- `src/modules/actions/index.ts`
- `src/modules/actions/contract.ts`
- `src/modules/agent-access/contract.ts`
- `src/modules/capability-execution/quote.ts` (issue 15's `operation-commitment.ts`)
- `src/modules/capability-execution/quote.actions.ts` (issue 15's `operation-commitment.actions.ts`)
- `src/modules/capability-execution/call-entry.ts` (issue 16's `operation-invoke-entry.ts`)
- `src/modules/capability-execution/call-contracts.ts` (issue 16's `operation-invoke-contracts.ts`)
- `src/modules/capability-execution/call.actions.ts` (issue 16's `operation-invoke.actions.ts`)
- `src/modules/capability-execution/call-history.actions.ts` (issue 16's `operation-history.actions.ts`)
- `src/modules/capability-execution/call-recovery-contracts.ts` (issue 16's `operation-recovery-contracts.ts`)
- `src/modules/capability-execution/call-recovery.actions.ts` (issue 16's `operation-recovery.actions.ts`)
- `src/modules/capability-execution/call-authority.ts` (issue 16's `operation-invoke.ts`)
- `src/modules/registry/tool-entry.ts` (issue 13's `operation-entry.ts`)
- `src/modules/registry/tool-paths.ts` (issue 13's `operation-paths.ts`)
- `src/modules/registry/tool-action-contracts.ts` (issue 13's `operation-action-contracts.ts`)
- `src/modules/registry/tools.actions.ts` (issue 13's `operations.actions.ts`)
- `src/modules/common/market-tool-paths.ts` (issue 13's `market-operation-paths.ts`)

### HTTP/MCP adapters and route bindings

- `src/lib/server/call-api.ts`
- `src/lib/server/mcp-api.ts` (including operation-prefix classifier and
  action-derived MCP admission)
- `src/routes/mcp.ts`
- `src/routes/api.v1.operations.ts`
- `src/routes/api.v1.operations.inspect.ts`
- `src/routes/api.v1.operations.call.ts`
- `src/routes/api.v1.operations.$invocationRef.ts`
- `src/routes/api.v1.operations.$invocationRef.cancel.ts`
- `src/routes/api.v1.operations.$invocationRef.reconcile.ts`
- `src/routes/api.v1.market-operations.list.ts`
- `src/routes/api.v1.market-operations.search.ts`
- `src/routes/api.v1.market-operations.describe.ts`
- `src/routes/api.v1.market-operations.compare.ts`
- `src/routes/api.v1.tools.quote.ts`
- `src/routes/api.v1.tools.call.ts`
- `src/routes/api.v1.calls.ts`
- `src/routes/api.v1.calls.$callRef.ts`
- `src/routes/api.v1.calls.$callRef.cancel.ts`
- `src/routes/api.v1.calls.$callRef.reconcile.ts`
- `src/routes/api.v1.market-tools.list.ts`
- `src/routes/api.v1.market-tools.search.ts`
- `src/routes/api.v1.market-tools.describe.ts`
- `src/routes/api.v1.market-tools.compare.ts`


### Exact file moves owned by issue 19

- `src/lib/server/operation-invoke-api.ts` → `src/lib/server/call-api.ts`
- `tests/unit/server/operation-invoke-api.test.ts` → `tests/unit/server/call-api.test.ts`
- `tests/unit/server/operation-recovery-api.test.ts` → `tests/unit/server/call-recovery-api.test.ts`
- `tests/unit/server/operation-history-api.test.ts` → `tests/unit/server/call-history-api.test.ts`
- `tests/unit/server/mcp-api-operation-recovery.test.ts` → `tests/unit/server/mcp-api-call-recovery.test.ts`
- `tests/unit/routes/operation-invoke-route-binding.test.ts` → `tests/unit/routes/tool-call-route-binding.test.ts`

Issue 13 already moves `tests/imports/operation-surface-conformance.test.ts` to
`tests/imports/tool-surface-conformance.test.ts`; issue 19 consumes that target
without another file rename.

Update every existing import of the HTTP adapter in the listed route/MCP/test
files, including `tests/integration/capability-call-workpool.test.ts`
(post-16), in this same patch. This additional integration file is an owned
mechanical-import slice, not a change to workpool behavior. `package.json` is
read-only here: give issue 22's sole root-package writer the exact
`test:conformance` Call-API test-path receipt. Its early checkpoint applies
the receipt before accepting checks; no new scripts or dependencies. Add the moved workpool test to the
focused run. Preserve all existing test assertions.

### Existing contract tests

- `tests/unit/server/mcp-api-official-client.test.ts`
- `tests/unit/server/mcp-api-protocol.test.ts`
- `tests/unit/server/mcp-api-call-recovery.test.ts`
- `tests/unit/server/call-api.test.ts`
- `tests/unit/server/call-recovery-api.test.ts`
- `tests/unit/routes/tool-call-route-binding.test.ts`
- `tests/unit/server/call-history-api.test.ts`
- `tests/unit/market-terminal/cold-loop.test.ts`
- `tests/imports/tool-surface-conformance.test.ts`

The `operation-*.ts` and `api.v1.operations*` entries above are the current
pre-cutover anchors. Issues 13–16 must hand the coordinator the exact
before/after path pair for every core-renamed file before this ticket is
dispatched; the after paths must be added literally here or in the dispatch
record, never inferred with a wildcard. The target route entries listed above
are already the explicit HTTP replacements.

## Implementation steps that must not be skipped

1. **F1 MCP classifier:** audit every operation-prefix special case in
   `src/lib/server/mcp-api.ts` before changing action IDs. Update recognition to
   the declared `tool`/`call` IDs while preserving generic-action versus
   catalogue-read classification, required authority modes, authentication
   challenges and action-derived names.
2. Keep `tools/list` and `tools/call` dispatch aligned for Quote, Call and
   `call.status/cancel/reconcile`. Verify reconciliation terminal events remain
   one durable Call outcome, not a second effect or an unlisted recovery tool.
3. Propagate the declared Quote v2 contract to route files, schemas, errors and
   version details; do not invent a new version while moving from
   `operation.inspect` to `tool.quote`.
4. Run the focused HTTP/MCP/recovery tests first. Treat the CLI portions of
   `tests/imports/tool-surface-conformance.test.ts` and the full
   conformance result as the post-issue-20/22 integration checkpoint; do not
   demand an independently green CLI half before issue 20 updates it.
5. Preserve the existing HTTP command hash material in
   `src/lib/server/call-api.ts`'s `operationKeyFor`: its
   `contract` field currently receives the literal `operation.invoke` through
   `OPERATION_INVOKE_ACTION_ID`. The public action becomes `tool.call`, but
   this protected canonical field remains `operation.invoke`. Keep the
   existing key builder and add a literal before/after operation-key vector to
   the existing server test. This is a necessary adjustment at an existing
   encoder, not a public alias or a compatibility framework. Predecessors
   13/15/16 own their top-level Tool/Quote/Call reference projections here.
   The Quote's `input` is opaque Tool/Provider JSON, not an AE authorization
   envelope: preserve it exactly, including keys that happen to match old
   vocabulary. Never use recursive key substitution on customer input.

## Explicit exclusions and sequencing

Do not edit CLI command consumers, package README/dist/public archives,
discovery/llms/SKILL/plugin copy, screens, current README, generated Convex or
router output, test data, deployment state, Package 6/7 records or the plan,
map and work record. Do not add a legacy alias, new route hierarchy, SDK,
dependency or error framework. `operation.list` maps to `call.list`; the CLI
verb `history` remains owned by issue 20. The anonymous CLI `describe` remains
registry Tool detail and is not promoted to caller-specific `tool.quote`.

Issues 10–18 provide the core fields and producers before this ticket runs.
Issues 29 and 30 are review gates. Issue 22 owns generation after this source
contract lands. Issues 20 and 21 may prepare disjoint consumers in parallel,
but must not edit these paths or invent a pre-contract mapping.

## Verification commands and expected results

Use Node 22 and npm 11.5.1 through the project NVM runner for all commands:

- `NODE_VERSION=22 "$HOME/.nvm/nvm-exec" npm run typecheck` — pass with the
  target action/route schemas.
- `NODE_VERSION=22 "$HOME/.nvm/nvm-exec" npm run test:types` — pass without
  weakening protected vectors or protocol types.
- `NODE_VERSION=22 "$HOME/.nvm/nvm-exec" npx vitest run
  tests/unit/server/mcp-api-official-client.test.ts
  tests/unit/server/mcp-api-protocol.test.ts
  tests/unit/server/mcp-api-call-recovery.test.ts
  tests/unit/server/call-api.test.ts
  tests/unit/server/call-recovery-api.test.ts
  tests/unit/routes/tool-call-route-binding.test.ts
  tests/unit/server/call-history-api.test.ts
  tests/unit/market-terminal/cold-loop.test.ts
  tests/integration/capability-call-workpool.test.ts --no-file-parallelism` — pass for the focused
  HTTP/MCP/action/recovery boundary.
- `NODE_VERSION=22 "$HOME/.nvm/nvm-exec" npm run test:conformance` — run at
  the issue 20/22 integration checkpoint; do not use it to claim the CLI half
  is independently complete before issue 20.
- `NODE_VERSION=22 "$HOME/.nvm/nvm-exec" npm run test:imports` — run at that
  same integration checkpoint after CLI consumers and generated artifacts are
  updated; this is the repository's import/conformance command and it also
  serializes the CLI build owned by issue 22.
- `git diff --check -- src/modules/actions/index.ts
  src/modules/actions/contract.ts src/modules/agent-access/contract.ts
  src/modules/capability-execution/quote.ts
  src/modules/capability-execution/quote.actions.ts
  src/modules/capability-execution/call-entry.ts
  src/modules/capability-execution/call-contracts.ts
  src/modules/capability-execution/call.actions.ts
  src/modules/capability-execution/call-history.actions.ts
  src/modules/capability-execution/call-recovery-contracts.ts
  src/modules/capability-execution/call-recovery.actions.ts
  src/modules/capability-execution/call-authority.ts
  src/modules/registry/tool-entry.ts
  src/modules/registry/tool-paths.ts
  src/modules/registry/tool-action-contracts.ts
  src/modules/registry/tools.actions.ts
  src/modules/common/market-tool-paths.ts
  src/lib/server/call-api.ts src/lib/server/mcp-api.ts
  src/routes/mcp.ts src/routes/api.v1.operations.ts
  src/routes/api.v1.operations.inspect.ts src/routes/api.v1.operations.call.ts
  src/routes/api.v1.operations.\$invocationRef.ts
  src/routes/api.v1.operations.\$invocationRef.cancel.ts
  src/routes/api.v1.operations.\$invocationRef.reconcile.ts
  src/routes/api.v1.market-operations.list.ts
  src/routes/api.v1.market-operations.search.ts
  src/routes/api.v1.market-operations.describe.ts
  src/routes/api.v1.market-operations.compare.ts
  src/routes/api.v1.tools.quote.ts src/routes/api.v1.tools.call.ts
  src/routes/api.v1.calls.ts src/routes/api.v1.calls.\$callRef.ts
  src/routes/api.v1.calls.\$callRef.cancel.ts
  src/routes/api.v1.calls.\$callRef.reconcile.ts
  src/routes/api.v1.market-tools.list.ts
  src/routes/api.v1.market-tools.search.ts
  src/routes/api.v1.market-tools.describe.ts
  src/routes/api.v1.market-tools.compare.ts
  tests/unit/server/mcp-api-official-client.test.ts
  tests/unit/server/mcp-api-protocol.test.ts
  tests/unit/server/mcp-api-call-recovery.test.ts
  tests/unit/server/call-api.test.ts
  tests/unit/server/call-recovery-api.test.ts
  tests/unit/routes/tool-call-route-binding.test.ts
  tests/unit/server/call-history-api.test.ts
  tests/unit/market-terminal/cold-loop.test.ts
  tests/imports/tool-surface-conformance.test.ts` — no whitespace errors.

Do not start Convex, deploy, push schema, use `dev --once`, or exercise a
hosted target from this ticket. Any local backend/codegen activity requires the
local-target and activity-safety decision from issue 31 and belongs to issue
22. Record the supplied issue 08 baseline separately from refactor results.

## Acceptance

- [ ] One public action/route registry emits the exact fixed target map; no
      parallel old AE-owned API or compatibility alias remains.
- [ ] Quote v2 remains v2 under the target action name; route files, params,
      error/version details and declared schemas agree without inventing a new
      version.
- [ ] `toolRef`, `quoteRef` and `callRef` propagate through inputs, outputs,
      paths, recovery and conformance while opaque/hash/protocol values stay
      byte-for-byte protected.
- [ ] MCP names derive from action IDs; `/mcp`, initialize/tools/list/tools/call,
      DELETE, structured errors, authentication and OAuth/x402 fields retain
      their established behavior. F1 classifier prefix recognition covers
      Quote, Call and recovery, and reconciliation terminal events remain one
      durable Call outcome.
- [ ] Authority modes/scopes use the accepted values; Quote, request
      authorization/Approval and spending policy remain distinct.
- [ ] Invalid JSON, invalid arguments, auth/scope refusal, idempotency
      conflict, pending/unknown result, status, cancel and reconcile preserve
      problem media, correlation, Retry-After and no-duplicate-Call semantics.
- [ ] All listed focused tests and type/conformance checks pass; no unrelated
      source or protected protocol surface changes.

## Closure evidence

Attach the changed-path list, exact route/action/ref mapping table, MCP derived
name receipt, contract-version/error receipt, authority scope/mode receipt,
focused test results and `git diff --check`. Mark downstream package,
discovery, live-QA and hosted evidence as pending their owners; do not claim
those from this ticket.

## Comments

### Chat contract claim — 2026-09-05

`vocab_chat_contracts_01` owns exactly `convex/chatTools.ts`,
`src/modules/chat/tool-card.ts` and `tests/unit/chat/chat-agent-tools.test.ts`.
Map chat action IDs and existing Quote/Tool contract imports to the already
declared names; provider-safe names stay derived through the existing helper.
Preserve installed SDK behavior, admission and limits. The chat Quote/purchase
command hashes must retain original `operationRef`/`commitmentRef` material
keys and `chat-inspect-corr`/`chat-invoke-corr`/`chat-invoke` prefixes; outgoing
commands retain target fields. This corrects the bulk pass at the existing
hash owners, without a serializer or compatibility layer. Public card types,
structured property and stored card discriminator remain a separate consumer
batch. Narrow static/lint checks only; chat behavior tests are updated but unrun.

### Bounded recovery handoff and descriptor claim — 2026-09-05

`vocab_recovery_actions_02` completed its six-file assignment: focused recovery
action suite 16/16 passed, narrow source lint passed, zero compactions. Other
MCP/CLI fixtures remain unrun and incomplete beyond direct symbol propagation.

`vocab_route_descriptors_03` owns exactly `src/modules/actions/index.ts`,
`src/modules/discovery/internal/tool-contract.ts`, `tools/ae/commands/manifest.ts`,
`convex/capabilitySupplyToolQueries.ts`,
`tests/imports/tool-surface-conformance.test.ts` and
`tests/unit/market-terminal/manifest-oauth.test.ts`. Rename
`PublicOperationRouteDescriptor` and its Base to `PublicCallRouteDescriptor`,
`listOperationRouteDescriptors` to `listCallRouteDescriptors`, and the remaining
navigation constant caller to `CURRENT_TOOL_PROJECTION_NAVIGATION`. Existing
methods, routes, registrations and protected evidence are unchanged. Narrow
lint/stale-symbol/whitespace checks only; no test or generated-output claim.

### Coordinator assignment — Provider Tool inventory contract

The existing AE-owned supply inventory lists canonical Tools, not portfolio
Services. Include this exact additional contract propagation in issue 19:

| Current | Target |
| --- | --- |
| `supply.operations.list` | `supply.tools.list` |
| `supply.operations.list:v1` | `supply.tools.list:v1` |
| `/api/v1/supply/operations/list` | `/api/v1/supply/tools/list` |
| `src/routes/api.v1.supply.operations.list.ts` | `src/routes/api.v1.supply.tools.list.ts` |
| supply dispatch key `operationsList` | `toolsList` |

Additional exact editable callers/tests: `src/modules/capability-supply/supply-actions.ts`,
`src/lib/server/supply-action-api.ts`, `tests/unit/actions/registry.test.ts`,
`tests/unit/release/package5-provider-operations.test.ts`,
`tests/unit/server/supply-action-api.test.ts`, and
`tests/unit/server/mcp-api-tools-list.test.ts`. Issue 13 owns underlying Tool
symbols/fields first; issue 20 owns `tools/ae/commands/doctor.ts` and its
`tests/unit/market-terminal/doctor.test.ts` consumer; issue 21 owns derived
discovery. This adds no endpoint: preserve method/count, other supply verbs,
`market_supply:manage`, authentication and problem envelopes. Issue 22 owns
router generation before typecheck. Include the three named server/action tests
and the existing Package 5 test in the focused Vitest acceptance run; all pass.

- 2026-09-05 — Prepared as the single public HTTP/MCP contract owner after the
  bounded DX review. Current pre-cutover names are baseline; implementation
  begins only after issues 10–18, 29 and 30 satisfy their gates.
